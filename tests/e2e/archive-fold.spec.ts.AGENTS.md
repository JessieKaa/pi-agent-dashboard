# archive-fold.spec.ts — index

L3 for the per-folder `Archive (N)` fold (test-plan #F4, #F8, #F9, #F10, #F14, #F15, #X8, #P3): seeds archived sidecars out-of-band via Docker exec, asserts lazy listing, restore, delete, read-only open, cross-client eviction, and worktree folding. See change: archive-sessions-lazy-load.
