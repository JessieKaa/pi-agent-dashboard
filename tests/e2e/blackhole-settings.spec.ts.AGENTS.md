# blackhole-settings.spec.ts — index

L3 spec (changes: add-blackhole-plugin, blackhole-model-picker-chains). Covers test-plan X3 + F1-F9 for `/settings/plugins/blackhole` and model-picker-chains F1-F11. F6 (extension absent) runs against the REAL unrouted harness state — `pi-blackhole` is deliberately never installed, so the requirement probe reports it missing and the plugin's OWN not-installed state renders. Every other row `page.route`s `/api/plugins` (rewrites `missingRequirements` to `[]`, keeping all other rows authentic), `/api/plugins/blackhole/config` (config fixture or 409 parse-error), and `/api/models` (credentialed registry catalogue), then asserts rendered DOM + accessibility tree: keyboard-only reorder convergence, boundary controls disabled-not-absent, accessible names naming the model, `data-excluded` tail vs `sessionFallback`, no-restart copy, host save-bar dirty/discard, absence of per-session content, empty chain state with add button (F2), keyboard add to empty chain (F1), model selector without free-text inputs in labelled group (F3), off-registry entry preservation (F4), registry 503 gating with retry (F5, F6), pending registry gating (F7), empty registry message (F8), base model tail update before save (F9), recommended defaults confirm/cancel flow (F10), and add-flow focus retention (F11). The dirty-overlay dismissal matrix is Escape + backdrop only: a route-backed overlay is a flush `Dialog` and renders no built-in ✕; its ABSENCE is asserted inside each row so the shrunk gesture list is not silent coverage loss. See change: fix-flush-dialog-scroll-and-close-collision, blackhole-model-picker-chains.
