# model-favorites-cross-surface.spec.ts — index

L3 for model-picker-everywhere-favorites (test-plan F3): star model in Settings → Sessions Default Model picker, session composer row shows `model-fav-toggle[aria-pressed=true]`, reload still pressed (server-persisted). Stubs `GET /api/models`; needs `PI_E2E_SEED=1`. See change: model-picker-everywhere-favorites.
