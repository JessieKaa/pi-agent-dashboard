# openspec-connect-coverage.spec.ts — index

L3 for F1/F2 (fix-connect-snapshot-frame-loss): 64 KB `maxWsBufferBytes` connect delivers every folder/session OPENSPEC subcard within 5 s with no transcript drops; survives `POST /api/restart` reconnect without duplicates.
