# submodule-folder-grouping.spec.ts — index

L3 checkout-root resolver (change: add-git-checkout-root-resolver, test-plan #F1–#F3). Harness builds `/fixtures/super/models/sub` (submodule, own `.pi/`, pre-trusted in `trust.json`) + `/fixtures/sub-worktree`. F1 a submodule session groups under its OWN checkout and no `folder-home-row-*` contains `/.git/`. F2 a worktree-of-submodule reports `gitWorktree.mainPath` = the submodule and converges under it, never `<super>/.git/modules/models`. F3 SEEDS a cold `.meta.json` carrying the phantom (a live bridge can no longer emit one, and would re-probe over it), restarts, asserts the record is restored with `gitWorktree` dropped — with a PLAUSIBLE-mainPath control record that must SURVIVE, so the assertion measures the predicate not a blanket drop. Pins `FIXTURE_GIT` (never the submodule) to leave onboarding, else the assertion would be true by construction. `POST /api/session/spawn` returns no id — sessions are discovered by cwd.
