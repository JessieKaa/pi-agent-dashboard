# diagram-preview.spec.ts — index

L3 diagram preview (.puml and adoc hydration) in the editor pane (change: diagram-rendering, test-plan #F1–#F4, #X3). .puml rendering with stubbed Kroki SVG, decline notice fallback, client-side mermaid hydration without proxy requests, declined plantuml block in adoc keeping listing, dead upstream error fallback.
