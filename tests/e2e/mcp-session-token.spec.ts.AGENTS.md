# mcp-session-token.spec.ts — index

L3 (change: wire-mcp-session-token, test-plan #F1, #F5, #F6). Drives the REAL delivery path: pairing → bridge-scoped ticket → pi-gateway WS → `session_register` → `plugin_pi_message` `mcp/mint-token` → `mcp_token_minted` on the session-private lane → POST `/mcp` with the bearer. F6: provisioned entry carries the D2 `requestHeadersCommand`, no `mcp_` at rest, tools/list returns the 4 advertised tools, zero hand-editing. F5: two same-cwd sessions hold distinct credentials; self-target refused per own session; cross-session call not self-targeting. F1: `/api/restart` → stale token 401, re-registered bridge mints fresh token → tools/list 200. No adapter in the harness image → the in-pi env→header leg is pinned at L1 (`mcp-token-delivery.test.ts`) + qa X9. See change: wire-mcp-session-token.
