# host-pressure-badge.spec.ts — index

L3 for `fix-false-unresponsive-badge` (test-plan #F3, #F4). Drives a SYNTHETIC bridge straight at the pi gateway (`pairDeviceBearer` + `gatewayUrlWithTicket`, port from `/api/health#piGatewayPort`) and then says nothing — a real harness session heartbeats every 15 s and can never go quiet on demand. NO `spawnFreshGitSession`: the scenario needs a quiet bridge, not a live model. Registers under `FIXTURE_GIT`; `awaitCard` pins that folder only if the sidebar has no group for it yet. F3: card silent while the bridge is fresh → `data-host-pressure="unresponsive"` after 60 s of silence → pill gone after one `session_heartbeat`, all with NO page reload. F4: a SECOND browser context opened during the pressure learns the same state from `sessions_snapshot` alone, and both contexts read identically. Exemplar: `bridge-contention-health.spec.ts`.
