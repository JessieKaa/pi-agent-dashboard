# research/browser-relay-playwright-extension.md — index

Research artifact (2026-09-13, explore-mode) — the change it sketched is now SHIPPED as `add-browser-relay`; see the closing SHIPPED paragraph + §8 of the record and `docs/architecture.md` §Browser relay. Ask: Chrome plugin modelled on ManoloRemiddi/augmentor-dsh-extension-plugin (MV3 sidepanel chat + bespoke `browser_*` tools, native-messaging → DSH plugin WS). Verdict: do NOT build Augmentor-shaped extension — Microsoft Playwright Extension (Web Store id `mmlmfjhmonkocbjadbfplnigmagldckm`, Apache-2.0, source `microsoft/playwright` `packages/extension/src/` ~800 lines) already IS "ext T2" (perms `debugger tabs tabGroups activeTab`, host `<all_urls>`); relay side lives upstream `packages/playwright-core/src/tools/mcp/{cdpRelay.ts 391, cdpRelayV2.ts 121, browserModel.ts 247, extensionContextFactory.ts 50}`; `agent-browser connect <cdp-url>` → existing browser skill runs against real logged-in profile unchanged, ZERO new `browser_*` tools; kept Augmentor lesson "no browser connected → tell user, never improvise headless fallback" (= §13). Augmentor anatomy: MV3 ext (sidepanel chat, `sw.js` executor, frost veil/click pulse, no `debugger` → T1) ↔ native-messaging `pipe.mjs` (loopback, token-gated) ↔ DSH `dsh-augmentor` plugin (`browser_tabs_list/navigate/snapshot/click/type`, WS `/api/augmentor`); lessons: 128-bit secret + 0600 endpoint file, `timingSafeEqual`, native-messaging 1 MiB frame cap kills host silently, MV3 SW 30 s idle-kill needs keep-alive. Relay mechanics (`relayConnection.ts`): relay→ext `{id, method, params[]}`, allow-list `ALLOWED_CHROME_COMMANDS` = `chrome.debugger.attach/detach/sendCommand` + `chrome.tabs.create/remove`, positional params; ext→relay `chrome.debugger.onEvent/onDetach`, `chrome.tabs.onCreated/onRemoved`, `extension.initialized` gates Playwright traffic; `CDPRelayServer` paths `/cdp/<guid>` (`connectOverCDP`) + `/extension/<guid>` (ext dials outbound), ONE `_extensionConnection` + ONE `_cdpConnection` (second CDP socket closed line 209 `'Another CDP client already connected'`); `BrowserModel` synthesizes `Target.attachedToTarget`/`detachedFromTarget`, sessionIds `pw-tab-N`, sessionId→tabId routing, child sessions (workers/oopifs), `createTarget` = `chrome.tabs.create`, `Browser.getVersion` faked, ctor takes `profileDirectory`; multi-client = N relay instances (own guid, own ext socket, own coloured TAB GROUP) — per pi session own tab group, dissolves §15 one-browser ceiling inside one profile, last controlled tab closed → `'All controlled tabs detached'`; token = `localStorage['auth-token']` per profile (`ui/connect.tsx:96-103`), shown `PLAYWRIGHT_MCP_EXTENSION_TOKEN=…`, match → no dialog, absent → Allow/Reject; HARD LIMIT `connect.tsx:61` relay host must be `127.0.0.1`/`[::1]` — stock ext loopback-only, remote/docker Chrome needs fork; Web Store 0.4.0 requires `protocolVersion=2` (`lib/ui/connect.js:6`). Option table: 1 reuse MS ext + own relay (0 ext code, relay port ~900) / 2 fork MS ext (remote Chrome + pairing token, always-connected auto-dial `ws://127.0.0.1:8000`) / 3 Augmentor-shaped from scratch (no advantage once sidepanel dropped); 1→2 graduation relay-identical, Option-2 triggers = remote Chrome, `systemOpen` false, separate agent window. Security (grounded `server.ts:2516`): ext socket REJECTED today — SW sends `Origin: chrome-extension://<id>`, verified live 2026-09-13; need new `WsRouteScope` + PINNED ext-id allowlist before generic origin gate (web page cannot forge Origin); no plugin WS-upgrade API (`ServerPluginContext` = `ctx.fastify` + `broadcastToSubscribers` + `registerPiHandler/registerBrowserHandler`) → relay in CORE `packages/server/src/browser-relay/`, plugin = UI/config; `/ext/<guid>` gate `isGenuinelyLocal` OR per-profile token + pinned id (guid 128-bit, 0600, never logged), `/cdp/<guid>` gate `isGenuinelyLocal` + `localToken`, never ticketable never tunnel; relay deny-list `Storage.getCookies`, `Network.getAllCookies`, `Browser.setDownloadBehavior`, `Page.navigate`/`Target.createTarget` to `file://` or outside `allowedDomains` — denied → loud CDP error; remote viewer via existing gated `/ws` (`browser-gateway.ts`), v1 = Chrome on dashboard host only; kill switch `plugins.browser.enabled`, audit `GET /api/browser/audit` (attach/detach, navigate/createTarget URLs, denied verbs, no payloads), capability `capabilities.browserBridge = systemOpenCapability() && chromeFound` (`routes/system-routes.ts:846`). Profile reach (live host): 17 profiles in `Local State → profile.info_cache` (`Profile 23 → robson@semmi.se`, `Profile 37 → OSS`), VERIFIED `open -na "Google Chrome" --args --profile-directory="Profile 37" <url>` lands URL in that profile, requires `systemOpen`; per-profile capabilities installed/token/connected; `systemOpen` false (docker/headless) → `browserBridge=false` → Option-2 trigger. Screencast tap: `chrome.debugger.attach` = one session per tab per extension (human DevTools → detach `canceled_by_user`, reattach only on `target_closed`, 150 ms delay + 3000 ms cooldown, surface in UI); viewer = in-process tap on RelayInstance NOT second CDP client — `BrowserModel._emit` listener list, tap owns `Page.startScreencast`/`screencastFrameAck`, filter `Page.screencastFrame` from Playwright stream while tap active + deny CDP-client `Page.startScreencast`, viewer input `Input.dispatchMouseEvent/dispatchKeyEvent/synthesizeScrollGesture` only never `Runtime.*`, ack immediately + drop on `ws.bufferedAmount` > threshold, message `browser_relay_frame`. Live spike (`/tmp/pw-ext/spike-relay.mjs`, ext 0.4.0, `Profile 37`, jpeg q50 800×600): visible idle → 0 frames/5 s (emit on repaint only); repainting every 100 ms → 49 frames/5 s ≈ 10 fps ≈ 4 KB/frame; HIDDEN → 0/5 s; after `Page.bringToFront` → 50/5 s; `chrome.windows.create` + `chrome.tabs.update` → `Unknown method`; verdict = viewer detects frame starvation, "Bring to front" button = `Page.bringToFront`, separate window fork-only, `chrome.tabs.create {windowId}` places tab in chosen EXISTING window; traps: relay inside context-mode `ctx_execute` sandbox → inbound blocked ("Failed to connect to MCP relay: WebSocket error") — run from plain shell; `protocolVersion=2` mandatory. SHIPPED `add-browser-relay` (see §8 + `docs/architecture.md` §Browser relay; ground truth `openspec/changes/add-browser-relay/`): relay lives in the PLUGIN `packages/browser-plugin/` (plugin-owned `registerWsRoute` seam, D1 — the sketch's core `packages/server/src/browser-relay/` placement was superseded), vendored playwright-core relay Apache-2.0 hash-pinned, scopes `browser-ext`+`browser-cdp`, guid = socket credential, deny-list, audit, `ScreencastTap`; plugin `packages/browser-plugin/` settings + `session-card-badge` status subscriber + live-view tile; config `plugins.browser.{enabled, browsers:{<profileDirectory>:{token?, zeroDialog?, allowedDomains?}}, defaultBrowser, allowMultipleInstancesPerProfile}`, `token` `writeOnly` (fail-closed redaction), `defaultEnabled:false`; REST `GET /api/browser/status|profiles|audit` + `POST /api/browser/connect` (body `{profileDirectory}`) + `POST /api/browser/disconnect?instanceId=` + `PUT /api/browser/enabled|profile`, NO cdpUrl lookup; skill `references/dashboard-relay.md` ("logged-in state needed → `POST /api/browser/connect` then `agent-browser connect <cdpUrl>`"), Panerelay → legacy; discipline skills security-hardening (done, task 6.1), observability-instrumentation, doubt-driven-review; still deferred = remote Chrome (fork), separate window (fork), 17-profile token onboarding. Sources: microsoft/playwright `packages/extension/src/{background,relayConnection,pendingConnection,ui/connect.tsx,ui/authToken.tsx}` + `packages/playwright-core/src/tools/mcp/{cdpRelay,cdpRelayV2,browserModel,extensionContextFactory}.ts` (2026-09-13); augmentor README + PROPOSAL-plugin-architecture.md; installed ext `Profile 37/Extensions/…/0.4.0_0`; repo `server.ts:2516`, `auth/cors-origin.ts:214`, `auth/ws-ticket.ts:30,53`, `auth/localhost-guard.ts`, `system-open-capability.ts:32`, `routes/system-routes.ts:846`, `dashboard-plugin-runtime/README.md`. Live-probed macOS host.
