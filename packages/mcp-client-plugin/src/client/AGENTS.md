# DOX — packages/mcp-client-plugin/src/client

Files in this directory. One row per source file. See change: extract-mcp-client-plugin.

| File | Purpose |
|------|---------|
| `FolderMcpPage.tsx` | `shell-overlay-route` `/folder/:encodedCwd/mcp` (tasks 8.2-8.4). `decodeFolderCwd` (malformed encode → `mcp-folder-not-allowed`, 0 requests). `FolderMcpPageBody` reads `useEffectiveConfig(cwd)` + one `useAdapterStatus`; `folderErrorState`: 403 → not-allowed ONLY (no retry, no data), 504 → `mcp-folder-timeout` + `mcp-folder-timeout-retry` (`reload()` = one request), generic → `mcp-folder-error` + retry. `mcp-folder-readonly-banner` when adapter not `ok` (switches/Override/Save disabled). `useFolderActions`: `open` (`edit` when folder-owned else `override`), `openAdd`, `removeOverride` → `removeServer(name, scopeToWire({kind:"project",cwd}))` + undo (`mcp-folder-undo-toast`, `mcp-folder-undo`) re-submitting the raw `removed` entry byte-equivalently, `mcp-folder-action-error`. `FolderEditor` → `ServerEditor` (`mcp-folder-editor`) with `inherited` hints + `mcp-folder-remove-override`. `mcp-folder-back` → `onBack`; `FolderSummary`. `useIsNarrow` gates chip inline remove. See change: extract-mcp-client-plugin (tasks 8.2-8.4). |
| `FolderMcpSection.tsx` | `sidebar-folder-section` + `worktree-card-section` pill (task 8.1). One `SlotPill` (`mcp-folder-pill`) per folder cwd: effective count (`mcp-folder-pill-count`), disabled count (`mcp-folder-pill-off`, warning colour) when non-zero, error marker (`mcp-folder-pill-error`, `role="img"` aria-label naming failing path / timeout), muted `mcp-folder-pill-not-tracked` on 403, same-height loading placeholder (`mcp-folder-pill-loading`). `folderMcpUrl(cwd)` = `/folder/<encodeURIComponent(cwd)>/mcp`, activated via wouter `useLocation`. `useSessionRetry` drops the cached 403 + `reload()`s when the `useAllSessions()` id signature changes. `placement` card→flat / sidebar→raised. `pillContent` is the pure state → accent/body derivation. See change: extract-mcp-client-plugin (tasks 8.1, 8.3). |
| `GlobalSettingsForm.tsx` | `settings-section` global settings form (task 7.6). Adapter `$defs.McpSettings` fields via `fieldsForDef` + `widgetFor`; ONE host draft source `useSettingsDraftSource({ id: "plugin:mcp-client" })` — NO local Save/footer. Baseline = Pi-global keys (`view.settings[key].source === "pi-global"`); unset keys show the effective fallback (shared → labelled with `path`, else adapter default); clear button "use inherited" (shared) / "reset to default". `commit` → `computePatch` → `patchSettings(set, unset)` changed keys only; no-op when nothing changed; `invalidateEffective` + rebase on success, stays dirty on reject. Dashboard plugin settings group (`mcp-plugin-settings`): `adapterLoadTimeoutMs` (`mcp-adapter-timeout`, id `#mcp-adapter-timeout`) from `usePluginConfig`, range 1000–120000 inline error blocks commit; awaits `plugin_config_write` (id `mcp-client`) + adapter patch via `Promise.allSettled`, each baseline advances independently, error on the failing group only. Testids: `mcp-global-settings`, `mcp-setting-<key>`, `mcp-setting-input-<key>`, `mcp-setting-clear-<key>`, `mcp-setting-inherited-<key>`, `mcp-comments-note`. Read-only disables every input. See change: extract-mcp-client-plugin (task 7.6). |
| `McpSettings.tsx` | `settings-section` root. `McpSettingsClaim` → `McpSettings`. ONE adapter status (`useAdapterStatus`) drives pill + banner + page-wide `readOnly`. Error states: 504 `adapter-timeout` (retry + `#mcp-adapter-timeout` link), 403 `not-allowed`, generic. Renders `GlobalSettingsForm` when the effective view lands (task 7.6). `EditorForEditing` mounts `ServerEditor` keyed on `editing` (`""` = add) so a reopen REMOUNTS the editor (reveal state resets). See change: extract-mcp-client-plugin (tasks 7.2-7.4, 7.6). |
| `ServerEditor.tsx` | Schema-driven server editor (tasks 7.4 + 7.5, extended 8.3). Modal `role="dialog"` (`mcp-editor-dialog`, or `dialogTestId="mcp-folder-editor"`), Escape closes, focus in on open / back to trigger on close. Fetches the schema itself (`fetchSchema`). Transport tabs (`TRANSPORT_FIELDS`; switching CLEARS the other transports' keys). Advanced `<details>`. ONE primary Save → `patchServer(name, {...scopeToWire(scope), set, unset})` via `computePatch` (changed keys only; unknown fields preserved; sentinels never sent) — `scope` defaults global, folder page passes `{kind:"project",cwd}`. `initialMode` forces edit (folder Override…). `inherited={{fields,layer}}` renders `mcp-folder-inherited-<server>.<field>` = "inherited from <layer>" and `folderPage`. `onRemoveOverride` → `mcp-folder-remove-override` footer button. Non-editable server → View + `Override at Pi global`; page-wide `readOnly` → view render + Save disabled. Atomic override (`mcp-override-<name>`, starts `{}`, `atomicCounts` note). Secrets: password + reveal (`mcp-reveal-<name>`); redacted placeholder NO reveal excluded from patch. `EditorDialogBody` holds the JSX body. |
| `ServerList.tsx` | Server list (tasks 7.2 + 7.3, extended 8.2/8.3). `ServerRow`, `ServerList`, `transportOf`, `provenanceBadges`, `isEditable(server, scope)`, `Badge`. Rows sorted; badges **Pi global / Pi folder / Shared 🔒 / Other: <kind>**; parse-error rows; empty + skeleton. Enable/disable switch writes `setServerDisabled(name, !next, scopeToWire(scope))` (scope defaults global) with pending + revert-on-failure. Folder variant (`scope.kind==="project"`): row testid `mcp-folder-row-<name>`, action `mcp-folder-action-<name>` (`Edit` folder-owned else `Override…`), every toggle writable, `OverrideChip` (`mcp-folder-override-chip-<name>` + `mcp-folder-chip-remove-<name>` when `chipsRemovable`) from `overrideFieldsOf`. |
| `api.ts` | REST surface over `/api/mcp-client/*`. `API_BASE`, `EffectiveResponse`, `ScopeWire`, `scopeToWire`, `ApiError` (`.isAdapterTimeout` 504, `.isNotAllowed` 403, `.fields`), `fetchEffective`, `fetchSchema`, `fetchAdapterVerdict`, `patchServer` (set/unset), `removeServer` (returns `{removed}`), `setServerDisabled`, `patchSettings`. No caching, no React; types TYPE-ONLY from core. See change: extract-mcp-client-plugin (task 7.1). |
| `folder-view.ts` | Pure folder-scope view helpers over an effective server. `FolderServerView` = `EffectiveServerView & { own?: Record<string,unknown> }` (folder layer's own entry, when sent). `layerLabel` (Shared / Pi global / Pi folder / Other), `inheritedLayerOf` (highest non-folder layer), `inheritedFieldsOf` (fields the folder entry does not define; `undefined` when unknowable), `overrideFieldsOf` (own keys = chip list), `isFolderOwned` (pi-folder provenance), `ownEntryOf`. See change: extract-mcp-client-plugin (tasks 8.2, 8.3). |
| `hooks.ts` | `useEffectiveConfig`/`loadEffective`/`invalidateEffective` — module-level store, ONE in-flight per key, cached success. `notTrackedError` + per-cwd 403 cache (`loadEffective` short-circuits a refused cwd; a successful force clears it; `invalidateEffective` drops it; `__resetNotTrackedCache` for tests). `useAdapterStatus`/`deriveAdapterStatus`/`AdapterStatus` — single status (pill/banner/readOnly agree). See change: extract-mcp-client-plugin (tasks 7.1, 8.1). |
| `index.tsx` | Client barrel for the plugin manifest. Exports `McpSettingsClaim`, `FolderMcpSection`, `FolderMcpPage`. |
| `schema.ts` | Schema model over `schema/mcp-config.schema.json`. `fieldsForDef`, `fieldsOf`, `widgetFor` (NEVER null — unknown shapes → `json`; toggle-list = boolean+string[] oneOf; nested-group = x-atomic + object branch), `atomicFieldsOf`. `TRANSPORT_FIELDS` + `visibleUnderTransport`. Masking: `SECRET_KEY_PATTERN`, `isSecretKeyName`. Redaction: `isRedacted`, `redactedKeys`, `atomicCounts`, `stripRedacted`. Draft model: `clone`, `deepEqual`, `computePatch`, `getPath`/`setPath`, `validateFields`, `validateDraft`. See change: extract-mcp-client-plugin (tasks 7.4, 7.5, 7.6). |
| `useIsNarrow.ts` | `useIsNarrow(query = "(max-width: 639px)")` — reactive `matchMedia` boolean; jsdom (no `matchMedia`) degrades to `false`. Gates the folder page's chip inline-remove control. See change: extract-mcp-client-plugin (task 8.4). |
