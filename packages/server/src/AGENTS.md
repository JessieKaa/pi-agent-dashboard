# DOX — packages/server/src

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `__tests__/health-client-build.test.ts` | P0 (`optimize-client-bootstrap-and-bundle-coherence`): `/api/health.clientBuild` states driven by injected served dirs — matched / mismatched / metadata-missing (missing + malformed) / not-served (null dir + absent dep) — plus a no-path-leak assertion and the missing-declaration `console.warn` (filename only). `discoverPlugins` mocked; `pluginRegistryHash` real. |
| `__tests__/pi-gateway-session-identity.test.ts` | Regression tests for pi-gateway owner identity. Stale socket register/messages/unregister/close/heartbeat timers cannot affect current owner; current owner quit and automation close still end. See change: reload-resume-lifecycle. |
| `auto-name-outcome-store.ts` | Bounded (500) in-memory retention of the last auto-naming outcome per session. Eviction prefers non-`stopped` entries; when `stopped` entries alone reach the bound the OLDEST stopped goes — the bound is absolute, protection is only an order. Served by `GET /api/auto-name-outcomes` so a stop latched with no subscribed client is still discoverable. See change: fix-auto-naming-reasoning-model. |

| `browse.ts` | Directory-browse logic: `listDirectories` (readdir + tiered rank filter, cap 200, opt-in `.git`/`.pi` flag… → see `browse.ts.AGENTS.md` |
| `cli.ts` | `pi-dashboard` CLI entry. Exports `parseArgs` (subcommand + flags), `buildConfig` (CLI+env+file merge →… → see `cli.ts.AGENTS.md` `buildConfig()` records `hostFlag` alongside the resolved `host`. See change: warn-unreachable-trusted-networks. |
| `commit-draft-relay.ts` | Correlates `POST /api/git/commit-draft` → bridge `git_commit_draft_result`. → see `commit-draft-relay.ts.AGENTS.md` |
| `config-api.ts` | Config REST helpers: `readConfigRedacted` (redacts `auth.secret`/`providers[].clientSecret` + tunnel provider… → see `config-api.ts.AGENTS.md` `reachability` stripped on the write path, like `resolvedTrustedNetworks`. See change: warn-unreachable-trusted-networks. |
| `config-snapshot.ts` | mtime-gated snapshot of `~/.pi/dashboard/config.json` (D15). Exports `getConfigSnapshot` (stat per call, reparse only when `mtimeMs:size` moved), `liveCorsAllowedOrigins`, `liveTrustedNetworks`, `resetConfigSnapshot`, `configSnapshotParseCount`. Consumed by the CORS `origin` callback and `networkGuard` so a runtime config write APPLIES without a restart. MUST stay filesystem-derived — a boot snapshot reinstates the bug, and invalidate-on-write misses a hand-edited config. ~1.9 µs steady state vs ~24.5 µs read+parse. See change: config-override-oauth-redirect-base. Adds `liveAllowedHosts`, `livePublicBaseUrls` (via `resolvePublicBaseUrls`, legacy `pairing.publicBaseUrls` honoured) and `liveHostGateMode`. A present-but-unparseable `config.json` keeps the LAST-GOOD snapshot (one `[config-snapshot]` line) instead of silently flipping every live gate to defaults; an absent/empty file still falls back to defaults. See change: add-host-allowlist-admission. |
| `directory-service.ts` | Server-side directory-scoped ops. Exports `createDirectoryService`, `DirectoryService` interface,… → see `directory-service.ts.AGENTS.md` Adds readiness fold: memoized config-root skills stat, per-generation signature memo (`invalidateOpenSpecSignatureCache`), `finalizeOpenSpecData` on every pollOne exit, opt-out poll gate, `reconfigurePolling` readiness-key diffing (broadcast-only-changed). See change: add-openspec-init-affordances. Replayed events stamped with resolved `groupId` post-pool (`options.customEventGroupResolver`) — same resolver + helper as live. See change: add-custom-event-group-filters. Adds `getOrPollOpenSpec(cwd)` (gated cache-hit / cold-miss pull, per-cwd shared in-flight poll) and the `pollAndBroadcastIfChanged(cwd)` extraction shared by the scheduler tick and the pull. See change: fix-connect-snapshot-frame-loss. Adds `ensureLoadWorkerPool()` (shared hydration pool; returns `SessionLoadWorkerPool` or `null` once `stopPolling` disposes it). See change: fix-session-diff-durable-source. |
| `event-wiring.ts` | Wires pi-gateway events → browser-gateway + session manager. Exports `wireEvents`, `EventWiringDeps`. → see `event-wiring.ts.AGENTS.md` `models_list` broadcast forwards `refreshErrors` verbatim when present. See change: upgrade-model-selector-primitives. Handles `auto_name_outcome` (retain in the bounded store + forward to subscribers) and `auto_name_state` (persist the namer stop state); pushes `auto_name_state_restore` to a freshly-registered bridge so a stop survives a process restart. See change: fix-auto-naming-reasoning-model. Groupable custom events (`custom_entry`, custom `message_end`) fork: resolver groupId stamped BEFORE ingest via `annotateAndIngest().catch()`; handler runs exactly once. See change: add-custom-event-group-filters. Handles bridge register archive drop (`onBridgeRegister`) and completes idle-alive archive on unregister. See change: archive-sessions-lazy-load. |
| `__tests__/api-origin-gate.test.ts` | Mutating-REST Origin gate L1 (#E12-#E17, #X3). Drives the REAL `createMutationOriginGate` hook on stub routes (handler spy proves "never invoked"), plus two real-server wiring checks (`POST /auth/logout` cookie NOT cleared on refusal; `/api/ws-ticket` 403). See change: fix-ws-origin-cswsh. |
| `__tests__/retained-transcript-route.test.ts` | L1 HTTP surface of `GET /api/sessions/:id/retained-transcript`: spy-preHandler proves the route is behind `networkGuard` (asserted here, NOT at L3 — docker publishes through a proxy so the server sees a host request as loopback and exempts it, as it does for `/api/session-file`); status split 200/400/403/404/503; shape-before-lookup (a `?path=` probe answers 400 for known AND unknown ids, so the pair cannot be differenced into an existence oracle); `?sessionId=` rider cannot steer the subject; an ARCHIVED remote session is served, not 404'd. See change: serve-retained-remote-transcripts. Plugin context gains the trust-gated `sendExtensionMessage` hook → `piGateway.sendToSession(sessionId, msg)` — the `credentials_updated` lane without the `pi.events` re-emit; mcp-server-plugin delivers minted session tokens over it. See change: wire-mcp-session-token (D5). |
| `__tests__/browser-gateway-critical-frames.test.ts` | Bounded critical-frame delivery suite (E1–E6, X6, P1): pending-prompt replay exempt past MAX_WS_BUFFER under the 4-frame cap + 5 MB ceiling; transcript/notify still shed; split drop counters; real gateway + `DrainingFakeWs` with a control-run delta. See change: fix-pending-prompt-lost-on-replay. |
| `__tests__/browser-gateway-resync-delivery.test.ts` | Requester-scoped resync suite (E7–E9, X3, E16, P2): `prompt_resync_request` forwarding/recording, `deliverPromptResyncReply` unicast + TTL + forget fallbacks, 100-delivery bound soak. See change: fix-pending-prompt-lost-on-replay. |
| `__tests__/plugin-ws-route.test.ts` | Plugin-registered WS route gates on the REAL upgrade handler (test-plan #E4-#E8, #E5 golden, #X11; spec plugin-ws-route). `createTestServer` + fake scopes registered on the real registry: pinned `admitOrigins` admits only the listed Origin (localhost:5173 + absent → 403); empty list byte-identical to core policy; core `/ws` unaffected while a plugin scope is live (routeScopeForUrl core rows + live dials); `POST /api/ws-ticket` 400 for plugin scope + `consume` false + cookie/ticket cannot substitute for the plugin credential; loopback-Host BVA (4 admitted / 3 refused 403 + `[ws-gate]` scope+peer log); 8 forwarding headers singly → 403; toggle-off closes open sockets 1001 ≤1s and 404s later upgrades. See change: add-browser-relay. |
| `__tests__/ws-origin-gate.test.ts` | WS upgrade Origin gate L1 (#E4-#E9, #E18, #X1, #X2, #F4). Real server + real `ws` dials: attacker origin 403s on every path (incl. `/ws/bridge`, which otherwise 400s — proof the gate runs first), absent/loopback/Host-matched origins open, `Origin: null` 403s except on `live`, a refused dial does not burn a ticket, `cors.allowedOrigins` applies without restart, and `/live/<id>` proxies a null-origin dial with the header stripped upstream. Control-char origins never reach the gate (Node's parser 400s first) — asserted via a raw socket. See change: fix-ws-origin-cswsh. |
| `__tests__/keeper-log-maintenance.test.ts` | Sweep + stats L1 suite (E10-E15, X5-X11, P4): size/age gates, launch-log exclusion, PID-sidecar liveness (NOT isKeeperAlive), never-unlink invariant (readdir set equality), EACCES isolation, unsafe-home block, runaway threshold, reclaimedBytes survival, TTL cache. See change: fix-runaway-keeper-log-growth. |
| `file-watch-manager.ts` | Narrow open-files watch. `setWatched(ws,sessionId,cwd,relPaths,onChange)` reconciles per (ws,session,path); `clearConnection(ws)` on disconnect, no fd leak. See change: split-editor-workspace. |
| `fix-pty-permissions.ts` | Runtime fix for node-pty spawn-helper exec bit. Exports `fixPtyPermissions` (no-op on Windows / already… → see `fix-pty-permissions.ts.AGENTS.md` |
| `pi-agent-settings.ts` | Read/WRITE of pi's own agent-level retry policy in GLOBAL `~/.pi/agent/settings.json`. Exports `readPiRetryPolicy`, `writePiRetryPolicy`, `validatePiRetryPolicy`, `PI_RETRY_DEFAULTS`, `ValidationError`, `WriteResult`; all fs deps injectable for tests. Write is MERGE-PRESERVING — every other key survives, incl. `retry.provider.*`; only `retry.{enabled,maxRetries,baseDelayMs}` are set. Project `<cwd>/.pi/settings.json` NEVER written. Validates first (`maxRetries` non-negative int, `baseDelayMs` positive int); invalid → nothing written. DISTINCT from `config-api.ts`, which writes the dashboard's own `~/.pi/dashboard/config.json`. See change: retry-forever-with-stop-control. |
| `plugin-intent-cache.ts` | Server-side cache of most recent plugin intent per `(pluginId, sessionId, slot)`. → see `plugin-intent-cache.ts.AGENTS.md` |
| `resolve-path.ts` | Exports `safeRealpathSync(p)` — `fs.realpathSync` with original-path fallback on error. |
| `ripgrep-detection.ts` | One-time `rg` detection via ToolResolver. `detectRipgrep`/`resetRipgrepCache`. See change: split-editor-workspace. |
| `runtime-publication.ts` | Startup publication of the diagnostic `runtime.resolved` block into RAW `~/.pi/dashboard/config.json` (never the typed loader — unknown keys + user-owned `runtime.override` survive byte-identical). Exports `publishResolvedRuntime(rt, {configPath, resourcesPath, warn})` → `{block, written}` (atomic tmp+rename via `writeJsonFile`; corrupt/non-object-`runtime` config → warn + bail, file untouched), `readPublishedRuntimeBlock(configPath)`. Nothing consumes the block for execution — the ladder re-resolves live each start. Wired at the end of `runForeground`. See change: unify-pi-runtime-identity (task 4.1, design D8). |
| `runtime-resolution.ts` | Process-lifetime holder for the ladder-resolved pi spawn runtime. Exports `setCurrentSpawnRuntime`, `currentSpawnRuntime` (null before startup resolution — Doctor rows suppress), `resolveLiveSpawnRuntime(opts)` (ladder re-run + holder update; used at startup and on spawn-time re-validation failure). Consumed by process-manager spawn surfaces, doctor-routes, rebuild executor. See change: unify-pi-runtime-identity (design D1/D3). |
| `server.ts` | Dashboard HTTP + WebSocket server. Exports `ServerConfig`, `DashboardServer`, `createServer(config)`. → see `server.ts.AGENTS.md` `ServerConfig.hostFlag` retains the raw `--host` so `pendingBindHost` can re-resolve; startup emits the `[bind-reachability]` warning. See change: warn-unreachable-trusted-networks. mDNS advertise passes `{bindHost: config.host}`; loopback-bound servers publish nothing. See change: fix-bridge-mdns-migration-hijack. Wires the Host-admission gate (issue #637): a `HostGateState` + per-request `getHostGateCtx` (live inputs, env-over-config mode, boot warning), the `onRequest` hook registered BEFORE `@fastify/cors`, the Host check as the first statement of the `upgrade` handler, a 60 s unref'd window-flush timer (cleared in `stop`), the shared `corsOpts` host fields, and `registerHostGateRoutes`. See change: add-host-allowlist-admission. Wires the `sessionOrderManager` snapshot-orders collaborator into `createMemorySessionManager`. See change: fix-connect-snapshot-frame-loss. Instantiates `sessionArchive`, `archiveSweeper`, `pendingArchiveIntents`, seeds archive on boot, and wires broadcasts. See change: archive-sessions-lazy-load. Hoists ONE `createRemoteTranscriptStore()` ahead of `createBrowserGateway` and shares it across the retention write (`wireEvents`), the read route (`registerSessionRoutes`), and remote-origin hydration — two instances would be two views of one directory. See change: serve-retained-remote-transcripts. | Static client dir resolved ONCE before `registerSystemRoutes` via `resolveClientDist` (installed-first, `DASHBOARD_CLIENT_DIST_DIR` env legacy override) and reused for Fastify static serving + health. See change: optimize-client-bootstrap-and-bundle-coherence (P0). Static `setHeaders` cache policy: path-segment `assets` (Vite content-hashed output, checked with `path.sep`, never the URL string) → `public, max-age=31536000, immutable` incl. precompressed `.gz` siblings; `.html` + SPA fallback → `no-store`; remaining root files keep the static-plugin default. See change: trim-cold-start-transfer-and-config-fanout (①). |
| `system-open-capability.ts` | System file-open capability: `systemOpenCapability`, `buildOpenCommand`/`buildRevealCommand`/`runOpener` (`OpenerCommand`) — platform reveal/open shell-out for the `/view` system-open path. See change: open-view-command-in-editor-pane. |
