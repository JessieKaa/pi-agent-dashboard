# DOX — packages/server/src/session

Files in this directory. One row per source file. See change: fold-oversized-agents-directories.

| File | Purpose |
|------|---------|
| `active-sessions-in-cwd.ts` | `isPathInside` now RE-EXPORTED from `@pi-dashboard-shared/path-containment.js` (see change: scope-openspec-auto-attach-to-session-cwd); pure helpers `isPathInside(parent, child)`, `activeSessionsUnder(path, sessions)` (excludes `status ===… → see `active-sessions-in-cwd.ts.AGENTS.md` |
| `archive-sweeper.ts` | Runtime auto-archive sweeper. Exports `createArchiveSweeper`, `ArchiveSweeper`, `ArchiveSweeperDeps`. Runs on `setInterval`, checks ended sessions against `sessionList.archiveAfterDays`, batch cap 200. See change: archive-sessions-lazy-load. |
| `custom-event-group-annotation.ts` | `isGroupableCustomEvent`, `customEventTypeOfEvent`, `stampEventGroup` — server-side `groupId` stamping on `custom_entry` + custom `message_end` before ingest/broadcast. See change: add-custom-event-group-filters. |
| `custom-event-group-matcher.ts` | `CustomEventGroupMatcher` — bounded user-regex matcher in a long-lived `worker_threads` worker. FIFO-serialized matches, per-match timeout → terminate + lazy respawn; ready banner excludes boot from the deadline. See change: add-custom-event-group-filters. |
| `custom-event-group-matcher-worker.ts` | Worker body: one `{idx, pattern, customType}` per message → `{idx, matched}`; posts a ready banner on load. Spawned by the matcher; never imported. See change: add-custom-event-group-filters. |
| `custom-event-group-resolver.ts` | `CustomEventGroupResolver.resolve(customType)` — first-match-wins, memoized per type per process, per-group quarantine on matcher failure, `flow-event` excluded (no matcher call), fallback `other`. See change: add-custom-event-group-filters. |
| `death-reason.ts` | `classifyCarrierLoss({pid}, {kill?}) → ClosedReason` — best-effort involuntary-death classification: `esrch` (provably absent) → `process_gone`; no pid / EPERM / recycled-pid-alive → `unknown`. Never names a signal, never claims SIGKILL/OOM. Uses shared platform `signalZero` (NOT raw `process.kill`). See change: stop-discarding-known-session-state (design D1, task 4.10). |
| `derive-ended-at.ts` | Evidence rule for an UNWITNESSED session ending. Exports `readJsonlMtime(sessionFile)` (the single transcript-stat path, also used by `session-scanner`), `EndedAtEvidence`, `EndedAtDeriver`, `deriveEndedAt(evidence)` — precedence `lastActivityAt` → transcript mtime → `startedAt`, never `Date.now()`. Witnessed endings (explicit signal, user shutdown) keep `Date.now()` and do NOT come through here. See change: fix-ended-session-missing-endedat. |
| `event-status-extraction.ts` | Extract session status/tool/model stats from forwarded events. → see `event-status-extraction.ts.AGENTS.md` |
| `host-pressure-tracker.ts` | Per-session bridge-silence verdict, emitted on STATE TRANSITION only. Exports `createHostPressureTracker({onChange, degradedMs?, unresponsiveMs?})` → `noteFrame`/`clear`/`stop`, `HostPressure`, `HostPressureState`, `size()` (tracked-session count — the exit-path leak oracle). Thresholds now RE-EXPORTED from `shared/src/host-pressure.ts`, not defined here: the card escalates between transitions on the same numbers. `noteFrame` re-arms both timers and emits an explicit `null` when a prior verdict clears; healthy session emits nothing. Fed by `pi-gateway` on EVERY bridge frame (a blocked event loop cannot put one on the wire); every session-exit path must `clear()` or the entry and its two timers leak. See change: fix-false-unresponsive-badge. |
| `memory-session-manager.ts` | Pure in-memory session registry; replaces SQLite-backed session-manager. → see `memory-session-manager.ts.AGENTS.md` `register()` carry-over also preserves `notifyLog` across a bridge reattach (the reattach onChange save is a full `.meta.json` overwrite). See change: split-notify-from-prompt-request. Adds `endedSequence`/`snapshotVisibleIds`/`buildSnapshot` + `stripNotifyLog` for the windowed snapshot (`SNAPSHOT_ENDED_GLOBAL=120`, `SNAPSHOT_ENDED_PER_GROUP=3`). See change: fix-connect-snapshot-frame-loss. Adds `remove(sessionId)` eviction. See change: archive-sessions-lazy-load. `register()` carries `gitWorktree` over a SAME-cwd reattach (different cwd → reset) so a restart / bridge reconnect cannot re-open the removed-worktree clear. See change: fix-worktree-grouping-lost-on-remove. `update()` and `unregister()` stamp the central `→ ended` transition with a `closedReason` (default `unknown`, explicit reason from sites that know it, exact-transition check so a no-op re-entry preserves the original) and route it through `metaPersistence.setLiveness` — the ONLY durable path, since `session-to-meta.ts` is a full overwrite. See change: stop-discarding-known-session-state (design D1/D2, tasks 4.4/4.8). BOTH `→ ended` seams clear `session.hostPressure`: the verdict describes a LIVE bridge's silence, so leaving it on the row lets a later `sessions_snapshot` serve a stale badge for a dead card. See change: fix-false-unresponsive-badge. |
| `open-tool-calls.ts` | Pure derivation of what a dying session left open + the events that heal it. Exports `findOpenToolCalls(events)` (backwards walk, stops at last `agent_start`, reads `agentId` at `data.partialResult.details.agentId` — NOT `data.details`), `findOpenSubagents(events)` (whole retained window, `created`/`started` minus `completed`/`failed`), `synthesizeSessionEndedEnd(call, now)`, `synthesizeSessionEndedSubagentFail(id, now)`, `SESSION_ENDED_RESULT`, `SESSION_ENDED_MARKER`. Idempotent by construction: synthesized ends re-enter the same stream, so a second pass finds nothing open. See change: heal-orphaned-tool-cards-on-session-end. |
| `openspec-locality.ts` | Tri-state (`present`/`absent`/`unknown`) OpenSpec change locality over the poll cache: `candidateRoots` (cwd + worktree `mainPath`, + unknown root while unresolved), `localityGateAllows`, `attachedStillExistsInCandidateRoots`. Cache reads only. See change: scope-openspec-auto-attach-to-session-cwd. |
| `reattach-placement.ts` | Reattach placement policy: decides how a re-registered session id (`registerReason: "reattach"`, dashboard… → see `reattach-placement.ts.AGENTS.md` |
| `reconcile-session-order.ts` | Pure startup reconciliation of persisted `sessionOrder` map under all-status model. → see `reconcile-session-order.ts.AGENTS.md` |
| `replay-compact.ts` | Replay-time compaction. Exports `compactReplayEvents(events)`: drops completed-message `text_*`/`toolcall_delta` paint updates while preserving thinking, tool boundaries, streaming tails, original seq values, and EventStore contents. |
| `replay-compact.test.ts` | Unit coverage for replay compaction: completed paint removal, load-bearing update retention, streaming tails, malformed shapes, message identity isolation, and input immutability. |
| `replay-window.ts` | Selects cumulative retained tail by approximate logical transcript items. Aligns to safe user-turn boundary; returns `HistoryWindowMetadata`; handles sequence gaps and retention boundaries. |
| `replay-window.test.ts` | Unit coverage for safe tail selection, boundary overshoot, open tool-heavy turns, sequence gaps, retained-first-seq semantics, and empty metadata. |
| `remote-transcript-store.ts` | `createRemoteTranscriptStore(env?)` — retains remote sessions' `.jsonl` under `~/.pi/dashboard/remote-transcripts/<id>.jsonl` (0600 in 0700) so it outlives the origin host (11.10) at full fidelity (11.9). Session ids validated against a safe charset, NOT interpolated (untrusted wire input); `restarted` chunks REPLACE, never append; `.complete` sidecar marks partial vs whole (#X18). See change: add-pi-gateway-transport-identity (task 11.6). `read()` also returns `retained` (transcript FILE exists) — `complete:false` alone cannot separate a truncated transfer from one that never happened. See change: serve-retained-remote-transcripts (task 1.2). |
| `replay-compaction.ts` | `compactEventsForReplay(stored)` — pure REPLAY-ONLY drop of `message_update` snapshots superseded by a later… → see `replay-compaction.ts.AGENTS.md` |

| `replay-truncate.ts` | truncateToolResultForReplay(event). Strategy B reconciled onto adopt-pi-071-072-073-features. → see `replay-truncate.ts.AGENTS.md` |
| `retained-transcript.ts` | READ half of D12. Exports `decideRetainedRead({sessionId,query,origin})` — refuses path-bearing fields via shared `decideTranscriptRequest` (`path-on-the-wire`), then LOCAL-origin sessions (`local-origin`); shape before subject, so two refusals are not an origin oracle. Exports `readRetainedTranscript(store,sessionId,ctxWindow?)` → `{entries,events,state}`, `state` = `complete\|incomplete\|absent`. Parses via `parseSessionEntries` (same branch order as the local path); never throws — a store refusal reads as `absent`. Also exports `chooseHydrationSource({origin,sessionFile,hasRetentionStore})` → `retained\|local-file\|none`: the remote arm has NO branch returning `local-file`, so "remote never reads the local file" (#E15) holds by construction, and remote + no store fails CLOSED to `none`. Split reads: `readRetainedState(store,id)` → `{entries,state}` with NO replay (both HTTP callers discard events, so replaying would parse up to 44.1 MB for nothing); `readRetainedTranscript(...)` adds the replay and is for cold hydration only. Both never throw — parse included; a crafted `message.content:[null]` degrades to zero events but KEEPS the state. Backs `GET /api/sessions/:id/retained-transcript` + remote-origin hydration. See change: serve-retained-remote-transcripts (tasks 1.1, 1.2, 1.3). |
| `resolve-order-key.ts` | Resolves `sessionOrder` map key for a session server-side. → see `resolve-order-key.ts.AGENTS.md` |
| `session-api.ts` | REST wrappers for session control. Exports `registerSessionApi(fastify, deps)`. → see `session-api.ts.AGENTS.md`. Replaces hide/unhide with `POST /api/session/:id/archive` and `POST /api/session/:id/unarchive`. See change: archive-sessions-lazy-load. |
| `session-archive.ts` | Archive transition owner + in-memory archive index. Exports `createSessionArchive`, `SessionArchive`, `decodeCursor`. Manages sidecar writes, broadcasts, cursor pagination, and disk file deletions. See change: archive-sessions-lazy-load. `archiveSession` captures `originDeviceId` onto the row and `unarchiveSession` restores it (`restored.originDeviceId ?? row.originDeviceId`) — losing it makes an unarchived remote session local, reopening #E15 hydration AND `decideResume`, which D13 forbids. See change: serve-retained-remote-transcripts. |
| `session-bootstrap.ts` | Exports `discoverAndBroadcastSessions(deps)` — async startup discovery from known directories, restores… → see `session-bootstrap.ts.AGENTS.md`. Guards against resurrecting archived session ids; non-archived discovered sessions seeded as visible (hidden=false). See change: archive-sessions-lazy-load. |
| `session-diff.ts` | `extractFileChanges(events, cwd)` scans `tool_execution_start` write/edit events, groups by path, attaches… Async + event-loop-safe: `enrichWithGitDiff`/`buildSessionDiff` async, ONE batched `git diff --relative HEAD` split per file (`splitBatchedDiff`), `TRACKED_DIFF_MAX_BYTES` (5 MB) cap, no `spawnSync` git on the path; `buildSessionDiffCached(sessionId, load, cwd, cache, {sourceKey, ended})` wraps with TTL+single-flight; `buildSessionDiff` gains `opts.windowEnd` (ended-session Bash clamp). See change: fix-session-diff-eventloop-block. See change: fix-session-diff-durable-source. → see `session-diff.ts.AGENTS.md` |
| `session-diff-cache.ts` | `SessionDiffCache<T>` (per-session TTL result cache + single-flight coalesce; TTL 0 disables; bounded eviction) + `djb2` key hash. Backs `/api/session-diff` poll coalescing. See change: fix-session-diff-eventloop-block. |
| `session-diff-source.ts` | Transcript-sourced session-diff events for LOCAL sessions. → see `session-diff-source.ts.AGENTS.md` |
| `session-discovery.ts` | Standalone per-cwd session discovery from `~/.pi/agent/sessions/<encoded-cwd>/`. → see `session-discovery.ts.AGENTS.md` |
| `session-file-reader.ts` | Standalone JSONL session reader. Exports `SessionEntry`, `loadSessionEntries(filePath)` (leaf→root branch… → see `session-file-reader.ts.AGENTS.md` |
| `session-load-worker-pool.ts` | Session-load worker pool. Fixed slots = `max(1, min(maxConcurrentSpawns, os.cpus().length))`; FIFO queue when… → see `session-load-worker-pool.ts.AGENTS.md` |
| `session-load-worker.ts` | Pure `loadAndReplay(req): {jobId, success, events, error, entryCount?, lastEntryTs?}` + `parentPort` bootstrap. `mode:"diff-events"` returns the `projectDiffEvents` projection (+`maxStringSize`, `lastEntryTs`). See change: fix-session-diff-durable-source. → see `session-load-worker.ts.AGENTS.md` |
| `session-order-manager.ts` | Per-cwd session ordering persisted via `PreferencesStore`. → see `session-order-manager.ts.AGENTS.md` |
| `session-origin.ts` | `attributeOrigin()` (origin from the CREDENTIAL, never a bridge self-report) + `mayReadLocalSessionFile()` + `decideResume()`. Gates every filesystem read/resume of `sessionFile` on the session having originated HERE — two hosts with the same username produce identical paths, so a present file is the hazard, not an absent one (#E15/#X19). Absent `originDeviceId` = local (back-compat, task 11.12). See change: add-pi-gateway-transport-identity (11.7/11.8/11.11). |
| `session-scanner.ts` | Cold-start session scanner. Exports `ScanResult`, `ScanOptions`, `sessionFromMeta`, `scanAllSessions(sessionsDir, opts)` — scans… → see `session-scanner.ts.AGENTS.md` `sessionFromMeta` restores `notifyLog`. See change: split-notify-from-prompt-request. `sessionFromMeta` restores `autoNamerState` so a permanent naming stop survives a PROCESS restart and does not re-spend a full attempt budget. See change: fix-auto-naming-reasoning-model. Performs boot-time archive migration (hidden+ended sidecars) and scan-time age archiving into `ScanResult.archived`. See change: archive-sessions-lazy-load. `sessionFromMeta` infers worktree parentage from an absolute `<X>/.worktrees/<name>[/<sub>...]` cwd when persisted parentage is absent/implausible. See change: fix-worktree-grouping-lost-on-remove. `sessionFromMeta` AND `archivedRowFromMeta` restore `originDeviceId` — without it a restart reads every remote session as local and hydration opens the origin host's path on THIS disk (#E15). See change: serve-retained-remote-transcripts. |
| `session-stats-reader.ts` | Exports `SessionStats`, `extractSessionStats(filePath)` — reads session JSONL once, accumulates tokensIn/Out,… → see `session-stats-reader.ts.AGENTS.md` |
| `session-to-meta.ts` | Exports `sessionToMeta(session)` — the EXPLICIT `.meta.json` field enumeration extracted from `server.ts`… → see `session-to-meta.ts.AGENTS.md` Enumerates `notifyLog` so retained notifications survive a restart. See change: split-notify-from-prompt-request. Enumerates `autoNamerState` — this save is a FULL overwrite, so omitting it would wipe a permanent naming stop on the next unrelated save. See change: fix-auto-naming-reasoning-model. Enumerates `archived`, `archivedAt`, `restoredAt`. See change: archive-sessions-lazy-load. Enumerates `originDeviceId` — same full-overwrite trap: omitting it wipes origin on the next routine save, and origin is a filesystem-read gate (#E15). See change: serve-retained-remote-transcripts. |
| `viewed-session-tracker.ts` | Exports `ViewedSessionTracker` interface, `createViewedSessionTracker()` — per-session set of viewing… → see `viewed-session-tracker.ts.AGENTS.md` |
