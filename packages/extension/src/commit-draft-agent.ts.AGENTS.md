# commit-draft-agent.ts — index

pi-SDK-coupled half of AI-draft. Exports `buildSessionContextText(ctx, maxChars)` (compacts `buildSessionContext().messages` → bounded text) + `runForkSubagentDraft(seed, cwd, getModel, timeout)` (ephemeral `SessionManager.inMemory` `AgentSession`, `tools:[]`, subscribe→capture text_delta, prompt, dispose; throws on failure → ladder falls back). Isolated so the risky in-process AgentSession spawn is behind one guarded entry; visible conversation untouched. See change: add-session-uncommitted-indicator-and-commit. `SessionManager.inMemory(cwd)` retained on 0.85.1 (task 5.7): pi#8980 added an optional third `entries` restore parameter (signature `inMemory(cwd?, options?, entries?)`), but the draft runner WANTS a FRESH throwaway session with no prior entries — the restore param is additive and intentionally unused. See change: update-pi-core-0-85-adopt-apis.
