# themes.ts — index

Theme token definitions. CSS_VAR_KEYS includes --status-needs-you/working/idle/error/notice. statusVars maps each to var(--accent-purple/yellow/green/red/blue); withStatus merges statusVars into every theme dark+light. See change: improve-dashboard-attention-routing. `--status-notice`=var(--accent-blue) (non-error info). See change: fix-gemini-subagent-silent-tool-schema-failure. Body-text contrast floor: `--text-secondary` + `--text-tertiary` reach WCAG AA 4.5:1 on BOTH `--bg-tertiary` and `--bg-surface` in all 18 palettes; upstream values remediated by lightness only (hue+saturation preserved). 4 palettes (catppuccinLight, rosePineLight, solarizedDark, solarizedLight) also got a paired `--text-secondary` lift — secondary was itself sub-AA, lifting tertiary alone inverted hierarchy. Solarized = accepted identity loss (its `--bg-surface` sits inside its text ramp → near-white text), user decision 2026-09-14/15. Pinned by `src/lib/__tests__/theme-body-text-contrast.test.ts` (AA floor + hierarchy + themes.ts/index.css parity + hue/saturation preservation). See change: stop-discarding-known-session-state.
