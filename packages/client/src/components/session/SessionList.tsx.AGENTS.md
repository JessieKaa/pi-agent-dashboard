# SessionList.tsx — index

Main sidebar session list. DnD-ordered (`@dnd-kit`) pinned/unpinned + workspace tiers, folder grouping,… `renderGroup` folder-home open affordance (`folder-open-home-<cwd>`) renders when `isPinned || inWorkspace` (unpinned workspace folder has `folder.pinned===false`). See change: enable-workspace-folder-home-page. Whole folder header name-row (`folder-home-row-<cwd>`) navigates to `buildFolderHomeUrl(cwd)` on click (all folders); collapse moved solely to chevron `folder-toggle-btn` in drag gutter. See change: directory-card-clickable-select. `compactSidebar` presentation flag hides folder tab, plugin/OpenSpec grid, Create/spawn tray, Sessions separator; keeps folder header, Git, FolderActionBar, DnD, full SessionCard pipeline; applies denser folder/list spacing. See change: compact-workspace-sidebar. `renderGroup` card wrapped in `relative pt-[9px]` with a non-interactive folder-tab nub (`aria-hidden`, `pointer-events-none`, `top-0 left-3.5 w-[78px] h-3`, behind the card) so the card reads as a folder; the old `folder-3d.svg` watermark layer is removed. See change: folder-card-tab-nub. `renderGroup` now ENCLOSES the Create tray + session list + "N ended" row inside a `folder-body-<cwd>` wrapper (header `rounded-t`/`border-b-0`+inset-rim-only shadow when expanded, body `border-t-0`/`rounded-b`+fold-shadow child) so the card reads as a folder; body uses `flow-root` so the first `CREATE` divider's `my-2` margin stays inside instead of creating an 8px header/body gap. Adds a `SESSIONS` separator mirroring `CREATE`; top-level (`!inWorkspace`) folders get an accent-tinted surface via `folderTint` (color-mix on `--accent-blue`). See change: folder-card-enclosure. Sidebar tag/phase filter area sits under ONE master collapse (`tag-area-toggle`, `aria-expanded`, default-collapsed via `getTagAreaOpen`/`setTagAreaOpen`); collapsed header shows `N tags · M phases` (`tag-area-count`) + an active-selection badge (`tag-area-active-indicator`) with a fold-free clear (`clear-tag-filters-collapsed`) when filters active; expanded renders `TagFilterGroup` user (cap=10, per-chip ✕ → `TagDeleteConfirmDialog` → `onRemoveTagGlobally`) + read-only phase group. See change: sidebar-tag-collapse-and-delete. → see `SessionList.tsx.AGENTS.md`
