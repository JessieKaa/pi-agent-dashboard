# SessionList.tsx — index

| File | Purpose |
|------|---------|
| `SessionList.tsx` | Main sidebar session list. Groups pinned, unpinned, and workspace sessions; supports folder/session ordering and folder membership DnD through `compatibleClosestCenter`, `MeasuringStrategy.Always`, spring-open workspaces, and `onMoveFolderToWorkspace`. Folder headers provide keyboard-accessible folder-home navigation, a single `FolderActionsMenu`, workspace actions, and slot-contributed OpenSpec/refresh actions. Folder cards intentionally avoid `overflow-hidden` so action popovers remain visible. `compactSidebar` keeps folder headers, Git facts, core folder controls, DnD, and SessionCards while hiding heavy auxiliary/create surfaces, using denser spacing and preserving visible folder basenames; it also hides the tier-0 `FolderActionBanner` in every rung (`folders.notAPiProjectYet` / re-trust / init / running / failed) while its init-status probe keeps running for the menu's Project setup tally. See changes: `compact-workspace-sidebar`, `drag-folders-across-workspaces`, `add-folder-actions-menu`, `move-slot-actions-to-menu`. |
