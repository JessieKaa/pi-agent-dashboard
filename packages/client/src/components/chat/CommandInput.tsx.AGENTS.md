# CommandInput.tsx — index

Chat composer with controlled/uncontrolled draft and image state, paste/upload previews, provider/model refresh errors, thinking-level and model controls, slash/@ autocomplete including local `/view`, terminal control, steer/queue delivery, history recall, and pending-prompt status handling. Exports `CommandInput`, `CommandInputProps`, `parseViewCommand`, `shouldWalkFileQuery`. Chat composer textarea + autocomplete. Exports `CommandInput`, `parseViewCommand`, `shouldWalkFileQuery`,… `pendingPrompt` prop is now the status union, not a boolean; only `"sending"` gates the composer. See change: fix-optimistic-prompt-stuck-sending. New `modelRefreshErrors?` prop forwarded to `ModelSelector.refreshErrors`. See change: upgrade-model-selector-primitives. Below-floor allocation lower bound 72px and shrink. See change: define-chat-pane-below-floor-allocation.
