# DOX — packages/mcp-server-plugin/src/server

Files in this directory. One row per source file. See change: add-dashboard-mcp-server.

| File | Purpose |
|------|---------|
| `auth.ts` | `parseBearer(header)`, `authenticate(header, deps) -> McpCaller \| null`, `credentialFingerprint(header) -> sha256-hex` (throttle key dim 2, D7; never logged, X6). Credential check LOCAL to `/mcp` — `createNetworkGuard` is per-route, so `/mcp` sits outside it and self-guards. Pure function of the `Authorization` header value: takes no address/`isGenuinelyLocal` param (so no loopback carve-out can leak in, A3) and never reads `request.isAuthenticated` (so a cookie-authed browser cannot reach it, A4). Statelessness gives per-request credentials for free (A7). Session tokens checked before device tokens. |
| `adapter-diagnostic.ts` | `adapterVerdictOf(service)` → the consumed verdict, or `{kind:"unknown", floor: ADAPTER_VERSION_FLOOR}` when `mcp-client.config` is absent — this plugin declares NO `dependsOn`, so `/mcp` must serve without it. `createAdapterWarnOnce(log, consume)` returns a callback that consumes lazily and warns at most once on a non-`ok` verdict; pure until invoked, so registration emits nothing. Wired as the routes' `onMcpRequest` hook — the warning fires on the first `POST /mcp`, the path where a below-floor local adapter actually matters. See change: extract-mcp-client-plugin. |
| `dispatch.ts` | `dispatchRpc(request, resolved: {era, version}, caller, deps)`, `buildDiscoverResult`, `parseSubscriptionFilter`, `REMOVED_METHODS`, `versionFailureResponse(failure, id)`, `ResolvedVersion`. Version is resolved ONCE in `routes.ts` (single resolution site) and passed in — this module never re-resolves. Legacy-era adapter (D1): `notifications/*` prefix → 202 empty body (stray `id` ignored), `initialize` → static `InitializeResult` (`capabilities` exactly `{tools:{listChanged:false}}`, `Mcp-Session-Id` minted + echoed, D2), `ping` → `{}`, `subscriptions/listen` → 404 `MethodRemoved` (streaming modern-only, D3); everything else flows through the same modern per-method dispatcher. Modern era keeps `initialize`/`notifications/initialized`/`resources/{subscribe,unsubscribe}` → 404 `MethodRemoved`. `tools/call` validates required args BEFORE the self-target guard (E26). Optional/typed args and unknown names are validated from the tool's own `inputSchema` via `validateToolArguments` (type/enum/range/array items + unknown-key rejection, enforcing `additionalProperties:false`), then the tool's own `validateArgs` hook (e.g. the cursor's filter binding) — every failure `-32602` before the handler. `server/discover` built fresh from constants each call (E19/E20), `protocolVersions` = all four revisions. See change: mcp-legacy-clients-and-token-issuance. |
| `guard.ts` | `evaluateSelfTarget(caller, targetSessionId, tool)`, `SESSION_TARGETING_TOOLS`. Req 6. Takes NO prompt text — G2's slash-command case is handled by construction, so no payload can route around the verdict and the refusal necessarily precedes extension-command dispatch. Equality normalises case, surrounding whitespace and ONE matched quote pair (G7), no further (G3 must still work). Device callers exempt (G4). Indirect A→B→A loop PERMITTED and asserted (G6) — documented scope limit. Malformed target permitted, deferring to invalid-params. |
| `header-command.mjs` | The `requestHeadersCommand` child the adapter spawns per HTTP request (D2). Echoes `{"Authorization":"Bearer <token>"}` read from its OWN environment (`PI_DASHBOARD_MCP_TOKEN`) — never argv (`ps -ww`-safe, spike Q1b). Drains the adapter's stdin envelope, then answers. Fail-closed: env unset → non-zero exit + stderr diagnostic (the adapter discards stderr, so the 401 + server-side records are the visible half, X2). Plain ESM, no build step. See change: wire-mcp-session-token. |
| `index.ts` | Server entry `registerPlugin` (+ `export default`, required by the loader). Creates the token registry and subscription registry, mounts routes on `ctx.fastify`, registers `mcp/mint-token` + `mcp/revoke-token` pi handlers — the mint handler sends `mcp_token_minted` back on the session-private lane via `ctx.sendExtensionMessage` (D5; handler returns are discarded, so a `return {token}` is dead — closed-socket delivery failure logged with the session id, X1) — and wires `ctx.onSessionEnded` → `revokeSession`, and provisions the Pi-global `mcp.json`. The `list_sessions` handler delegates to `listSessions(listAll(), args)` — bounded/filterable/cursor-paged (paginate-mcp-list-sessions). Adapter version is NOT probed at registration: `createAdapterWarnOnce` builds `warnAdapterOnce`, passed to `mountMcpRoutes` as `onMcpRequest` — no `dependsOn`, so `mcp-client.config` is consumed lazily on the first `POST /mcp`. Registry is created HERE and never persisted, so it dies with the plugin (X8) and a restart invalidates everything (X9). Provisioning failure is logged, never thrown (J7). Logs completeness + partition failures at load. See change: extract-mcp-client-plugin. |
| `jsonrpc.ts` | JSON-RPC 2.0 framing: `parseRpcRequest`, `rpcResult`, `rpcError`, `extractId`, standard codes. Keeps the HTTP-status/RPC-code pairing explicit because on revision 2026-07-28 it is a conformance requirement: unknown method = 404 + `-32601`. Unparseable bytes (`-32700`) are a DIFFERENT failure from valid JSON that is not JSON-RPC (`-32600`) — E17 asserts both separately. |
| `list-sessions.ts` | Pure bounded/filterable/cursor-paged listing (paginate-mcp-list-sessions). `listSessions(rows, args, platform)` → `{ sessions, total, nextCursor? }`; `DEFAULT_LIMIT` 25, `MAX_LIMIT` 200, `SESSION_STATUSES`. `sessionSortKey` = `endedAt ?? lastActivityAt ?? startedAt` desc with `id` asc tiebreak; non-finite keys sort last deterministically. Filters: `status` (array any-match), `cwd` via `pathKey`, `since` against the sort key; `hidden` excluded by default. Keyset cursor = base64url `{key,id,digest}` where digest is SHA-256 of the canonical filters+limit; `validateListSessionsArgs` rejects an undecodable or digest-mismatched cursor. `nextCursor` ABSENT (not null) on the final page. Removal mid-walk tolerated: cursor names a position, not a row. |
| `protocol.ts` | `resolveProtocolVersion(method, headerValue, params)` → `{ ok, era: "modern"\|"legacy", version }` or failure, `LEGACY_PROTOCOL_VERSIONS` (`2025-03-26`/`2025-06-18`/`2025-11-25`), `MODERN_PROTOCOL_VERSION` = `SUPPORTED_PROTOCOL_VERSIONS` union tail, `META_VERSION_KEY`, `PROTOCOL_VERSION_HEADER`, `ProtocolVersionFailure` (+`AmbiguousHeader`). D1 rules 0–4, order observable: repeated/comma-joined header → `AmbiguousHeader` (Node's parser joins duplicates, so the joined form is what arrives); `initialize` negotiates from `params.protocolVersion` ONLY (legacy echoed, `2026-07-28` resolves modern → handshake refused downstream, unknown string → negotiate down `2025-11-25`, missing/non-string → `UnsupportedProtocolVersion` naming all four); header decides otherwise (legacy on header alone, modern still requires the `_meta` key); `_meta` version without header → `MissingHeader`; NO version marker at all → legacy `2025-03-26` (the one loosening). Header/body agreement judged before supported-ness so a typo'd near-version still reports `HeaderMismatch`. Never throws on arbitrary input. See change: mcp-legacy-clients-and-token-issuance. |
| `provisioning.ts` | `provisionDashboardEntry(configIO, {url, adapter?})`, `buildDashboardEntry`, `DASHBOARD_MCP_KEY = "pi-dashboard"`, `PROVISIONED_PROTOCOL_VERSION = "2026-07-28"`, `ProvisionResult`, `DashboardMcpEntry`. Writes THROUGH `createMcpClientConfigService` — `checkConfigFiles` parse gate, `readServerEntry` (Pi-global, file-only), `ensureServerEntry`, `targetPath` — so JSONC parsing, the `mcpServers`/`mcp-servers` alias, the hardened atomic write and the config-dir resolution match pi instead of a local copy. Merge-only, so operator-added `disabled`/`headers` survive a refresh. Entry uses the HTTP `url` shape, NOT the stdio `command` shape (J1), and never omits `protocolVersion` — without it the adapter defaults to the LEGACY handshake, silently downgrading pi's own adapter off the strict modern path (J2). Reserved key: absent→create, declares `url`→overwrite, any other shape→REFUSE THE WHOLE WRITE (J6). `probeAdapterVersion` + `ADAPTER_VERSION_FLOOR` moved to mcp-client core. Entry also carries the D2 auth transport: `requestHeadersCommand` `{command:"node", args:[headerCommandPath()], env:{PI_DASHBOARD_MCP_TOKEN:"${PI_DASHBOARD_MCP_TOKEN}"}}` — interpolation form in the `env` slot only (no literal `mcp_` at rest, E9), `args` plain path (adapter `Array.map` bug makes every interpolation form there "", spike Q1a). Exports `headerCommandPath()`, `MCP_TOKEN_ENV_VAR`. See change: wire-mcp-session-token. See change: extract-mcp-client-plugin, mcp-legacy-clients-and-token-issuance, wire-mcp-session-token. |
| `rate-limit.ts` | `AuthFailureThrottle`, `MAX_AUTH_FAILURES` 10 / `MAX_IP_AUTH_FAILURES` 100 / `AUTH_FAILURE_WINDOW_MS` 60s / `AUTH_LOCKOUT_MS` 60s / `MAX_TRACKED_SOURCES` 10000. API `(ip, fingerprint, now)` — per-`(ip,fingerprint)` bucket + coarser per-ip ceiling (D7): one session's stale token no longer throttles the other local sessions sharing 127.0.0.1; credential rotation walks into the ceiling. Maps SEPARATE, so fingerprint churn never evicts a per-ip record (P3). Added for CodeQL `js/missing-rate-limiting`: `/mcp` is an authorization boundary reachable over a zrok tunnel and runs a credential comparison per request. Counts ONLY failed attempts, and a success clears the counter, so legitimate traffic is never throttled (a throttle that locks out real users is a self-inflicted outage). The tracking map is itself capped — an unbounded per-IP map would trade CPU exhaustion for memory exhaustion, which is worse because the attacker picks the key space. Defence in depth; the primary control is the token's 256-bit entropy. |
| `routes.ts` | `mountMcpRoutes(fastify, deps)` → registers inside an ENCAPSULATED Fastify scope. That is load-bearing: `setErrorHandler` is global on the instance it is called on, so calling it on the shared `ctx.fastify` would replace the DASHBOARD'S handler and 500 every SPA route. SINGLE VERSION-RESOLUTION SITE: `resolveProtocolVersion` runs once per POST, before the `subscriptions/listen` interceptor (closes the pre-change gap where a `listen` never had its version checked); version failures map via `versionFailureResponse`, resolved `{era, version}` passed into `dispatchRpc`; a legacy-era `listen` never hijacks the reply (D3). `send()` echoes `Mcp-Session-Id` ONLY from the legacy-era initialize adapter — a modern response never carries one (E5/E11). Optional `deps.onMcpRequest?()` fires per POST — the lazy adapter-diagnostic hook. Registers every non-POST method explicitly (405 + `Allow: POST`), because Fastify falls an unmatched method through to `setNotFoundHandler` → 200 + SPA HTML in `--dev`. `HEAD` is asserted but NOT registered — Fastify derives it from `GET`, and declaring both throws. `MCP_BODY_LIMIT_BYTES` 1 MiB (X11). Auth runs first, from the header alone. Throttle keyed `(request.ip, credentialFingerprint(authorization))` (D7); `recordSuccess` runs before the RPC parse so an authorized malformed body still clears the counters. See change: mcp-legacy-clients-and-token-issuance, wire-mcp-session-token. |
| `streaming.ts` | `McpSubscription`, `SubscriptionRegistry`, `MAX_BUFFERED_EVENTS = 1000`. `ctx.onEvent` delivers EVERY session's events, so per-subscription filtering is a spec requirement, not an optimisation (S2). Clean close, transport abort and shutdown all funnel through one idempotent `close()`, and registry deregistration is wired INTO `close()` rather than left to call sites, so no teardown path can forget (S4–S6). `isStillAuthorised` re-checked per delivery, so a revoked token TERMINATES the stream instead of letting it drain silently (S9). A slow consumer is disconnected, not silently dropped — a stream with a hole is worse than none (X12). |
| `tokens.ts` | `McpTokenRegistry`, `McpCaller`. Opaque 256-bit `mcp_`-prefixed token, SHA-256 at rest, plaintext returned once, no independent expiry, IN-MEMORY only (Decision 7). Rows are a flat ARRAY, not a hash-keyed `Map`, so a miss performs the same `timingSafeEqual` work as a hit — a `Map` would leak membership through timing while still passing a naive test (X10). `mintForSession` REPLACES the session's row (D4): a re-mint on reconnect kills the previous token immediately and keeps one row per session; trusts its caller to have proven identity; only the bridge socket key can (Decision 6). `revokeSession` is the primary revocation path; `dispose()` covers plugin unload. |
| `tools.ts` | `ToolLike`, `filterToolsForTier`, `listTools`, `findTool`, `checkToolCompleteness`, and the context partition: `ALL_CONTEXT_MEMBERS` (20), `ALLOWLISTED_CONTEXT_MEMBERS` (5), `DENIED_CONTEXT_MEMBERS` (14), `INTERNAL_ONLY_CONTEXT_MEMBERS` (`fastify`), `assertContextPartitionTotal` — a new `ServerPluginContext` member fails the partition at load instead of defaulting to exposed. `sendExtensionMessage` denied (D5): forging server-originated control messages on another session's private lane. See change: add-dashboard-mcp-server; wire-mcp-session-token; expand-mcp-tiered-surface. |
| `tools.manifest.ts` | `MANIFEST`, `ToolRow`. Reviewed tool table: name, tier, annotations, `input` (shared type), `sessionTargeting`, `bind` (`rest` method+path+fixed / `session` message / `context` member). Codegen turns it into `generated/tools.ts`. See change: expand-mcp-tiered-surface. |
| `tools.denylist.ts` | `DENYLIST`, `isDenylisted(name)`. Routes (exact path or trailing-`/` prefix) and browser-WS verbs deliberately NOT exposed, each with a reason; the completeness test fails an unbound, undenied route/verb. See change: expand-mcp-tiered-surface. |
| `route-skew.ts` | `filterToolsByRoute(tools, hasRoute, warn)`. Drops `rest` rows whose route the host does not register, one `mcp.manifest_route_missing` warning each — published-plugin/host skew guard. See change: expand-mcp-tiered-surface. |
| `manifest-export.ts` | Re-exports `MANIFEST`, `DENYLIST`, `isDenylisted`, `GENERATED_TOOLS` and the context partition constants for the server-side completeness test without pulling the plugin runtime. See change: expand-mcp-tiered-surface. |
| `generated/tools.ts` | GENERATED by `codegen/generate-tools.ts` — one entry per manifest row: `inputSchema` (from the row's shared `input` type), effective `tier`, annotations, `bind`, `paramSplit`, `sessionTargeting`. Freshness test regenerates + compares. See change: expand-mcp-tiered-surface. |
