# DOX — packages/shared/src/test-support

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `git-fixtures.ts` | Builds the nine git states as REAL repos in a temp dir: normal (+ deep subdir), linked worktree, superproject, submodule, worktree-of-submodule, `clone --bare` hub, worktree-of-bare, `--separate-git-dir`, a checkout named `app.git`, plus a non-repo. Exports `buildGitFixtures()` → `GitFixtures` (paths + `cleanup()`), `fixtureGit(cwd,args)`, `probeTriple(cwd)`, `FIXTURE_GIT_ENV`, `restoreEnv(name, saved)` (deletes when `saved` is `undefined` — `process.env.X = undefined` stores the string `"undefined"`, which leaks across reused vitest fork workers). Every git call sets `GIT_CONFIG_GLOBAL=/dev/null` + `GIT_CONFIG_SYSTEM=/dev/null` (else a developer's own `core.worktree` leaks into assertions) and `submodule add` passes `-c protocol.file.allow=always` (CVE-2022-39253). Lives in `test-support/` not `__tests__/` so extension + kb-plugin suites can import it across packages. `execFileSync` from `platform/exec.js` (the `no-direct-child-process` rule). See change: add-git-checkout-root-resolver. |
| `setup-home-perfile.ts` | vitest `setupFiles` hook. Runs inside each worker fork before test file imports. Sets `process.env.HOME = mkdtempSync(os.tmpdir()/pi-test-)` per file. Pre-creates `.pi/agent/sessions` + `.pi/dashboard`. Isolates HOME across parallel forks (maxWorkers PARALLEL_MAX_WORKERS). Complements `setup-home.ts` globalSetup tripwire (kept as second-line guard). Wired in shared/server/extension/subagents-plugin vitest configs via config-relative path (worktree-local source wins over node_modules symlink). No localStorage handling — only client jsdom uses localStorage, isolated in-memory per fork. See change: parallelize-test-suite. Also sets `PI_GATEWAY_TCP ??= "1"` — the gateway TCP listener is opt-in in production (task 8.1) but the server corpus dials `ws://127.0.0.1:<piPort>`. See change: add-pi-gateway-transport-identity. |
| `setup-home.ts` | Vitest `globalSetup` test-isolation tripwire. Default export verifies `process.env.HOME` set + not equal real user home (aborts run before destructive tests touch live `~/.pi/`). Pre-creates `<HOME>/.pi/agent/sessions/` + `<HOME>/.pi/dashboard/`. Warns when HOME outside `os.tmpdir()`. |
