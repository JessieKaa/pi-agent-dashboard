# mdns-discovery.ts — index

mDNS `_pi-dashboard._tcp` advertise + discover. `advertiseDashboard(port, piPort, {bindHost?, publish?})` (loopback bind advertises NOTHING), `shouldAdvertise(bindHost)`, `stopAdvertising()`,… → see `mdns-discovery.ts.AGENTS.md`
