# pi-package-resolver.ts — index

Walks `~/.pi/agent/settings.json#packages[]` + per-workspace `<cwd>/.pi/settings.json`. Resolves `npm:` / `git:` / `https:` / abs / rel sources to absolute install dir. Returns entry path via `exports["."]` → `main` → `pi.extensions[0]` → `index.{js,ts}` chain. Read-on-call: settings + `package.json` reads are never cached; the default `npm root -g` IS memoized per process (see below). Exports `resolvePiPackage` + `resolvePiPackageEntry`. Consumers import from `@blackbelt-technology/pi-dashboard-shared/pi-package-resolver.js`. Exports `listPiPackages(opts)` returning `ResolvedPiPackage[]` across user (`~/.pi/agent/settings.json`) + project (`<cwd>/.pi/settings.json`) scopes, deduped by `packageDir`, no name filter; consumed by Electron `probePiExtension` in `launch-source.ts`. See change: fix-electron-cold-launch-probe-cascade. See change: add-shared-pi-package-resolver.

Memoizes ONE value process-wide: the default `npm root -g` (`defaultNpmRoot()` wrapping `rootGlobalOr("")`, kept as the literal token `resolver-parity-with-scanner.test.ts` greps). Exports `resetNpmRootCacheForTests()`. Both `opts.npmRoot ?? …` sites use it; an explicit `opts.npmRoot` bypasses it; an empty result is cached too (retrying repeats a ~150 ms `spawnSync` failure). Settings + `package.json` reads stay uncached. Measured: 7-way child-loader fan-out 24 spawns / 5046 ms loop block → 1 spawn / 54 ms. Module header no longer claims "holds no module-level cache". See change: heal-orphaned-tool-cards-on-session-end.
