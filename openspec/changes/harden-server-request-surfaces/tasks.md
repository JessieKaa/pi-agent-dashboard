## 1. `openspec_refresh` gate (#647) — `packages/server/src/directory-service.ts`, `browser-handlers/directory-handler.ts`

- [ ] 1.1 L1 test: untracked cwd does not spawn — see `packages/server/src/browser-handlers/__tests__/directory-handler.test.ts` for the handler+fake-service harness. Triple: empty session registry with no pins · `openspec_refresh { cwd: "/tmp/anywhere" }` · CLI spawn spy not called and no `openspec_update` broadcast for that cwd. Verify red first. (test-plan #E1)
- [ ] 1.2 L1 test: opted-out cwd does not spawn — same file. Triple: `/repo/a` tracked via a registered session and opted out of OpenSpec polling · `openspec_refresh { cwd: "/repo/a" }` · spawn spy not called, no broadcast. Verify red first. (test-plan #E2)
- [ ] 1.3 L1 test: tracked cwd without an openspec root does not spawn — same file. Triple: `/repo/c` tracked with no `<cwd>/openspec/` dir · `openspec_refresh { cwd: "/repo/c" }` · spawn spy not called, no broadcast. Verify red first. (test-plan #E3)
- [ ] 1.4 L1 test: global disable still wins — same file. Triple: `openspec.enabled: false` with the cwd tracked and holding an openspec root · `openspec_refresh` · spawn spy not called, no broadcast. Verify red first. (test-plan #E4)
- [ ] 1.5 L1 test: tracked cwd still force-polls — same file. Triple: tracked cwd with `openspec/` and a cache warm with a stale mtime · `openspec_refresh` · spawn spy called exactly once (mtime gate bypassed) and exactly one `openspec_update` broadcast. Verify red first — this is the over-gating guard. (test-plan #E5)
- [ ] 1.6 L1 test: tracked gate precedes the filesystem probe — same file. Triple: untracked cwd `/tmp/anywhere` with `fs.statSync` spied · `openspec_refresh { cwd: "/tmp/anywhere" }` · stat spy never called with any path at or below `/tmp/anywhere`. Verify red first. (test-plan #E6)
- [ ] 1.7 L1 test: ended session's cwd is still refreshable — same file. Triple: cwd whose only session has status `"ended"`, not pinned, still present in the registry · `openspec_refresh` · spawn spy called once and one broadcast (parity with `openspec_get`). Verify red first. (test-plan #E7)
- [ ] 1.8 L1 test: non-canonical cwd gates — same file. Triple: `/repo/a` tracked and the browser sends `/repo/a/` · `openspec_refresh { cwd: "/repo/a/" }` · spawn spy not called; pins the accepted strict-equality trade-off. Verify red first. (test-plan #E8)
- [ ] 1.9 L1 test: `refreshOpenSpec` keeps its un-gated contract — see `packages/server/src/__tests__/directory-service.test.ts` for the real-service harness. Triple: an untracked, opted-out cwd · direct `directoryService.refreshOpenSpec(cwd)` call · resolves to an `OpenSpecData` object, never `null`. This is the regression guard for the four internal callers; verify it is GREEN before and after 1.12. (test-plan #E9)
- [ ] 1.10 L1 test: gated refresh leaves no cache residue — same file as 1.1. Triple: untracked cwd with an empty cache · `openspec_refresh` then read the cache · no cache entry created for that cwd. Verify red first. (test-plan #X7)
- [ ] 1.11 L1 test: gated refresh cannot force process spawns — same file as 1.1. Triple: 100 `openspec_refresh` messages naming distinct untracked cwds delivered back-to-back · handler invoked for each · OpenSpec CLI spawn count is exactly 0. Verify red first. (test-plan #P1)
- [ ] 1.12 Implement D1: add `directoryService.refreshOpenSpecGated(cwd): Promise<OpenSpecData | null>` applying opt-out → `isTrackedCwd` → `hasOpenSpecRoot` after the `cfg.enabled` check, returning `null` with no cache write, no spawn and no filesystem probe, and delegating to `refreshOpenSpec` on a pass; point `handleOpenSpecRefresh` at it and skip the broadcast on `null`. Leave `refreshOpenSpec`'s signature and its four internal callers untouched. Verify 1.1–1.11 green and the existing `directory-service` / `openspec-polling` tests green.

## 2. Pairing routes (#665) — `packages/server/src/routes/pairing-routes.ts`

- [ ] 2.1 L1 test: device bearer cannot revoke a sibling — see `packages/server/src/__tests__/pairing.test.ts` for the `fastify.inject` harness. Triple: registry rows A and B with `Authorization: Bearer <A>` from a non-local ip · `DELETE /api/paired-devices/<B>` · `401`, B's row remains and its token still verifies. Verify red first. (test-plan #E10)
- [ ] 2.2 L1 test: device bearer cannot revoke itself — same file. Triple: row A with `Authorization: Bearer <A>` from a non-local ip · `DELETE /api/paired-devices/<A>` · `401`, A's row remains. Verify red first. (test-plan #E11)
- [ ] 2.3 L1 test: loopback device bearer is still refused — same file. Triple: rows A and B with `Authorization: Bearer <A>` from `127.0.0.1` and no forwarding headers · `DELETE /api/paired-devices/<B>` · `401`, B's row remains. Verify red first — the credential, not the network position, decides. (test-plan #E12)
- [ ] 2.4 L1 test: operator revokes over a tunnel — same file. Triple: cookie session (`authVia: "session"`) from a non-local ip on an admitted Host · `DELETE /api/paired-devices/<B>` · `200`, B's token no longer verifies. Verify red first — guards against over-tightening. (test-plan #E13)
- [ ] 2.5 L1 test: local-token operator revokes — same file. Triple: valid `X-Pi-Local-Token` with no session · `DELETE /api/paired-devices/<B>` · `200`, B's token no longer verifies. Verify red first. (test-plan #E14)
- [ ] 2.6 L1 test: mint route inherits the device refusal — same file. Triple: `Authorization: Bearer <A>` from `127.0.0.1` · `POST /api/paired-devices` · `401` and no new row minted. Verify red first — pins the intended collateral tightening. (test-plan #E15)
- [ ] 2.7 L1 test: operator on a non-admitted Host is refused — same file. Triple: operator session with a non-admissible `Host` and the global gate in `report` mode · `DELETE /api/paired-devices/<B>` · `403 host_not_admitted`. Verify red first. (test-plan #X5)
- [ ] 2.8 L1 test: device bearer cannot approve — same file. Triple: pending device P with the correct `code` + `confirmCode` and `Authorization: Bearer <A>` · `POST /api/pair/approve` · `401`, `pairing.poll(P.pendingId).status` unchanged, P's token does not verify. Verify red first. (test-plan #E16)
- [ ] 2.9 L1 test: approval label just above max — same file. Triple: operator session with a `label` of 65 UTF-8 bytes · `POST /api/pair/approve` · `400`, P remains pending. Verify red first. (test-plan #E17)
- [ ] 2.10 L1 test: approval label at max — same file. Triple: operator session with a `label` of exactly 64 UTF-8 bytes · `POST /api/pair/approve` · `200`, stored label is the 64-byte value. Verify red first. (test-plan #E18)
- [ ] 2.11 L1 test: approval label at min — same file. Triple: operator session with `label: "x"` · `POST /api/pair/approve` · `200`, stored label `"x"`. Verify red first. (test-plan #E19)
- [ ] 2.12 L1 test: whitespace-only label is below min — same file. Triple: operator session with `label: "   "` · `POST /api/pair/approve` · `400`, P remains pending, the redemption label is NOT overwritten with `""`. Verify red first. (test-plan #E20)
- [ ] 2.13 L1 test: label is trimmed, not rejected — same file. Triple: operator session with `label: "  phone  "` · `POST /api/pair/approve` · `200`, stored device label is exactly `"phone"`. Verify red first. (test-plan #E21)
- [ ] 2.14 L1 test: absent label keeps the pending label — same file. Triple: operator session with no `label` key in the body · `POST /api/pair/approve` · `200`, device label equals the label recorded at redemption. Verify red first. (test-plan #E22)
- [ ] 2.15 L1 test: bound counts bytes, not characters — same file. Triple: operator session with a multi-byte label of exactly 64 bytes and a second of 65 bytes · `POST /api/pair/approve` · `200` then `400`. Verify red first. (test-plan #E23)
- [ ] 2.16 L1 test: supplied non-string label is rejected — same file. Triple: operator session with `label: 123` · `POST /api/pair/approve` · `400`, P remains pending, NOT a `200` silently keeping the pending label, no `500`. Verify red first. (test-plan #E24)
- [ ] 2.17 Implement D2: add a `via === "device"` → `401` short-circuit at the top of `createOperatorGuard`, before the session / local-token / `isGenuinelyLocal` clauses; swap `preHandler: networkGuard` → `preHandler: operatorGuard` on `DELETE /api/paired-devices/:id` and `POST /api/pair/approve`; add the trim + `MAX_DEVICE_LABEL_BYTES` check with its 0-byte lower bound on approve, mirroring the mint route, and pass the trimmed value to `pairing.approve`; update the route-file docblock lines that still say "networkGuard" / "Phase C". Verify 2.1–2.16 green and the existing pairing / paired-devices / bearer-auth tests green.

## 3. Argv migration (#514) — `packages/server/src/git-worktree/git-operations.ts`

- [ ] 3.1 L1 test: command separator passed verbatim — see `packages/server/src/__tests__/git-worktree-lifecycle-ops.test.ts` for the `vi.spyOn(platformExec, "execFileSync")` harness. Triple: create-worktree naming branch `feat&calc` · `addWorktree` · a spy call with `file === "git"` and `feat&calc` as exactly one argv element, and the `execSync` spy never called. Verify red first. (test-plan #E25)
- [ ] 3.2 L1 test: merge metacharacter branch — same file. Triple: worktree whose branch is `x; echo pwned` · `mergeWorktree` · `git merge --no-ff` receives `x; echo pwned` as one argv element. Verify red first. (test-plan #E26)
- [ ] 3.3 L1 test: diff-stat base with spaces — same file. Triple: diff-stat resolving base `release 2026` · `worktreeDiffStat` · the range is a single argv element `release 2026..<branch>`. Verify red first. (test-plan #E27)
- [ ] 3.4 L1 test: PR title passed untouched — same file. Triple: create-pull-request with a title containing `"` and `$(` and an injected `ghPath` · `createPullRequest` · the title is one argv element with both characters intact and argv[0] is the injected absolute `ghPath`, not the literal `"gh"`. Verify red first. (test-plan #E28)
- [ ] 3.5 L1 test: remaining migrated sites carry argv shape — same file. Triple: caller-supplied values for `pushBranch`, `addWorktreeFromPr`, `listPullRequests`, the fetch and the `resolveDefaultBase` hint verify · each op invoked · every call goes through `execFileSync`/`spawnSync` with the caller value as one argv element and `execSync` is never called. Verify red first. (test-plan #E29)
- [ ] 3.6 L1 test: no shell interposed on Windows — same file. Triple: platform reported as `win32` with an injected absolute `ghPath` · every migrated git/`gh` op · argv[0] is the resolved binary path, never `cmd.exe`, and no `/d /s /c` sequence appears. Verify red first — guards against a `buildSafeArgv` regression. (test-plan #E30)
- [ ] 3.7 L1 test: timeouts and env survive the migration — same file. Triple: every migrated git/`gh` op invoked once · inspect the options passed to each spawn · `timeout` equals `GIT_TIMEOUT` and `env` still carries `GH_PROMPT_DISABLED` / `GIT_TERMINAL_PROMPT=0` where it does today. Verify red first. (test-plan #P2)
- [ ] 3.8 L1 test: exit-code → stable-code mapping unchanged — same file. Triple: `execFileSync` spy throws `{ status: 1, stderr: "already used by worktree at '/x'" }`, and separately a merge-conflict stderr · `addWorktree` / `mergeWorktree` · the same stable codes as today (`branch_in_use`, `merge_conflict`, …). Verify GREEN before and after 3.12 — behaviour-preserving. (test-plan #X1)
- [ ] 3.9 L1 test: conflict notice on stdout is still classified — same file. Triple: spy throws with the conflict notice on `stdout` and an empty `stderr` · `mergeWorktree` · `merge_conflict`, proving the stdout+stderr concatenation is preserved. Verify red first. (test-plan #X2)
- [ ] 3.10 L1 test: missing git binary reports a dedicated code — same file. Triple: spy throws `ENOENT` with `status: null` and no `stderr` · `addWorktree` · a stable code identifying git as not found, no crash, not an empty-stderr generic failure. Verify red first. (test-plan #X3)
- [ ] 3.11 L1 test: missing gh binary reports a dedicated code — same file. Triple: spy throws `ENOENT` with `status: null` · `createPullRequest` · a stable code identifying gh as not found. Verify red first. (test-plan #X4)
- [ ] 3.12 Implement D3: change `run`/`tryRun` to take `string[]` and call `execFileSync(argv[0], argv.slice(1), { cwd, encoding, stdio, timeout, env })`; convert all 17 sites (`:55`, `:81`, `:669`, `:716`, `:1170`, `:1201`, `:1247`, `:1260`, `:1287`, `:1320`, `:1357`, `:1388`, `:1418`, `:1452`, `:1771`, `:1875`, `:1912`) to argv literals keeping the resolved `ghPath` as argv[0]; add the `ENOENT`/`ENOTDIR` → dedicated stable-code mapping; delete `shellEscape`, its two comment mentions (`:1097`, `:1118`) and the `execSync` import, leaving the `ban:child_process-ok` async `execFile` import in place. Verify 3.1–3.11 green and all `git-worktree*` tests green.
- [ ] 3.13 L1 test: property cannot silently regress — same file. Triple: the `git-operations.ts` module source read as text · assertion · contains neither `execSync(` nor `shellEscape`, comments included. Verify green after 3.12. (test-plan #E31)
- [ ] 3.14 L2 test: git/gh prompt would block — see `qa/tests/05-git-ops.sh` for the git-ops smoke harness. Triple: a git remote op that would prompt for credentials · worktree push / PR path exercised in a real process · the op fails fast on git's own non-interactive error rather than hanging to the timeout, proving `env` survived in a real spawn. Verify red first if feasible, else assert the new behaviour. (test-plan #X8)

## 4. Host gate default (#637 follow-up) — `packages/shared/src/config.ts`, `packages/server/src/auth/host-gate.ts`, `server.ts`, client settings

- [ ] 4.1 L1 test: `hostGate.mode` loader decision table — see `packages/shared/src/__tests__/config-host-gate.test.ts`. Triple: `parseHostGateMode` called with `undefined`, `"report"`, `"enforce"`, `"yes"`, `123`, `null` · direct call · `undefined` → `"enforce"`, `"report"` → `"report"`, `"enforce"` → `"enforce"`, and `"yes"` / `123` / `null` → `"report"`. Verify red first. (test-plan #E32)
- [ ] 4.2 L1 test: absent key loads as enforce — same file. Triple: `config.json` with no `hostGate` key · `loadConfig` · loaded `hostGate.mode === "enforce"`. Verify red first. (test-plan #E33)
- [ ] 4.2b L1 test: loadConfig fallback paths resolve to enforce — same file. Triple: (a) no `config.json` on disk, (b) a `config.json` that is empty/whitespace, (c) a `config.json` holding malformed JSON · `loadConfig` · each returns `hostGate.mode === "enforce"`, never `"report"` (test-plan #E33b). Verify red first — it fails until `DEFAULTS.hostGate` is flipped.
- [ ] 4.3 L1 test: explicit report survives a round-trip — same file. Triple: `config.json` holding `hostGate.mode: "report"` · `loadConfig` · loaded value is `"report"`. Verify red first. (test-plan #E34)
- [ ] 4.4 L1 test: unrelated write does not seed hostGate — see `packages/server/src/__tests__/host-gate-config-api.test.ts`. Triple: `config.json` with no `hostGate` key · `PUT /api/config` updating an unrelated key · the written file still has no `hostGate` key, so the boot source stays `default`. Verify red first. (test-plan #E35)
- [ ] 4.5 L1 test: mode resolution decision table — see `packages/server/src/__tests__/host-gate.test.ts`. Triple: `(env, configMode)` pairs `(unset, undefined)`, `("enforce", undefined)`, `("report", "enforce")`, `("yes", undefined)`, `("yes", "report")` · `resolveHostGateMode` · `enforce`, `enforce` with `envOverridden: true`, `report` with `envOverridden: true`, `enforce`, `report`. Verify red first. (test-plan #E36)
- [ ] 4.6 L1 test: enforce default refuses — same file as 4.4. Triple: env unset with no `hostGate` config · `GET /api/health` with `Host: rebind.example` · `403` with `error: "host_not_allowed"`, one refusal line logged, and no `Access-Control-Allow-Origin` header. Verify red first. (test-plan #E37)
- [ ] 4.7 L1 test: enforce default admits the loopback/IP population — same file as 4.4. Triple: env unset with no `hostGate` config · `GET /api/health` with `Host:` each of `localhost:8000`, `127.0.0.1:8000`, `[::1]:8000`, `192.168.1.20:8000` · all four `200`. Verify red first. (test-plan #E38)
- [ ] 4.8 L1 test: report opt-out still works — same file as 4.4. Triple: `hostGate.mode: "report"` with env unset · request with a non-admissible `Host` · served normally and one refusal line logged. Verify red first. (test-plan #E39)
- [ ] 4.9 L1 test: boot line names mode and source — see `packages/server/src/__tests__/host-gate-wiring.test.ts` for the boot-wiring harness. Triple: (a) env unset + config absent, (b) `hostGate.mode: "report"`, (c) `PI_DASHBOARD_HOST_GATE=report`, (d) `PI_DASHBOARD_HOST_GATE=yes` + config absent · server boot · exactly one host-gate boot line per boot reporting `mode=enforce source=default`, `mode=report source=config`, `mode=report source=env`, `mode=enforce source=default`. Verify red first. (test-plan #E40)
- [ ] 4.10 L1 test: refusal page does not enumerate admitted hosts — see `packages/server/src/__tests__/host-gate.test.ts`. Triple: enforce default with a live tunnel origin and an `allowedHosts` entry configured · `GET /` with a refused `Host` and `Accept: text/html` · body contains neither the tunnel origin nor the `allowedHosts` entry, contains the escaped received Host plus `localhost:<port>`, `allowedHosts` and `publicBaseUrls`, and contains no `<script>` and no `/assets/` reference. Verify red first. (test-plan #E41)
- [ ] 4.11 L1 test: refused WebSocket upgrade under the default — see `packages/server/src/__tests__/host-gate-upgrade.test.ts`. Triple: env unset with no `hostGate` config · WS upgrade with a non-admissible `Host` · `HTTP/1.1 403` and the socket destroyed. Verify red first. (test-plan #X6)
- [ ] 4.12 Implement D4 shared side: `parseHostGateMode(raw, absentDefault: HostGateMode = "enforce")` returning `absentDefault` for `undefined`, the value for a recognised string, and `"report"` for anything else; AND flip the module-private `DEFAULTS.hostGate` to `{ mode: "enforce" }` — it is NOT inert: `loadConfig`'s three early returns (missing file, empty file, malformed JSON `catch`) return `DEFAULTS` wholesale without ever reaching `parseHostGateMode`, so leaving it at `report` would keep a fresh install report-only. Verify 4.1–4.3 and 4.2b green and `config*.test.ts` green.
- [ ] 4.13 Implement D4 server side: align `liveHostGateMode(fallback = "enforce")` and `resolveHostGateMode`'s `?? "enforce"`; add one boot line in `server.ts` next to the existing `resolveHostGateMode` call, deriving `source` from `env` when a recognised env value was supplied, `config` when the raw config file carried `hostGate.mode`, else `default`. Verify 4.4–4.11 green and `host-gate*.test.ts` / `cors.test.ts` / `ws-origin-gate.test.ts` green, updating any test that asserted the old default.
- [ ] 4.14 L1 test: settings shows the real effective default — see `packages/client/src/components/settings/__tests__/settings-field-contract.test.tsx`. Triple: config payload with `hostGate.mode` absent · Settings ▸ Security renders · the mode control converges to `enforce` and never displays `report`. Verify red first. (test-plan #F1)
- [ ] 4.15 L1 test: settings copy no longer claims a report-only default — see `packages/client/src/components/settings/__tests__/AllowedHostsSection.test.tsx`. Triple: `AllowedHostsSection` rendered · component render · the copy describes `enforce` as the default and names the `report` opt-out, with no remaining "default is report-only" claim. Verify red first. (test-plan #F2)
- [ ] 4.16 Implement D4 client side: `SettingsPanel.tsx` three `?? "report"` fallbacks (lines 335, 336, 1985) → `?? "enforce"`; update the `AllowedHostsSection` copy describing the default. Verify 4.14–4.15 green and the client suite green.
- [ ] 4.17 L3 test: dashboard still loads end-to-end under the enforce default — see `tests/e2e/host-gate-allow.spec.ts` for the host-gate e2e harness; read the port from `.pi-test-harness.json` (`dashboardPort`), never hardcode `:18000`. Triple: docker harness with default config and no `hostGate` key · browser loads the dashboard · page loads and the WebSocket connects. Verify red first if feasible, else assert the new behaviour. (test-plan #F3)
- [ ] 4.18 `CHANGELOG.md` `[Unreleased]` — **Breaking** entry: host gate now enforces by default; opt-out `hostGate.mode: "report"` / `PI_DASHBOARD_HOST_GATE=report`. Verify the entry renders under the correct heading.

## 5. Docs + closeout

- [ ] 5.1 Delegate to `DocScribe`: `docs/faq.md` and `docs/architecture.md` host-gate paragraphs (default now enforce, opt-out named); one-line note in the pairing/security section that revoke + approve are operator-only and refuse a device bearer. Verify grep `report-only` in `docs/` returns no stale "default" claim.
- [ ] 5.2 Update directory `AGENTS.md` rows: `packages/shared/src/AGENTS.md` (`config.ts` — `parseHostGateMode` absent-default), `packages/server/src/auth/AGENTS.md` (`host-gate.ts` default), `packages/server/src/routes/AGENTS.md` (`pairing-routes.ts` guards + device short-circuit), `packages/server/src/git-worktree/git-operations.ts.AGENTS.md` (argv, `shellEscape` gone, ENOENT codes), `packages/server/src/browser-handlers/AGENTS.md` + the `directory-service` row (`refreshOpenSpecGated`). Verify `kb dox lint` clean.
- [ ] 5.3 Full suite: `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` — zero failures. Run `npm run quality:changed` clean.
- [ ] 5.4 Docker harness smoke: `docker/test-up.sh`, `GET /api/health` on `localhost` and on the container IP literal both `200` with default config (enforce); `docker/test-down.sh`. Verify the Playwright E2E subset that exercises pairing (`tests/e2e/*pair*`) and `tests/e2e/host-gate-allow.spec.ts` is green.
- [ ] 5.5 Manual verification, deferred post-merge: open the `403` refusal page in a real browser and confirm it makes the fix obvious to an operator with no prior knowledge. (test-plan: manual-only, #F4)
- [ ] 5.6 Comment on #647, #665, #514 with the change name; #637 already closed with a pointer to this change.
