## 1. Package scaffold

- [x] 1.1 Create `packages/mcp-client-plugin/` (package.json id `mcp-client`, npm `@blackbelt-technology/pi-dashboard-mcp-client-plugin`, `requires.piExtensions: ["pi-mcp-adapter"]`, `pi-mcp-adapter` `^2.20.0` as a regular dependency, direct deps `ajv` + `strip-json-comments` + `@blackbelt-technology/pi-dashboard-shared`, exports `./client` `./server` `./core` (no host/React imports), claims: `settings-section`, `sidebar-folder-section`, `worktree-card-section`, `shell-overlay-route` at `/folder/:encodedCwd/mcp` depth 2; manifest `configSchema: ./configSchema.json` declaring `adapterLoadTimeoutMs` integer default 10000 min 1000 max 120000 with `additionalProperties: false`) and verify `pnpm install` links it, the manifest-validator test passes, and the host config route rejects `500`/`200000`/`"10s"` against that schema
- [x] 1.2 Add `mcp-client-plugin` to `BUNDLED_PLUGINS` in `packages/electron/scripts/bundle-server.mjs` and verify the bundle-completeness test passes
- [x] 1.3 Scaffold `packages/mcp-client-plugin/AGENTS.md` with rows for every file created in this change and verify `kb dox lint` reports no missing rows for the package

## 2. Config core (server, TDD)

- [x] 2.1 Write tests for the merge-only patch writer generalised from `apple-tools/src/mcp-config.ts` (patch `set`+`unset` preserves siblings + unknown keys, JSONC parse via `strip-json-comments`, `mcp-servers` alias preserved as the write key with no `mcpServers` shadow, hardened atomic write moved from `apple-tools/env.ts` (`wx` random tmp + `0o600` + fsync + rename; resulting file stays `0600`), unparseable target refused byte-identical, non-object existing entry refused, `__proto__`/`constructor`/`prototype` names refused and null-prototype merge objects, refusals as the closed `ConfigRefusal` union (`unparseable`|`entry-not-object`|`invalid-name`|`transport-conflict`|`write-failed`+code from EACCES/ENOSPC/ENOENT, file byte-identical) from one validation function shared with `checkConfigFiles`, remove returns the removed raw entry, remove deletes one key, enable deletes `disabled`, re-runs the adapter's full merge via the injected adapter port for the scope (`cwd` for project, scratch cwd for global), and writes `disabled: false` only when still disabled (tests with a stubbed port: no lower disable → key absent and an emptied entry deleted, lower disable → `false`, at most two writes), all target paths from the port's `getPiGlobalConfigPath` / `getProjectPiConfigPath` (test: stub pointing at a custom dir → writes land there), write-only ops never call the port's loading functions (port whose loaders throw → ops still succeed), `setDirectTools` never calls `writeDirectToolsConfig`, never calls `writeProjectServerDisabledOverride`, `settings` object merge + unset-deletes-key) via injected `ConfigIO`; verify they fail, then implement `src/core/config-writer.ts` and verify they pass
- [x] 2.2 Write tests for the effective-view reader: folder view calls `loadMcpConfig(undefined, cwd)` + `getServerProvenance(undefined, cwd)`; global view passes the `mkdtemp` scratch dir under `os.tmpdir()` as cwd and reads nothing under the server's `process.cwd()`; classifies by provenance `kind` + Pi-path equality (`user` → Pi global; `project` with `path === getProjectPiConfigPath(cwd)` → Pi folder; `project` otherwise i.e. `<cwd>/.mcp.json` → Shared; `import` → Shared labelled by `importKind`; absent → Other) — an import whose provenance `path` equals the Pi-global file is still Shared/read-only; lists lower defining layers; reports a JSONC-parse-error layer with path + message while still returning other layers' servers; redacts secret-marked values (incl. an inherited `bearerToken`) not defined in the scope's writable layer — scalar marker for scalars, `{redacted, keys:[{name,secret}]}` for `env`/`headers`/`requestHeadersCommand.env` with no values — and leaves own-layer secrets intact; reports each effective `settings` key's source layer or adapter-default; implement `src/core/effective-view.ts` and verify tests pass
- [x] 2.3 Move `probeAdapterVersion` + `ADAPTER_VERSION_FLOOR` from `mcp-server-plugin/src/server/provisioning.ts` and `readInstalledAdapterVersion` from `mcp-server-plugin/src/server/index.ts` into `src/core/adapter-verdict.ts`, probing `dirname(getPiGlobalConfigPath())/{npm/,}node_modules/pi-mcp-adapter/package.json` (test: custom agent dir via stubbed port -> probe follows it) and returning `AdapterVerdict { kind: ok|absent|below-floor|unparseable|unknown; installed?; floor; message? }` with a 30s TTL cache; move the existing probe tests, add one asserting `floor` and `installed` on `below-floor`, and verify they pass in the new package
- [x] 2.4 Extract the admission module (`isAllowedCwd`, `canonPath`, `mainCheckoutPath` + its git checkout-root probes) from `packages/kb-plugin/src/server/kb-routes.ts` into `packages/shared/src/cwd-guard.ts` (move the existing kb tests with them; kb-plugin imports from shared, behaviour unchanged), then write tests for mcp-client project-scope admission (unknown cwd → no write, traversal alias → no write, worktree of a known main checkout → admitted) and verify they pass against the writer entry points
- [x] 2.5a Write tests for `src/core/adapter-worker.ts` (default adapter port: `loadMcpConfig`/`getServerProvenance` executed in one lazily-spawned `worker_threads` Worker per port instance, requests serialised, each call takes `{ timeoutMs }`; on expiry `worker.terminate()` + reject `AdapterTimeoutError { timeoutMs }`, next call respawns; a stub worker script that sleeps proves the event loop stays free — a concurrent timer fires on schedule; write-only ops on the same service never spawn the worker — assert `Worker` constructor spy count 0 after `ensureServerEntry`/`checkConfigFiles`/`ensureAdapterPackage`), verify they fail, implement, verify they pass
- [x] 2.5 Implement `createMcpClientConfigService({ configIO, knownCwds, adapter? })` in `src/core/` (adapter port defaults to `pi-mcp-adapter/config`; `ensureAdapterPackage` targets `dirname(getPiGlobalConfigPath())/settings.json`) exporting the `mcp-client.config` service object (`adapterVerdict`, `readServerEntry`, `ensureServerEntry`, `setServerDisabled`, `setDirectTools`, `ensureAdapterPackage` lifted from `mcp-config.ts`, `checkConfigFiles({ serverName, fields })` (global files only; generic parse / `mcpServers` object / `packages` array status plus a dry run of `ensureServerEntry` over the post-patch entry, replacing the iMCP-hard-coded `validateConfigShape`), `Scope` union) and verify tests that `ensureServerEntry` refreshes `command` while preserving `disabled` + `directTools`, that `ensureAdapterPackage` preserves other `packages`, that `checkConfigFiles` never writes and reports the same `ConfigRefusal` exactly when `ensureServerEntry(name, fields)` would refuse (existing `url` + fields `{command}` → `transport-conflict` in both), that a factory instance built with in-memory IO + a stubbed port pointing at a temp dir touches no real path, and one integration test that runs the real adapter port under `HOME=<tmp>` via dynamic import and reads the expected shared-global paths

## 3. Schema

- [x] 3.1 Author `schema/mcp-config.schema.json` (ServerEntry with every field optional, `x-transport` annotation for command/url/socket, McpSettings with `default` as display metadata, `x-secret` on `bearerToken`, `oauth.clientSecret`, `headers`, `env`, `requestHeadersCommand.env`, `x-atomic` on `env`, `headers`, `searchKeywords`, `oauth`, `requestHeadersCommand`; no `oneOf`) transcribed from `pi-mcp-adapter/dist/types.d.ts` and verify a bidirectional compile-time key-set test (per `verify-compile-time-assertions-fail-closed`) fails on a misspelled property, fails on an omitted `ServerEntry` key, and passes on the real schema
- [x] 3.2 Add validation of server patches and settings patches with the plugin's own `new Ajv({ strict: true, useDefaults: false, allErrors: true })` + `addKeyword` for `x-secret`/`x-transport`/`x-atomic` (test: schema compiles; `set: {command}` validates to exactly `{command}` with no injected default) plus layer-entry transport check (≤1 transport in the resulting entry, enforced in the writer for every path; ≥1 only when no lower source defines the server, enforced in the HTTP layer via the adapter merge for the scope cwd / scratch cwd) and verify tests for: partial `{disabled: true}` project patch accepted, new server with no transport rejected naming the transport fields, two transports in one layer entry rejected, `command`-only override of a `url` shared server accepted, unknown extra field accepted and preserved through write

## 4. Server plugin + routes

- [x] 4.1 Implement `src/server/index.ts` `registerPlugin`: build the service via the factory with real IO + `host.knownFolderCwds`, `ctx.provide("mcp-client.config", service)` before routes; verify a loader test that a plugin with `dependsOn: ["mcp-client"]` observes the service during its own registration
- [x] 4.2 Implement routes `GET /effective?cwd=`, `GET /schema`, `GET /adapter`, `PUT /servers/:name` (body `{scope, set, unset?}`), `DELETE /servers/:name`, `PUT /servers/:name/disabled`, `PUT /settings` (patch); project `cwd` in body for `PUT`, `?cwd=` for `DELETE`; `:name` URL-decoded and validated (1–128 chars, no `/` `\` or control chars, not `.`/`..`/`__proto__`/`constructor`/`prototype`; whitespace allowed), every route including `GET /effective` registered with `{ preHandler: ctx.networkGuard }` as in `goal-plugin/src/server/routes.ts`, 400 (schema/name, field list) / 403 (unknown cwd, guard refusal) / 409 (unparseable target) / 504 `{ error: "adapter-timeout", timeoutMs }` when the port rejects with `AdapterTimeoutError`, `timeoutMs` read per request from `ctx.getPluginConfig().adapterLoadTimeoutMs ?? 10000` (test: config write to 2500 → next request's port call receives `timeoutMs: 2500`; enable whose merge times out leaves the removal write in place and writes no `disabled: false`); verify route tests cover each status, that a whole-entry body without `set` is rejected, that a guard-refused call (GET and PUT) performs no IO, and that mutating routes require the plugin auth like other plugins
- [x] 4.3 Write the `dependsOn` matrix test in the loader suite (both enabled → order mcp-client first; mcp-client disabled → apple-tools `missingDeps` and not loaded; cascade toggle names `apple-tools`) and verify it passes

## 5. apple-tools shrink

- [x] 5.1 Edit `packages/apple-tools/package.json`: add `dependsOn: ["mcp-client"]`, remove `requires.piExtensions`, drop the `pi-mcp-adapter` dependency, add `@blackbelt-technology/pi-dashboard-mcp-client-plugin`; verify the manifest test and `pnpm install` pass
- [x] 5.2 Refactor `install.ts` to take the `McpClientConfigService` via `InstallerEnv` (`env.ts` `createInstallerEnv` builds it from `@blackbelt-technology/pi-dashboard-mcp-client-plugin/core` with its existing `configIO` seam and the default adapter port, dropping the hard-coded `join(home, ".pi", "agent")` `mcpJsonPath`/`settingsJsonPath` so the CLI honours `PI_CODING_AGENT_DIR` like pi does; `bin/install.ts` keeps working hostless) and call `ensureServerEntry("iMCP", { command }, { kind: "global" })`, `ensureAdapterPackage()`, and `checkConfigFiles({ serverName: "iMCP", fields: { command } })` in place of the local `ensureMcpEntry` / `ensureAdapterPackage` / `validateConfigShape`; update its tests with a stubbed service and verify the existing merge-only/atomic/unparseable (`CONFIG_UNPARSEABLE`)/write-failed (`CONFIG_WRITE_FAILED`)/no-credential/check-mode-parity scenarios still pass plus the new "operator-set fields survive re-provisioning" scenario
- [x] 5.3 In `server/index.ts` consume the service at registration (throw descriptively if `undefined`), replace the `readImcpEntry` panel readout (L77) with `readServerEntry("iMCP", { kind: "global" })`, delete the `set-disabled` and `set-direct-tools` actions, delete `src/mcp-config.ts` and its tests; verify `npm test` passes with no reference to `readImcpEntry`/`setServerDisabled` in `apple-tools`
- [x] 5.4 Trim `client/index.tsx`: remove enable/disable + direct-tools controls, add "Manage MCP servers" link to `/settings/plugins/mcp-client`, add the missing-dependency banner (Enable link to the plugins index) driven by the plugin's own row `missingDeps` from `GET /api/plugins` that hides Run installer; verify component tests for both states (the section mounts while `enabled: true, loaded: false` — the client enabled-set keys on `enabled`)
- [x] 5.5 Update `packages/shared/src/recommended-extensions.ts`: add a `pi-mcp-adapter` extension row and a `@blackbelt-technology/pi-dashboard-mcp-client-plugin` row with `dashboardPlugin: "mcp-client"` and `requires: { piExtensions: ["pi-mcp-adapter"] }` (test: plugins index renders the inline Install for the mcp-client row's missing requirement), remove `requires` from the apple-tools row (keep its `dashboardPlugin: "apple-tools"`), and repoint the floor-enforcement comment from `mcp-server-plugin/.../provisioning.ts probeAdapterVersion` to `mcp-client-plugin/src/core/adapter-verdict.ts`; verify its test

## 6. mcp-server-plugin consume

- [x] 6.1 Delete `probeAdapterVersion` from `mcp-server-plugin/src/server/provisioning.ts` and `readInstalledAdapterVersion` + the registration-time `logger.warn` call in `server/index.ts` L53-67/L187; call `ctx.consume("mcp-client.config")?.adapterVerdict() ?? { kind: "unknown", floor }` lazily (replace `probe.ok` checks with `kind === "ok"`) and emit the below-floor warning once on the first request to the `/mcp` route; verify tests for verdict-passthrough, absent-service → `unknown`, warn-once-on-first-`/mcp`-request-not-at-registration, and that the plugin declares no `dependsOn`
- [x] 6.2 Rewrite `provisionDashboardEntry` + `mcpConfigPath()` (`server/index.ts` L41-43) to use `createMcpClientConfigService({ configIO })` from `@blackbelt-technology/pi-dashboard-mcp-client-plugin/core` (package dependency added to `mcp-server-plugin/package.json`, no manifest `dependsOn`): `readServerEntry("pi-dashboard", global)` (file-only) for the refuse-to-overwrite check, then `ensureServerEntry` (merge-only: operator-added `disabled`/`headers` on the entry survive a refresh — test it); delete its local `parseConfig`/`JSON.parse` writer; verify tests: alias key preserved at boot, JSONC file provisioned, `PI_CODING_AGENT_DIR` honoured via a stubbed port, non-dashboard entry still refused with the existing message

## 7. Client — global settings (S1/S2)

- [x] 7.1 Implement `useEffectiveConfig(cwd?)` store (single in-flight per key) and `useAdapterStatus` deriving one status object; verify hook tests for dedupe and for the read-only flag under `below-floor`/`absent`
- [x] 7.2 Implement `McpSettingsClaim` server list (provenance badges Shared 🔒 / Pi global, sorted, parse-error row, empty state, skeleton) and verify component tests per state from the spec
- [x] 7.3 Implement row enable/disable with pending + revert-on-failure and verify tests
- [x] 7.4 Implement the schema-driven server editor (field renderer incl. `boolean | string[]` toggle-with-list, nested object groups, validated-JSON fallback for `outputGuard`/`trace`-shaped fields, plus a type-coverage test that every `ServerEntry`/`McpSettings` field maps to a widget or the fallback; transport tabs command/url/socket hide the other transports, initial tab from the layer entry else effective, Advanced disclosure, inline + summary errors, read-only View variant with "Override at Pi global", `x-atomic` object-field override (env/headers/searchKeywords/oauth/requestHeadersCommand, list derived from the schema) as an explicit action starting empty with the "N inherited keys incl. K secrets will no longer apply" note, unknown fields preserved, single primary Save) and verify component tests
- [x] 7.5 Implement secret masking (`x-secret` or `Authorization|*_TOKEN|*_KEY|*_SECRET`) with per-field reveal that resets on reopen, and inherited secrets rendered as the server's redaction placeholder with no reveal and excluded from the patch; verify tests
- [x] 7.6 Implement the global settings form over the `settings` object as a host draft source via `useSettingsDraftSource({ id: "plugin:mcp-client" })` (no local Save/footer; host Save Bar commits a patch of only changed keys; unset keys show the effective fallback labelled with its lower layer or as adapter default, clear action labelled "use inherited" when a lower layer defines the key, "comments not preserved" footer note) plus a separate "Dashboard plugin settings" group in the same draft source holding `adapterLoadTimeoutMs` from `usePluginConfig` (number input, inline range error 1000–120000 blocks commit, help text; `commit` awaits both `plugin_config_write` and the adapter `PUT /settings` patch, issuing only the one(s) whose keys changed, and stays dirty showing the failing group's error if either rejects) and verify tests incl. dirty → host discard-confirm on route change, commit → clean, timeout-only change issues no `PUT /settings`, and a `504 adapter-timeout` list state with retry + link to the timeout field
- [x] 7.7 Verify accessibility: axe run on the rendered section reports no violations and touch targets ≥44px at 390px in a Playwright spec

## 8. Client — folder surfaces (S3/S4)

- [x] 8.1 Implement `FolderMcpSection` `SlotPill` for `sidebar-folder-section` and `worktree-card-section` (count · off · error marker, loading placeholder, muted "not tracked" state on a 403 cached per cwd and invalidated when the session or pinned-folder list changes, navigates to `/folder/<encodedCwd>/mcp`, worktree uses its own cwd) and verify component tests
- [x] 8.2 Implement `FolderMcpPage` route (403 from the server → not-allowed empty state, no retry, nothing rendered; 504 → timeout error state with a retry action issuing exactly one request; pill shows the error marker naming the timeout on 504), inherited hints, override chips, folder-scope switches, adapter read-only banner; verify component tests
- [x] 8.3 Implement "Override…" showing effective values and writing only changed keys to the folder layer, and chip removal with an undo toast that re-submits the raw entry returned by `DELETE`; verify tests incl. byte-equivalent restoration of an entry with unknown keys
- [x] 8.4 Implement mobile presentation (bottom-sheet editor, display-only chips with "Remove override" in the sheet) and verify a Playwright spec at 390px

## 9. Integration + docs

- [x] 9.1 Run the docker harness E2E: enable both plugins, toggle iMCP from the MCP page, run apple-tools installer, assert `~/.pi/agent/mcp.json` keeps `disabled` and gains `command`; disable `mcp-client`, assert apple-tools row shows `missingDeps` and its panel hides Run installer; verify the spec passes
- [x] 9.2 Security pass (`security-hardening`): review route layer for cwd admission on every project write, schema validation before IO, secrets absent from logs; verify findings closed or filed
- [x] 9.3 Delegate `docs/` prose to DocScribe: `docs/architecture.md` plugin table row, `README.md` MCP section; update `packages/apple-tools/AGENTS.md`, `packages/mcp-server-plugin/AGENTS.md`, `packages/shared/src/AGENTS.md` rows; verify `kb dox lint` clean
- [x] 9.4 `npm run quality:changed` + `npm test` green; `review-code` pass on the full diff; verify no `set-disabled`/`set-direct-tools` strings remain outside archive

## 10. Tests folded from test-plan.md (one task per manifest row; ids stable)

### 10.1 Unit (vitest, `packages/*/src/**/__tests__/`)

- [x] 10.1 E1 CFG/HTTP name validation [BVA] — input: name of length 1 (`a`) · trigger: `PUT /servers/a` `{scope:global,set:{command:"/bin/x"}}` · observable: 200; entry `a` written (test-plan #E1; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.2 E2 CFG/HTTP name validation [BVA] — input: name of length 128 · trigger: same PUT · observable: 200 (test-plan #E2; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.3 E3 CFG/HTTP name validation [BVA] — input: name of length 129 · trigger: same PUT · observable: 400 naming `name`; no IO (ConfigIO spy: 0 reads, 0 writes) (test-plan #E3; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.4 E4 CFG/HTTP name validation [BVA] — input: name `""` (empty segment, `PUT /servers/`) · trigger: route call · observable: 404 or 400; no IO (test-plan #E4; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.5 E5 CFG/HTTP name validation [EP (invalid partitions)] — input: names `a/b`, `a\b`, `a\u0007b`, `.`, `..`, `%2e%2e` (decoded `..`), `__proto__`, `constructor`, `prototype` · trigger: one PUT per name · observable: each 400; no IO for any (test-plan #E5; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.6 E6 CFG/HTTP name validation [EP (valid partition)] — input: name `my server` (space), `%E2%9C%93` (decoded `✓`) · trigger: PUT · observable: 200; file key is the decoded string (test-plan #E6; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.7 E7 CFG/Writes target only Pi-owned layers [decision-table (scope × cwd)] — input: scope=`global` with cwd supplied / scope=`project` without cwd / scope=`project` with cwd / unknown scope · trigger: PUT · observable: global+cwd → cwd ignored, writes global path; project−cwd → 400; project+known cwd → writes `<cwd>/.pi/mcp.json`; unknown → 400 (test-plan #E7; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.8 E8 CFG/Alias key preserved [decision-table (existing keys)] — input: target file with (a) `mcpServers` only, (b) `mcp-servers` only, (c) both, (d) neither · trigger: upsert `x` · observable: (a) under `mcpServers`; (b) under `mcp-servers`, no `mcpServers` added; (c) written under the key the adapter reads first (test cites the adapter's `mcpServers ?? mcp-servers` line), other key byte-identical; (d) `mcpServers` created (test-plan #E8; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.9 E9 CFG/Writes are merge-only [EP] — input: file `{"$schema":"x","unknownTop":{"k":1},"mcpServers":{"a":{"command":"a","weird":[1]},"b":{...}}}` · trigger: patch `a` `set:{args:["1"]}, unset:["weird"]` · observable: `a` = `{command:"a",args:["1"]}`; `b`, `$schema`, `unknownTop` byte-equal (test-plan #E9; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.10 E10 CFG/Writes are merge-only [BVA (empty patch)] — input: `set:{}`, `unset:[]` · trigger: PUT · observable: 200, file byte-identical (no rewrite) OR rewritten but semantically identical — assert semantic equality + mtime unchanged if no-op (test-plan #E10; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.11 E11 CFG/Unset [BVA] — input: `unset:["nonexistent"]` · trigger: PUT · observable: 200; no error; other fields untouched (test-plan #E11; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.12 E12 CFG/Prototype key refused [EP] — input: service call `ensureServerEntry("__proto__", …)` and `setServerDisabled("constructor", …)` and `checkConfigFiles({serverName:"prototype"})` · trigger: each call · observable: refusal `invalid-name`; no file read for ensure/setDisabled; `Object.prototype` unpolluted (`({}).polluted === undefined`) (test-plan #E12; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.13 E13 CFG/Prototype key refused [EP (payload keys)] — input: `set:{"__proto__":{"polluted":true}}` inside a valid server patch · trigger: PUT · observable: 400 (schema: no such field) or key dropped; `({}).polluted === undefined` afterwards (test-plan #E13; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.14 E14 CFG/Disabled flag semantics [state-transition] — input: Pi-global entry `{command:"a",disabled:true}`, stub port: merged view says enabled after removal · trigger: enable at global · observable: entry is `{command:"a"}`; exactly 1 write (test-plan #E14; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.15 E15 CFG/Disabled flag semantics [state-transition] — input: Pi-global entry `{disabled:true}` only (no other keys), stub port merged view enabled · trigger: enable · observable: `mcpServers.x` key absent; sibling servers kept (test-plan #E15; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.16 E16 CFG/Disabled flag semantics [state-transition] — input: Pi-global entry `{disabled:true}`, stub port merged view still disabled (shared layer disables) · trigger: enable · observable: entry `{disabled:false}`; port loader called exactly once; ≤2 writes (test-plan #E16; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.17 E17 CFG/Disabled flag semantics [state-transition (illegal edge)] — input: server absent from Pi-global, merged view disabled via `<cwd>/.mcp.json` import · trigger: enable at project · observable: `<cwd>/.pi/mcp.json` gains `{x:{disabled:false}}`; `~/.pi/agent/mcp.json` byte-identical (test-plan #E17; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.18 E18 CFG/Disabled flag semantics [state-transition] — input: server already enabled everywhere · trigger: enable · observable: no `disabled` key written; file semantically unchanged (test-plan #E18; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.19 E19 CFG/Disabled flag semantics [state-transition] — input: server enabled at global · trigger: disable at project scope for known cwd · observable: `<cwd>/.pi/mcp.json` has `disabled:true`; global file byte-identical (test-plan #E19; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.20 E20 CFG/Layer model provenance [decision-table (kind × path)] — input: provenance stubs: `user`/global path; `project`/`<cwd>/.pi/mcp.json`; `project`/`<cwd>/.mcp.json`; `import` with path == global path; server absent from map · trigger: classify each · observable: Pi global (writable) / Pi folder (writable) / Shared (ro) / Shared labelled importKind (ro) / Other + source kind (ro) (test-plan #E20; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.21 E21 CFG/Layer model provenance [EP] — input: server in shared `.mcp.json` AND `.pi/mcp.json` AND global · trigger: effective view for cwd · observable: `provenance` = [Pi folder, Shared, Pi global] in precedence order; fields = adapter-merged result (stub returns a known merge) (test-plan #E21; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.22 E22 CFG/Redaction [decision-table (field × own/inherited)] — input: `bearerToken`, `oauth.clientSecret`, `headers{Authorization,Accept}`, `env{API_KEY,PATH}`, `requestHeadersCommand.env{X_SECRET}` each defined (a) in shared layer, (b) in the scope's Pi-owned layer · trigger: effective view at that scope · observable: (a) scalar → marker; records → `{redacted:true, keys:[{name,secret}]}` with `Authorization`/`API_KEY`/`X_SECRET` secret=true, `Accept`/`PATH` secret=false; `JSON.stringify(response)` contains no secret value; (b) plain values present (test-plan #E22; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.23 E23 CFG/Redaction [EP (scope crossing)] — input: secret defined in Pi global · trigger: effective view at project scope for cwd · observable: redacted (global is not the project scope's writable layer) (test-plan #E23; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.24 E24 CFG/Global view uses no project layers [EP] — input: server `process.cwd()` contains `.mcp.json` and `.pi/mcp.json` with server `leak` · trigger: `GET /effective` (no cwd) · observable: `leak` absent; port loader received cwd under `os.tmpdir()`; that dir is empty; ConfigIO spy shows no read under `process.cwd()` (test-plan #E24; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.25 E25 CFG/JSONC [EP] — input: layer file `{ /* c */ "mcpServers": { "a": { "command": "a", }, }, }` · trigger: read + upsert `b` · observable: read: no parse error, `a` present; write: valid JSON, `a`+`b` present, comments gone (test-plan #E25; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.26 E26 CFG/Schema transport exclusivity [decision-table (transports in resulting entry)] — input: resulting layer entry has {command}, {url}, {socket}, {command,url}, {url,socket}, {command,url,socket}, {} · trigger: writer validation · observable: first three accepted; any ≥2 → `transport-conflict` naming every transport present; {} → accepted by writer (≥1 rule is HTTP-only) (test-plan #E26; see packages/apple-tools/src/__tests__/package-manifest.test.ts)
- [x] 10.27 E27 CFG/Schema ≥1 transport (HTTP) [decision-table] — input: resulting entry {} and (a) lower source defines server, (b) none · trigger: PUT · observable: (a) 200; (b) 400 naming `command`,`url`,`socket`; no write (test-plan #E27; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.28 E28 CFG/Schema ≥1 transport (HTTP) [EP (cwd used for merge)] — input: project scope, cwd=K; server defined only in K's `.mcp.json` · trigger: PUT `{disabled:true}` at project · observable: port loader invoked with cwd=K (not scratch, not `process.cwd()`); 200 (test-plan #E28; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.29 E29 CFG/Schema defaults [EP] — input: schema property with `default` (e.g. a settings key) · trigger: PUT settings `{set:{oneKey:v}}` and PUT server `{set:{command:"x"}}` · observable: written objects contain exactly the set keys; `useDefaults` off (test-plan #E29; see packages/apple-tools/src/__tests__/package-manifest.test.ts)
- [x] 10.30 E30 CFG/Schema coverage [compile-time assertion (fail-closed)] — input: (a) real schema; (b) schema with `bearerToken` misspelled; (c) schema missing `disabled`; (d) type missing a schema key · trigger: `tsc` on the key-set test · observable: (a) passes; (b)(c)(d) each fail compile (per `verify-compile-time-assertions-fail-closed`) (test-plan #E30; see packages/apple-tools/src/__tests__/package-manifest.test.ts)
- [x] 10.31 E31 CFG/Schema Ajv strict [EP] — input: schema containing `x-secret`,`x-transport`,`x-atomic` · trigger: `new Ajv({strict:true})` + `addKeyword` ×3 + `compile` · observable: compiles without throw; removing one `addKeyword` → compile throws `unknown keyword` (test-plan #E31; see packages/apple-tools/src/__tests__/package-manifest.test.ts)
- [x] 10.32 E32 CFG/Global settings merge-only [EP] — input: file `{settings:{a:1,b:2},mcpServers:{x:{…}},other:1}` · trigger: `PUT /settings {set:{a:9},unset:["b"]}` · observable: `settings` = `{a:9}`; `mcpServers`, `other` byte-equal (test-plan #E32; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.33 E33 CFG/Global settings fallthrough [decision-table (Pi-global × shared × default)] — input: key set in Pi-global only / shared only / both / neither · trigger: effective view `settings[key].source` · observable: Pi global / Shared(path) / Pi global / `default` with adapter default value (test-plan #E33; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.34 E34 CFG/Adapter verdict [BVA (semver vs floor 2.20.0)] — input: installed `2.19.99`, `2.20.0`, `2.20.1`, `2.30.0`, `3.0.0-beta.1`, `"garbage"`, package.json absent · trigger: `adapterVerdict()` · observable: below-floor(installed,floor) / ok / ok / ok / ok (includePrerelease) / unparseable / absent (test-plan #E34; see packages/mcp-server-plugin/src/server/__tests__/provisioning.test.ts)
- [x] 10.35 E35 CFG/Adapter verdict probe path [decision-table] — input: stub port global path `<tmp>/agent/mcp.json`; adapter at `<tmp>/agent/npm/node_modules/…` vs `<tmp>/agent/node_modules/…` vs neither · trigger: probe · observable: ok (npm/) / ok (node_modules/) / absent; `~/.pi/agent` never stat'd (fs spy) (test-plan #E35; see packages/mcp-server-plugin/src/server/__tests__/provisioning.test.ts)
- [x] 10.36 E36 CFG/Adapter verdict cache [BVA (TTL 30s)] — input: fake timers; first probe → ok · trigger: second call at t=29.9s; third at t=30.1s after swapping package.json to 2.0.0 · observable: 2nd: no fs read, ok; 3rd: fs read, below-floor (test-plan #E36; see packages/mcp-server-plugin/src/server/__tests__/provisioning.test.ts)
- [x] 10.37 E37 CFG/Project-scope admission [decision-table] — input: known set `{/k}`; cwd `/k`, `/k/`, `/k/../k`, `/k/../other`, `/other`, `/k/sub` (subdir, not worktree), worktree `/wt` whose git main checkout is `/k`, symlink `/lnk→/k` · trigger: project write · observable: admitted: `/k`, `/k/`, `/k/../k`, `/wt`, `/lnk` (canonicalised); refused with no write: `/k/../other`, `/other`; `/k/sub` gets whatever verdict the moved kb `isAllowedCwd` test asserts (parity test: same input table, same verdicts for kb and mcp-client) (test-plan #E37; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.38 E38 CFG/Service `readServerEntry` [EP] — input: port whose `loadMcpConfig` throws · trigger: `readServerEntry("iMCP", global)` · observable: returns raw file entry; loader never called (test-plan #E38; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.39 E39 CFG/Service check/write parity [decision-table (existing entry × fields)] — input: existing `{url:"u"}` + fields `{command}`; existing `"string"` + any; file unparseable; file absent + `{command}`; existing `{command:"old"}` + `{command:"new"}` · trigger: `checkConfigFiles` then `ensureServerEntry` with same args · observable: verdict pairs equal: transport-conflict/transport-conflict; entry-not-object ×2; unparseable ×2; ok/written; ok/written — and `checkConfigFiles` performed 0 writes in every case (test-plan #E39; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.40 E40 CFG/Service ensureAdapterPackage [EP] — input: `settings.json` `{packages:["npm:a"],other:{x:1}}` / `{packages:["npm:pi-mcp-adapter"]}` / `{}` / absent / unparseable · trigger: call · observable: `["npm:a","npm:pi-mcp-adapter"]`+`other` kept / unchanged (no rewrite) / `{packages:[…]}` / file created / refused `unparseable`, byte-identical (test-plan #E40; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.41 E41 CFG/Service ensureServerEntry preserves fields [EP] — input: existing `iMCP` `{command:"old",disabled:true,directTools:["a"],unknown:1}` · trigger: `ensureServerEntry("iMCP",{command:"new"},global)` · observable: `{command:"new",disabled:true,directTools:["a"],unknown:1}` (test-plan #E41; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.42 E42 CFG/HTTP remove returns entry [EP] — input: folder entry `{command:"a",unknownKey:{n:[1]},"weird key":true}` · trigger: `DELETE /servers/x?cwd=K` then `PUT /servers/x {scope:project,cwd:K,set:<returned>}` · observable: DELETE body `removed` deep-equals original; after PUT the file entry deep-equals original (test-plan #E42; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.43 E43 CFG/HTTP patch shape [EP (invalid)] — input: body `{scope:"global",command:"x"}` (whole entry, no `set`) · trigger: PUT · observable: 400; no IO (test-plan #E43; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.44 E44 FLD/Pill text [decision-table (servers × disabled × parseError)] — input: (4,1,no) / (4,0,no) / (0,0,no) / (4,1,yes) / (0,0,yes) · trigger: render `SlotPill` · observable: "4 servers · 1 off" / "4 servers" / "0 servers" (and (1,0,no) → "1 server") / text + error marker whose `aria-label` contains failing path / error marker only (test-plan #E44; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.45 E45 SET/Server list sorted [EP] — input: servers `b`, `A`, `a`, `Z`, `10`, `2` · trigger: render · observable: DOM order `2, 10, a, A, b, Z` (`localeCompare` with `numeric: true`, `sensitivity: "base"`; ties keep input order) (test-plan #E45; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.46 E46 APL/dependsOn matrix [decision-table (mcp-client × apple-tools enabled)] — input: (on,on) / (off,on) / (on,off) / (off,off) · trigger: loader run · observable: order [mcp-client, apple-tools] and service observed / apple-tools `missingDeps:["mcp-client"]`, not loaded, no throw / only mcp-client loaded / neither (test-plan #E46; see packages/apple-tools/src/__tests__/install.test.ts)
- [x] 10.47 E47 DMS/Provisioning refuse [decision-table (existing `pi-dashboard` shape)] — input: absent / `{url:"old",disabled:true,headers:{A:"b"}}` / `{command:"x"}` / `"str"` / file JSONC-commented / file unparseable / servers under `mcp-servers` · trigger: boot provisioning · observable: created / url+protocolVersion refreshed, `disabled`+`headers` kept / refused, byte-identical, existing message / refused / written, comments dropped, siblings kept / refused / written under `mcp-servers`, no `mcpServers` (test-plan #E47; see packages/mcp-server-plugin/src/server/__tests__/provisioning.test.ts)
- [x] 10.48 E48 CFG/Written permissions [EP] — input: existing file `0600`; existing `0644` · trigger: upsert · observable: resulting mode `0600` in both cases (writer always hardens) (test-plan #E48; see packages/apple-tools/src/__tests__/env-atomic-write.test.ts)
- [x] 10.49 E49 CFG/PI_CODING_AGENT_DIR [EP] — input: `PI_CODING_AGENT_DIR=<tmp>/custom` (real adapter port via dynamic import) · trigger: global write + effective view + verdict · observable: write lands `<tmp>/custom/mcp.json`; view reports that path as Pi global; probe stats `<tmp>/custom/npm/node_modules/pi-mcp-adapter` (test-plan #E49; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.50 P1 CFG/HTTP effective view [tail-latency] — workload: 50 servers × 4 layers, 20 sequential `GET /effective?cwd=K` (worker already warm) · metric: p95 ≤ 200ms · window: 20 calls (test-plan #P1; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.51 P5 CFG/Worker spawn [micro-perf (call count)] — workload: 100 sequential effective-view calls, no timeout · metric: `Worker` constructor called exactly 1× · window: single test (test-plan #P5; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.52 P2 CFG/Adapter verdict cache [micro-perf (call count)] — workload: 1000 `adapterVerdict()` calls within 30s · metric: fs reads == 1 · window: single test (test-plan #P2; see packages/mcp-server-plugin/src/server/__tests__/provisioning.test.ts)
- [x] 10.53 P3 SET/`useEffectiveConfig` dedupe [micro-perf (request count)] — workload: 3 components mount simultaneously for cwd K; 2 for global · metric: fetch calls == 2 (one per key) · window: mount (test-plan #P3; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.54 P4 CFG/Interrupted write [soak / crash-consistency] — workload: 200 iterations: spawn child that writes and is `SIGKILL`ed at a random point after tmp creation · metric: after each: target parses AND equals old or new content; no leftover file matching a predictable name; leftover random tmp count reported · window: 200 iterations (test-plan #P4; see packages/apple-tools/src/__tests__/env-atomic-write.test.ts)
- [x] 10.55 F1 SET/Adapter status single state [state-convergence] — input: mocked `/adapter` verdict `below-floor{installed:2.19.0,floor:2.20.0}` · trigger: render · observable: pill text and banner text both contain `2.19.0` and `2.20.0`; every `switch`, Add, Save `disabled=true`; editor opens with `role=dialog` fields as text (test-plan #F1; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.56 F2 SET/Adapter status [state-transition] — input: verdict `absent` → then `ok` (mock swap + refetch) · trigger: rerender · observable: `absent`: install action present, list rows still rendered read-only; `ok`: in ONE act() flush pill, banner removal and enabled controls all change (no intermediate render with pill=ok but switches disabled) (test-plan #F2; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.57 F3 SET/Toggle [state-transition (optimistic + revert)] — input: Pi-global row enabled; `PUT …/disabled` mocked to reject after 100ms · trigger: click switch · observable: t<100ms: `aria-busy=true`; after reject: switch `aria-checked` back to previous, inline error text on that row only; other rows untouched (test-plan #F3; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.58 F4 SET/Toggle [state-transition (double-click)] — input: slow PUT (500ms) · trigger: click switch twice within 50ms · observable: exactly 1 PUT issued (pending state blocks second) OR 2 PUTs with final state = last response — assert invariant: final `aria-checked` equals server's last persisted value (test-plan #F4; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.59 F5 SET/Editor transport tabs [state-transition] — input: layer entry `{url:"u"}`, effective also has `command` from lower layer · trigger: open editor · observable: initial tab `url`; `command`/`args`/`env`/`socket` inputs absent from DOM; switching to `command` tab hides url fields; Save from `command` tab with `command:"x"` sends `set:{command:"x"}, unset:["url"]` (previous transport's fields unset automatically; only fields the layer entry defined are unset) (test-plan #F5; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.60 F6 SET/Editor validation [EP] — input: command tab, `command` empty · trigger: Save · observable: inline error at `command`; summary list at top with 1 item; 0 PUT calls (test-plan #F6; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.61 F7 SET/Editor unknown fields [EP] — input: entry `{command:"a",unknownField:{n:1}}` · trigger: edit `args`, Save · observable: patch is `set:{args:[…]}` only; `unknownField` never in `unset` (test-plan #F7; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.62 F8 SET/Override shared [EP] — input: shared server, effective `{url:"u",timeout:5}` · trigger: Override, change `timeout`→7, Save · observable: PUT body `{scope:"global",set:{timeout:7}}` — no `url`; provenance after refetch lists Pi global + Shared (test-plan #F8; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.63 F9 SET/Override x-atomic record [BVA (keys 0/1/3)] — input: inherited `env` marker `keys:[{API_KEY,secret},{PATH},{HOME}]` / 1 key / 0 keys · trigger: choose "Override env" · observable: note "3 inherited keys incl. 1 secret will no longer apply" / "1 … 0 secrets" / no note or "0 keys"; record editor starts with 0 rows; Save with 1 row sends `set:{env:{X:"1"}}` only (test-plan #F9; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.64 F10 SET/Secret masking [decision-table (field × own/inherited × pattern)] — input: own `headers.Authorization`, own `env.MY_TOKEN`, own `env.PATH`, inherited `bearerToken` marker · trigger: open editor · observable: first two: `type=password`-equivalent bullets + reveal toggle; `PATH`: plain; inherited: placeholder text, NO reveal toggle, and after Save patch omits `bearerToken` (test-plan #F10; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.65 F11 SET/Secret reveal resets [state-transition] — input: reveal `Authorization` · trigger: close editor, reopen · observable: value masked again (test-plan #F11; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.66 F12 SET/Global settings draft source [state-transition] — input: change one setting · trigger: (a) host `isDirty()`; (b) navigate away; (c) host Save · observable: (a) true; (b) host discard-confirm rendered, Cancel keeps value; (c) `commit` sends `PUT /settings {set:{k:v}}` with exactly 1 key, then `isDirty()` false (test-plan #F12; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.67 F13 SET/Global settings fallback labels [decision-table] — input: key absent everywhere / defined in shared · trigger: render · observable: "default" label + adapter default value / shared layer label + clear button text "use inherited" (test-plan #F13; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.68 F14 SET/No local save button [EP] — input: section rendered · trigger: query · observable: no `button` named Save inside the section root (host Save Bar only) (test-plan #F14; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.69 F15 FLD/Pill loading + not-tracked [state-transition] — input: fetch pending → resolves 403 → session list changes → fetch resolves 200 · trigger: render timeline · observable: pending: placeholder element with same `offsetHeight` as pill; 403: text "not tracked", muted class, 0 further fetches for 2s; after list change: exactly 1 new fetch, pill shows counts (test-plan #F15; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.70 F16 FLD/Remove override undo [state-transition] — input: override chip on `x`; DELETE returns `removed:{disabled:true,unknown:1}` · trigger: click remove → click Undo · observable: PUT `{scope:project,cwd,set:{disabled:true,unknown:1}}` issued; after Undo the chip returns. Just-after-expiry variant: advance fake timers to host toast default + 1ms → toast gone, no Undo control, 0 PUTs (test-plan #F16; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.71 F17 FLD/Folder page not-allowed [EP] — input: route `/folder/<enc(/unknown)>/mcp`, fetch → 403 · trigger: render · observable: not-allowed empty state; 0 server rows; no retry fetch within 2s; no Override/switch controls (test-plan #F17; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.72 F18 FLD/Worktree pill cwd [EP] — input: worktree card cwd `/wt` under folder `/k` · trigger: render pill · observable: fetch URL contains `cwd=%2Fwt`, never `%2Fk` (test-plan #F18; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.73 F19 FLD/Folder enable over global disable [state-convergence] — input: server disabled at Pi global, pill shows "3 servers · 1 off" · trigger: switch on at folder page (mock PUT ok, refetch returns enabled) · observable: PUT `…/disabled {scope:project,cwd,disabled:false}`; pill converges to "3 servers" (test-plan #F19; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.74 F20 FLD/Inherited hints [EP] — input: server in Pi global, not in folder layer · trigger: open row · observable: every field carries "inherited from Pi global" hint; folder override row (defined in `.pi/mcp.json` with `disabled`,`args`) → chip lists exactly `disabled, args` (test-plan #F20; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.75 F26 APL/Missing-dependency banner [state-transition] — input: `/api/plugins` row `apple-tools {enabled:true,loaded:false,missingDeps:["mcp-client"]}` · trigger: render apple-tools section · observable: banner text contains `mcp-client`, link href to plugins index; no "Run installer" button; with `loaded:true` → no banner, installer offered (macOS + app present) (test-plan #F26; see packages/apple-tools/src/__tests__/install.test.ts)
- [x] 10.76 F27 APL/Settings surface trimmed [EP] — input: apple-tools section on macOS provisioned host · trigger: render · observable: no enable/disable switch; no direct-tools control; link href `/settings/plugins/mcp-client` (test-plan #F27; see packages/apple-tools/src/__tests__/install.test.ts)
- [x] 10.77 F28 SET/Editor field renderer coverage [type-coverage] — input: every `ServerEntry` + `McpSettings` schema field · trigger: render editor with all fields populated · observable: each field has a widget or the JSON-fallback editor; none missing from DOM (assert count == schema property count) (test-plan #F28; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.78 F31 SET/Plugin settings group [state-transition] — input: `usePluginConfig` → `{adapterLoadTimeoutMs:10000}` · trigger: set `2500`, host Save · observable: `plugin_config_write {id:"mcp-client",config:{adapterLoadTimeoutMs:2500}}` issued; 0 `PUT /settings`; then `isDirty()` false (test-plan #F31; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.79 F32 SET/Plugin settings group [BVA (1000/120000)] — input: enter `999`, `1000`, `120000`, `120001` · trigger: blur · observable: inline range error shown / none / none / shown; with an error present the draft source's `commit` is not callable by the host Save (source reports invalid) (test-plan #F32; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.80 F33 SET/Plugin settings group [fault-injection (partial commit)] — input: timeout → `2500` AND adapter setting → `k:v`; `PUT /settings` mocked 409 · trigger: host Save · observable: `plugin_config_write` issued; `PUT /settings` issued; `isDirty()` still true; error text rendered inside the adapter settings group only (test-plan #F33; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.81 F34 SET/504 list state [fault-injection] — input: `GET /effective` → 504 `{timeoutMs:1000}` · trigger: render · observable: list error state text contains `1000`; retry button issues exactly 1 fetch; a link/anchor targets the timeout field (focus moves to it) (test-plan #F34; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.82 F35 FLD/504 on pill + page [fault-injection] — input: folder view fetch → 504 · trigger: render pill; render page · observable: pill error marker `aria-label` contains timeout; page error state with retry issuing exactly 1 request; no stale rows (test-plan #F35; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.83 X1 CFG/IO failure write-failed [fault-injection (abort)] — input: ConfigIO `writeFile` rejects `EACCES` / `ENOSPC` / `mkdir` rejects `EACCES` · trigger: upsert · observable: result `write-failed` with `code` = injected code; target byte-identical; no tmp file left in target dir (test-plan #X1; see packages/apple-tools/src/__tests__/env-atomic-write.test.ts)
- [x] 10.84 X2 CFG/IO failure mid-sequence [fault-injection (abort at rename)] — input: `rename` rejects `EXDEV` · trigger: upsert · observable: `write-failed{code:EXDEV}`; target byte-identical; tmp file removed (cleanup attempted) (test-plan #X2; see packages/apple-tools/src/__tests__/env-atomic-write.test.ts)
- [x] 10.85 X3 CFG/Unparseable target [EP] — input: target `{ "mcpServers": ` (truncated) · trigger: upsert / disable / settings patch / `ensureAdapterPackage` on truncated `settings.json` · observable: each refused `unparseable` naming path + parser message; byte-identical; HTTP 409 (test-plan #X3; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.86 X4 CFG/Unparseable layer in view [fault-injection] — input: `.mcp.json` unparseable, `.pi/mcp.json` + global valid · trigger: effective view for cwd · observable: 200; `layerErrors:[{path:<cwd>/.mcp.json,message}]`; servers from the two valid layers present; server only in broken layer absent (test-plan #X4; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.87 X5 CFG/Network guard [fault-injection (guard refuses)] — input: `networkGuard` preHandler stub replies 403 · trigger: `GET /effective`, `GET /schema`, `GET /adapter`, `PUT /servers/x`, `DELETE`, `PUT …/disabled`, `PUT /settings` · observable: each: 403 body from guard; ConfigIO spy 0 reads/0 writes; adapter port 0 calls (test-plan #X5; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.88 X6 CFG/Auth [fault-injection] — input: request without plugin auth · trigger: every route · observable: same rejection status other plugin routes give; no IO (test-plan #X6; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.89 X7 CFG/Adapter port throws during read [fault-injection (abort)] — input: port `loadMcpConfig` throws `Error("boom")` · trigger: `GET /effective` · observable: 500 with sanitised message (no stack, no file contents); process alive; subsequent request works (test-plan #X7; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.90 X8 CFG/Adapter timeout (D10) [fault-injection (delay)] — input: worker script that busy-loops (sync) forever; `adapterLoadTimeoutMs: 1000` · trigger: `GET /effective` · observable: 504 `{error:"adapter-timeout",timeoutMs:1000}` at t≈1000ms (±200); a `setTimeout(…,500)` scheduled at t=0 fires by t=700 (event loop free); `worker.terminate` called 1×; next `GET /effective` with a sane worker → 200 and `Worker` constructor count now 2 (test-plan #X8; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.91 X8b CFG/Adapter timeout BVA [BVA (config range)] — input: plugin-config writes `999`, `1000`, `120000`, `120001`, `"10s"`, `10000.5` · trigger: `POST /api/config/plugins/mcp-client` · observable: 400 / 200 / 200 / 400 / 400 / 400; after each 400 the next request's port call still receives the previous accepted value (test-plan #X8b; see packages/kb-plugin/src/server/__tests__/kb-routes.test.ts)
- [x] 10.92 X8c CFG/Enable merge times out [fault-injection (delay)] — input: entry `{command:"a",disabled:true}`; merge load hangs · trigger: enable at global · observable: 504; file entry is `{command:"a"}` (removal landed, no `disabled:false`); exactly 1 write (test-plan #X8c; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.93 X8d CFG/Write-only ops never spawn worker [EP] — input: factory with default port · trigger: `ensureServerEntry`, `checkConfigFiles`, `ensureAdapterPackage`, `readServerEntry`, `setDirectTools` · observable: `Worker` constructor spy count 0 (test-plan #X8d; see packages/apple-tools/src/__tests__/mcp-config.test.ts)
- [x] 10.94 X9 SET/Verdict after install [fault-injection (state change)] — input: verdict `absent` cached; then adapter package appears on disk · trigger: install action completes · observable: client issues `GET /adapter?fresh=1` (or equivalent bust) immediately; server re-probes (fs read count +1 despite TTL); pill/banner/readOnly flip to `ok` in one render (test-plan #X9; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.95 X10 CFG/Service disabled [fault-injection (dependency absent)] — input: `mcp-client` disabled · trigger: loader · observable: `ctx.consume("mcp-client.config")` from another plugin → `undefined`; apple-tools not loaded; index `missingDeps` (test-plan #X10; see packages/dashboard-plugin-runtime/src/__tests__/dependency-graph.test.ts)
- [x] 10.96 X11 APL/Consume undefined [fault-injection] — input: apple-tools registered without mcp-client (loader bypassed in test) · trigger: `registerPlugin` · observable: throws with message naming `mcp-client.config`; no partial route registration (test-plan #X11; see packages/apple-tools/src/__tests__/install.test.ts)
- [x] 10.97 X12 APL/write-failed mapping [fault-injection] — input: stubbed service `ensureServerEntry` → `write-failed{code:ENOSPC}` · trigger: installer write run · observable: terminal state `CONFIG_WRITE_FAILED`; message contains `ENOSPC`; exit non-zero (test-plan #X12; see packages/apple-tools/src/__tests__/install.test.ts)
- [x] 10.98 X13 APL/Other refusals mapping [decision-table] — input: service → `unparseable` / `entry-not-object` / `transport-conflict` / `invalid-name` · trigger: write run · observable: each → `CONFIG_UNPARSEABLE`; the closed 9-state enum has no new member (type test) (test-plan #X13; see packages/apple-tools/src/__tests__/install.test.ts)
- [x] 10.99 X14 APL/Check-mode parity [EP] — input: existing `iMCP {url:"u"}` · trigger: `--check` then write run · observable: both `CONFIG_UNPARSEABLE` with transport-conflict message; write run performed 0 writes (test-plan #X14; see packages/apple-tools/src/__tests__/install.test.ts)
- [x] 10.100 X15 APL/CLI hostless [fault-injection (no host)] — input: run `bin/install.ts` entry with no dashboard ctx, `PI_CODING_AGENT_DIR=<tmp>`, fake `imcp-server` binary · trigger: write run · observable: `<tmp>/mcp.json` gains `iMCP.command`; `<tmp>/settings.json` packages gains `npm:pi-mcp-adapter`; `grep -r "mcpServers" packages/apple-tools/src` finds no writer (test-plan #X15; see packages/apple-tools/src/__tests__/install.test.ts)
- [x] 10.101 X16 DMS/Verdict degrade [fault-injection] — input: mcp-client disabled · trigger: provisioning surface reads adapter state · observable: `{kind:"unknown",floor:"2.20.0"}`; `/mcp` route still 200 (test-plan #X16; see packages/mcp-server-plugin/src/server/__tests__/provisioning.test.ts)
- [x] 10.102 X17 DMS/Warn once on first /mcp [state-transition] — input: verdict `below-floor` · trigger: register plugin; then 3 requests to `/mcp` · observable: logger.warn count: 0 after register; 1 after first request; still 1 after third (test-plan #X17; see packages/mcp-server-plugin/src/server/__tests__/provisioning.test.ts)
- [x] 10.103 X18 DMS/Boot provisioning refuse [fault-injection] — input: global file unparseable · trigger: server boot · observable: provisioning error surfaced in log; file byte-identical; server still starts (test-plan #X18; see packages/mcp-server-plugin/src/server/__tests__/provisioning.test.ts)
- [x] 10.104 X19 SET/Effective fetch fails [fault-injection] — input: `GET /effective` → 500 · trigger: render section · observable: error state with retry action; no skeleton stuck; retry issues a new fetch (test-plan #X19; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.105 X20 SET/Save fails [fault-injection] — input: editor Save → PUT 409 unparseable · trigger: Save · observable: editor stays open; summary shows server message with path; fields keep operator values (test-plan #X20; see packages/kb-plugin/src/client/__tests__/KbSettings.test.tsx)
- [x] 10.106 X21 FLD/Undo fails [fault-injection] — input: Undo PUT → 500 · trigger: click Undo · observable: error toast containing the removed entry as copyable JSON (`<pre>`/`<code>` text deep-equals the DELETE `removed` payload); chip stays absent (test-plan #X21; see packages/kb-plugin/src/client/__tests__/FolderKbSection.test.tsx)
- [x] 10.107 X22 CFG/Concurrent writes [race] — input: two `ensureServerEntry` for different servers on the same file started in the same tick · trigger: await both · observable: both results ok; final file contains both entries; ConfigIO write calls for that path did not overlap (second `writeFileAtomic` starts after first resolves — spy timestamps) (test-plan #X22; see packages/apple-tools/src/__tests__/mcp-config.test.ts)

### 10.2 Browser e2e (Playwright vs docker harness, port from `.pi-test-harness.json`)

- [x] 10.108 F21 SET+FLD/Mobile [BVA (viewport 639/640)] — input: `/settings/plugins/mcp-client` at 390×844 and at 639px, 640px · trigger: open editor · observable: ≤639: editor is bottom sheet (bounding box bottom == viewport bottom); 640: modal (centered); every `button`/`switch`/row action bounding box ≥44×44 at 390px (test-plan #F21; see tests/e2e/gateway-board-mobile.spec.ts)
- [x] 10.109 F22 FLD/Mobile chips [BVA] — input: folder page at 390px with an override chip · trigger: inspect chip; open row editor · observable: chip has no remove `button`; sheet contains "Remove override" (test-plan #F22; see tests/e2e/gateway-board-mobile.spec.ts)
- [x] 10.110 F23 SET/Keyboard-only [state-transition (focus order)] — input: settings page, editor open · trigger: Tab through · observable: focus visits: every transport tab, every field, Advanced disclosure, Save, Close — in DOM order; each focused element has visible focus ring (computed outline/box-shadow ≠ none); Escape closes editor (test-plan #F23; see tests/e2e/gateway-board-mobile.spec.ts)
- [x] 10.111 F24 SET/Accessibility floor [axe] — input: settings page (list + editor open), folder page · trigger: `axe.run` · observable: 0 violations at `serious`/`critical`; contrast checks pass in all 4 themes (test-plan #F24; see tests/e2e/gateway-board-mobile.spec.ts)
- [x] 10.112 F25 SET/Section renders once [EP] — input: navigate via plugin row settings affordance · trigger: URL + DOM · observable: URL `/settings/plugins/mcp-client`; exactly 1 element with the section `data-testid` (test-plan #F25; see tests/e2e/apple-tools-activation.spec.ts)
- [x] 10.113 F29 FLD/Back [state-transition] — input: open folder page from sidebar pill (previous view: sessions) · trigger: click Back · observable: shell shows the sessions view, URL restored to pre-pill URL (test-plan #F29; see tests/e2e/apple-tools-activation.spec.ts)
- [x] 10.114 X23 E2E/harness integration [end-to-end] — input: docker harness, both plugins enabled · trigger: toggle iMCP off on MCP page; run installer; then disable `mcp-client` + restart · observable: harness `~/.pi/agent/mcp.json` `iMCP` has `disabled:true` AND `command`; plugins index shows apple-tools `missingDeps`; apple-tools panel has no Run installer (test-plan #X23; see tests/e2e/apple-tools-activation.spec.ts)

### 10.3 Electron bundle

- [x] 10.115 E50 CFG/Production bundling [EP] — input: `BUNDLED_PLUGINS` list · trigger: bundle-completeness test · observable: contains `mcp-client-plugin`; built client bundle exports the settings section + pill claims (test-plan #E50; see packages/electron/scripts/assert-bundled-plugins-complete.mjs)

### 10.4 Manual-only (deferred post-merge by ship-change)

- [x] 10.116 F30 mockups visual parity — manual check after merge: live settings + folder pages vs `mockups/*.html` · human compares · [judgment: layout/spacing "matches mockup" — no automatable observable] (test-plan: manual-only)
