# Tasks — widen-containment-to-resolved-checkout

Design gate (design.md D1–D6, `## Review`) is done at planning time. TDD
throughout: write the failing test, run it red, implement, run it green. Test
scenarios come from `test-plan.md` (manifest ids in parentheses; disposition
lives there, not here). Test command:
`set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log; grep -nE 'FAIL|✗|Tests +[0-9]+ (failed|passed)' /tmp/pi-test.log`.

Exemplars: resolver tests → `packages/shared/src/platform/__tests__/git-checkout-roots.test.ts`;
containment helper → `packages/server/src/lib/__tests__/path-containment.test.ts`;
route-level → `packages/server/src/__tests__/file-absolute-containment.test.ts`;
kb guard → `packages/kb-plugin/src/server/__tests__/kb-routes.test.ts` (existing implausible-main-checkout test).

## 1. Design gate

- [x] 1.1 `doubt-driven-review` against design.md — 3 cycles, findings folded, notes under design.md `## Review`. Verify: section present.
- [x] 1.2 Run `security-hardening` on the widening: enumerate what becomes readable per state (D1 table) against `buildGitFixtures()` on disk — for each fixture, list `cwd`, expected anchors, and one path that MUST stay unreadable. Verify: the list becomes the parameter table for tasks 4.2–4.7.
- [x] 1.3 Measure the bounded-reach argument (test-plan E29; see kb-routes.test.ts): admitted known cwd whose `knowledge_base.json` names an absolute source dir outside the cwd and an absolute `dbPath` outside the cwd · `POST /api/kb/reindex` · the outside dir's file appears in the index and `<outside>/kb.db` exists on disk. Verify: test passes (proving reach is NOT bounded); docblock states this is the premise for D6.

## 2. Shared resolver — `packages/shared/src/platform/git.ts`

- [x] 2.1 Test `commonDir` per fixture state (test-plan E1; see git-checkout-roots.test.ts): each of the nine fixtures · `checkoutRoots({cwd})` · `commonDir` = normal→`<normal>/.git`, submodule→`<super>/.git/modules/models/sub`, bareWorktree→hub dir, separateGitDir→`elsewhere.git`; other fields unchanged. Verify: RED (field absent).
- [x] 2.2 Implement `commonDir: string` on `GitCheckoutRoots`, populated from the normalized `--git-common-dir` in `resolveCheckoutRootsFrom`; extract `isLinked(gitDirRaw, commonDirRaw)` and `wantsBareness(commonDir)` predicates from the core (design D4). Verify: 2.1 green; TypeScript compiles across consumers (`vcs-info.ts`, `kb-routes.ts`).
- [x] 2.3 Test sync/async parity over states (test-plan E2; see git-checkout-roots.test.ts): each fixture + nonRepo · `await checkoutRootsAsync({cwd})` vs `checkoutRoots({cwd})` · `toEqual`; nonRepo → both `null`. Verify: RED (function absent).
- [x] 2.4 Test degraded-probe parity (test-plan X3; see the injected-probe tests in git-checkout-roots.test.ts): injected probes where `--show-toplevel` throws, and separately where `core.bare` throws · run sync core and async wiring over identical probes · identical results, bareness `"unknown"` never `"not-bare"`. Verify: RED.
- [x] 2.5 Test async timeout (test-plan X4; see git-checkout-roots.test.ts + the slow-git shim from test-plan "New infra"): slow `git` shim on PATH · `checkoutRootsAsync({cwd, timeout: 1})` · `null`, no partial result. Verify: RED.
- [x] 2.6 Implement `checkoutRootsAsync({cwd, timeout?})`: two-phase `runAsync` probes memoized into thunks fed to `resolveCheckoutRootsFrom`; phase 2 gated by the shared `isLinked`/`wantsBareness` predicates; an un-run thunk throws (design D4). Verify: 2.3–2.5 green.
- [x] 2.7 Test binding positive controls (test-plan E3, E4, E5; see git-checkout-roots.test.ts): `worktree` fixture with no `core.worktree` · `isBoundCheckout(roots.mainCheckout, roots.commonDir)` · `true` (E3); `submoduleWorktree` · bind `mainCheckout` (= submodule checkout) · `true` (E4); two linked worktrees of `normal` built inline, `core.worktree` on the common dir = the other worktree · bind resolved `mainCheckout` from worktree A · `true` (E5). Async twin asserted alongside. Verify: RED (function absent).
- [x] 2.8 Test binding rejects unrelated/outside/nonexistent (test-plan E6, E7, E8; see git-checkout-roots.test.ts): worktree of repo A with `core.worktree` = `<repoB>` · bind · `false` (E6); `core.worktree` = fresh tmp dir, and = `<tmp>/does-not-exist` · bind · `false` both (E7); `core.worktree` = a separate ordinary repo that CONTAINS the worktree's parent dir · bind · `false` (E8). Verify: RED.
- [x] 2.9 Test binding rejects git-internal path (test-plan E9; see git-checkout-roots.test.ts): `core.worktree` = `<repo>/.git/x` (dir created) · bind · `false` via `.git`-segment test, AND `false` when the inner re-resolve is called directly with the segment test bypassed. Verify: RED.
- [x] 2.10 Test subdirectory of true main is MEASURED (test-plan E10; see git-checkout-roots.test.ts): `core.worktree` = `<main>/sub/dir` · bind · either `false`, OR `true` with the re-resolved `thisCheckout` samePath `<main>/sub/dir`; assert NEVER `<main>`. Verify: RED; record the measured branch in the test name.
- [x] 2.11 Test candidate realpath (test-plan E11; see git-checkout-roots.test.ts): symlink `<tmp>/link` → honest main checkout; candidate = the symlink path · bind · `true`, compare on realpaths. Skip on win32 if symlink creation unavailable. Verify: RED.
- [x] 2.12 Test binding fails closed (test-plan X5; slow-git shim + PATH without git): binding with `timeout: 1` on the slow shim, and with `git` absent from PATH · `isBoundCheckout` and async twin · `false` both. Verify: RED.
- [x] 2.13 Implement `isBoundCheckout(candidate, commonDir, {timeout?})` and `isBoundCheckoutAsync` (design D2: realpath candidate → no `.git` segment → re-resolve → same `commonDir` → re-resolved `thisCheckout` samePath candidate; any failure → false). Verify: 2.7–2.12 green.
- [x] 2.14 Update `packages/shared/src/platform/git.ts.AGENTS.md` "Checkout-root resolution" section: `commonDir` (identity-only, never an anchor), `checkoutRootsAsync`, `isBoundCheckout*`, `See change: widen-containment-to-resolved-checkout`. Verify: `kb dox lint` reports no stale row for `git.ts`.

## 3. kb-plugin cwd guard — `packages/kb-plugin/src/server/kb-routes.ts`

- [x] 3.1 Test unrelated known folder via `core.worktree` (test-plan E27; see the existing implausible-main-checkout test in kb-routes.test.ts): known = `<repoB>` (ordinary checkout); linked worktree of UNKNOWN repo A with `core.worktree` set `--local` on its common dir to `<repoB>` · `GET /api/kb/stats?cwd=<worktreeA>` · `403`, store-open spy = 0. Verify: RED on current code (currently admits).
- [x] 3.2 Test honest positive controls (test-plan E28; see kb-routes.test.ts): known = `<normal>`, cwd = its worktree; known = `<sub>`, cwd = submoduleWorktree; known = `/repo`, cwd = `/repo/src` · inject · all `200`; the existing `.git`-segment implausible-main test still `403`. Verify: green before AND after 3.4.
- [x] 3.3 Test the 2 s synchronous ceiling (test-plan P1; slow-git shim sleeping 5 s per call): worktree cwd with `core.worktree` set so binding runs · wall-time `isAllowedCwd` · ≤ 2.5 s and result = reject. Verify: RED (current 400 ms × probes exceeds it once binding lands, or the shim proves the pre-existing 2 s).
- [x] 3.4 Implement: `mainCheckoutPath` returns `null` unless `isBoundCheckout(main, roots.commonDir, {timeout: 200})`; resolver call drops to `timeout: 200` so the 10-probe worst case holds the file's documented 2 s ceiling (design D4); update the budget comment. Verify: 3.1–3.3 green; test-plan X8 (bare-hub worktree not known → `403`) still green.
- [x] 3.5 Update `packages/kb-plugin/src/server/AGENTS.md` row for `kb-routes.ts` (binding rule, 200 ms budget, `See change:`). Verify: `kb dox lint` clean for that file.

## 4. File-read containment — `packages/server/src/lib/path-containment.ts`

- [x] 4.1 Rebase `path-containment.test.ts` on `buildGitFixtures()` keeping the existing separator/drive-case assertion (test-plan E30; see path-containment.test.ts): anchor `C:/repo` forward-slash, resolved `C:\repo\x` with injected win32 helpers · compare · contained `true`. Verify: still green.
- [x] 4.2 Test cwd fast path and no-spawn (test-plan E12, P2; see path-containment.test.ts): resolved under cwd · `isAllowed(resolved, {anchors:[cwd]})` · `true` and `checkoutRootsAsync` spy = 0 calls (E12); 100 in-cwd reads · spy = 0 (P2). Verify: green before and after.
- [x] 4.3 Test own-checkout root from a subdir cwd (test-plan E13, E14; see path-containment.test.ts): `it.each` cwd = subdir of normal / submodule / bareWorktree / separateGitDir / dotGitNamedCheckout, resolved = `<checkout>/README.md` · `isAllowed` · `true` (E13); cwd = `worktree` subdir, resolved = `<normal>/node_modules/x/package.json` · `true` (E14). Verify: submodule, bareWorktree, separateGitDir RED on current code; normal, dotGitNamedCheckout, E14 green already.
- [x] 4.4 Test the "never" cells from 1.2 (test-plan E15–E18; see path-containment.test.ts): submodule subdir → `<super>/.env` · `false` (E15); submoduleWorktree → `<super>/models/sub/README.md` `true` and `<super>/.git/modules/models/x` `false` (E16); separateGitDir subdir → `<elsewhere>/x` · `false` (E17); bareWorktree subdir → `<hubs>/x` · `false` (E18). Verify: rejections green before AND after; E16's allow is RED before.
- [x] 4.5 Test bare cwd and outside-everything (test-plan E19, E21; see path-containment.test.ts): cwd = bare, resolved = `<bare-parent>/x` · `false` (E19); any state, resolved = `/etc/passwd` (`C:\Windows\win.ini` on win32) · `false` (E21). Verify: green before and after.
- [x] 4.6 Test unbound `core.worktree` does not widen (test-plan E20; see path-containment.test.ts): worktree cwd with `core.worktree` = `/` and = an unrelated checkout; resolved = a file under that dir outside the repo · `isAllowed` · `false` both. Verify: green before (basename guard) and after (binding).
- [x] 4.7 Test dedupe before binding (test-plan E22; see path-containment.test.ts): normal cwd · `checkoutAnchors(cwd)` with spy on `isBoundCheckoutAsync` · called once, result length 1. Verify: RED (function absent).
- [x] 4.8 Test fail-closed on timeout / missing git / responsiveness (test-plan X1, X2, X6; slow-git shim; see path-containment.test.ts): slow shim, timeout 200 ms, out-of-cwd path · `isAllowed` · `false` in < 1.5 s (X1); slow shim, timeout 2 s · start `isAllowed(out-of-cwd)` then immediately `GET /api/health` via inject · health resolves < 200 ms while containment pending (X2); PATH without git, worktree cwd · `isAllowed(<main>/file)` · `false` (X6). Verify: X6 green before; X1/X2 RED until the async wiring's timeout is honoured.
- [x] 4.9 Test symlink escape (test-plan X7; see path-containment.test.ts): symlink `<checkout>/esc` → `<tmp>/outside`, cwd = `<checkout>/sub` · `isAllowed(<checkout>/esc/file)` · `false` (layer-2 realpath). In-cwd symlink NOT asserted (pre-existing layer-1 allow). Verify: green before and after.
- [x] 4.10 Implement `checkoutAnchors(anchor): Promise<string[]>` via `checkoutRootsAsync` + `isBoundCheckoutAsync`, deduplicated by samePath before binding (design D5); keep `isAllowed`'s per-anchor layer-2 loop — for EACH layer-1 anchor compute its checkout roots, skipping a root samePath that anchor; delete `gitRoot`, the local `execFile` probe, and the `basename === ".git"` guard; update the header's "Known limitations" block. Verify: 4.1–4.9 fully green; `rg "gitRoot|basename\(normalized\)" packages/server/src` returns nothing.
- [x] 4.11 Route-level test: submodule session own checkout vs superproject (test-plan E24; see file-absolute-containment.test.ts): session cwd = submodule subdir · `GET /api/file?path=<sub>/README.md` and `?path=<super>/.env` · `200` / `403 {success:false,error:"path outside working directory"}`. Verify: allow RED before 4.10, green after.
- [x] 4.12 Route-level test: per-anchor widening preserved for pinned dir (test-plan E23; see file-absolute-containment.test.ts): pinned dir = `<normal>/packages/x`, no session cwd there · `GET /api/file/exists?path=<normal>/README.md` · `200`. Verify: green before and after (today's per-anchor `gitRoot` already allows it).
- [x] 4.13 Route-level regression: per-site strings and `~/.pi` anchor unchanged (test-plan E25, E26; see file-absolute-containment.test.ts): `exists` outside all anchors · `"path outside cwd"`; `tree` outside cwd · `"path outside working directory"` (E25); fake HOME, path under `~/.pi` outside cwd · read, raw, render `200`; `exists` same path `403` (E26). Verify: green before and after.
- [x] 4.14 Update `packages/server/src/lib/AGENTS.md` row for `path-containment.ts` (per-anchor bound checkout roots, async resolver, no `system-routes` consumer — the row's current claim is stale, `See change:`). Verify: `kb dox lint` clean.

## 5. Review, docs, verification

- [x] 5.1 `review-code` pass over the full diff (`git diff develop...HEAD`), fix findings. Verify: no Major/Critical findings open; `npm run quality:changed` clean.
- [x] 5.2 Delegate to `DocScribe`: update `docs/architecture.md` file-containment paragraph (anchor derivation, binding rule, unbounded-reach note for the kb guard) in caveman style. Verify: section present, `kb dox lint` clean for `docs/`.
- [ ] 5.3 Full suite + restart + reload: `npm test` green; `curl -X POST http://localhost:8000/api/restart`; `npm run reload` (the extension's `vcs-info.ts` shares `git.ts`); `openspec validate widen-containment-to-resolved-checkout --strict` passes. Verify: all three.
- [x] 5.4 Coordination with `add-access-grants-and-review` (same `file-read-containment` requirement): before archiving, diff this delta against the current main spec; if the sibling landed first, re-derive the delta on top of its text (keep the measured nine-site enumeration of this delta — the sibling repeats a phantom "preview" site — and the "per-site anchor sets unchanged" sentence). Verify: `openspec validate --strict` passes and the archived main spec contains both the layer-3 text (if landed) and the checkout-root derivation.
- [ ] 5.5 Manual live smoke (test-plan: manual-only, F1): from a real dashboard session whose cwd is a submodule subdir, open a file at the submodule root in the file viewer, then a superproject file. Verify: viewer shows the first; shows the 403 error state for the second. Deferred post-merge by ship-change.
