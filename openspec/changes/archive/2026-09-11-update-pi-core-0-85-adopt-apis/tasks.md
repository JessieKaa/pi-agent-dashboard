## 1. Pin bump (atomic — land as one commit)

- [x] 1.1 Bump `packages/server/package.json` `dependencies["@earendil-works/pi-coding-agent"]` from `^0.84.4` to `^0.85.1`; verify `node -p "require('./packages/server/package.json').dependencies['@earendil-works/pi-coding-agent']"` prints `^0.85.1`
- [x] 1.2 Set `packages/server/package.json` `piCompatibility.recommended` to `"0.85.1"` **and** `piCompatibility.minimum` to `"0.85.1"` (lockstep — design §2); leave `maximum` `null`
- [x] 1.3 Bump the `docker/Dockerfile:116` global install to `@earendil-works/pi-coding-agent@0.85.1`; verify `grep -n "pi-coding-agent@0.85.1" docker/Dockerfile` matches exactly one line
- [x] 1.4 Bump `scripts/verify-release-deps.mjs:67` `minVersion` to `"0.85.1"` and update its adjacent evidence note to cite 0.85.1 (fable 5.1 UA gate + zero breaking changes)
- [x] 1.5 Bump `pnpm-workspace.yaml:47` `overrides["@earendil-works/pi-coding-agent"]` from `0.84.4` to `0.85.1` and refresh its explanatory comment block (which cites the 0.84 ghost-version defect); verify `grep -n "pi-coding-agent\": 0.85.1" pnpm-workspace.yaml` matches
- [x] 1.6 Extend the pi-coherence gate in `scripts/verify-release-deps.mjs` (`checkPiPinCoherence`, ~line 143-180) to also compare `piCompatibility.minimum` **and** the `pnpm-workspace.yaml` override against the other pins; verify it fails naming each drifted location when minimum is temporarily reverted to `0.78.0` and when the override is temporarily reverted to `0.84.4`, then restore both
- [x] 1.7 Update BOTH `checkPiPinCoherence` fixtures in `packages/server/src/__tests__/pi-version-skew.test.ts` — the E4 drift fixture (~`:59-70`) and the E4 stale-`minVersion` fixture (~`:79-90`), each of which supplies only `dependencies` + `piCompatibility.recommended` — to also supply `piCompatibility.minimum` (and the override argument, if 1.6 changes the signature); verify each still fails for its ORIGINAL reason (`/pi pin drift/i`, stale `minVersion`) and not a new "missing pin" error, so neither test's intent silently degrades
- [x] 1.8 Re-resolve the lockfile with `pnpm install` (NEVER `npm install` — `pnpm-workspace.yaml` sets `nodeLinker: hoisted`); verify `node -p "require('@earendil-works/pi-coding-agent/package.json').version"` prints `0.85.1`
- [x] 1.9 Verify the tree resolves exactly ONE pi copy after the override bump: `find node_modules packages/*/node_modules -name package.json -path "*pi-coding-agent*" -maxdepth 4 -not -path "*/node_modules/*/node_modules/*" -exec node -p "require('{}').version" \;` reports only `0.85.1`
- [x] 1.10 Run `node scripts/verify-release-deps.mjs` and verify it exits 0 with the pi coherence check passing

## 2. Version-skew and floor surfaces

- [x] 2.1 Update `packages/server/src/__tests__/pi-version-skew.test.ts` `PINNED_PI` and the E1/E3 four-surface assertions for pin `0.85.1` **and** floor `0.85.1`; keep the literals literal (design §6 — this test asserts coherence, so deriving would make it a tautology)
- [x] 2.2 Add a test asserting a `0.84.4` runtime now produces a hard `error` (not merely `upgradeRecommended`) and that the message names `0.85.1`; verify it fails before the floor bump and passes after
- [x] 2.3 Verify `packages/server/src/pi/pi-version-skew.ts` needs no code change — the floor is read from `piCompatibility.minimum`, not hardcoded; record the finding rather than editing
- [x] 2.4 Verify the `PiVersionAdvisory` red-panel copy names the required version; if it renders a generic "below minimum" string, make it name `0.85.1` from the compatibility payload
- [x] 2.5 Verify `GET /api/pi/installs` still enumerates a `0.84.x` candidate (not omits it), flags it as failing the floor, and reports it unselectable; add/extend a test covering the previously-supported-now-below-floor case
- [x] 2.6 Verify an unknown-version candidate is still NOT floor-failed and remains selectable after the raise (existing `pi-runtime-selection` guarantee must survive)

## 3. Retire the agent_settled version gate

- [x] 3.1 Delete `packages/extension/src/agent-settled.ts` — all nine exports (`NATIVE_AGENT_SETTLED_FLOOR`, `FLOOR_RETRY_UNKNOWN_DELAY_MS`, `FLOOR_RETRY_GRACE_MS`, `floorRetryReconcileDelay`, `nativeAgentSettledSupported`, `interface SettleEvent`, `synthesizeAgentSettledEvent`, `markFloorSettle`, `settleFollowUp`) plus the private `parseVersion()` — design §3, wholesale removal not a stubbed gate
- [x] 3.2 Remove ALL THREE `bridge.ts` consumption sites (design §3 table): the import at `:24`, the `piEmitsNativeSettled` activation read at `:354-357`, and the floor-retry reconciliation machine — `floorRetryWaiting` at `:2097`/`:2152` and the `if (synthSettle)` block at `:2518-2557` including its `markFloorSettle` send and `setTimeout(… floorRetryReconcileDelay)`. Keep the `else` branch's `abortLatch.clear` / `retryTracker.observeAgentSettled` behavior reachable via the real `agent_settled` handler at `:2123-2136`. Verify `grep -rn "agent-settled\|piEmitsNativeSettled\|NATIVE_AGENT_SETTLED\|floorRetry\|markFloorSettle\|settleFollowUp" packages/*/src` returns no hits
- [x] 3.2c Update `packages/extension/src/__tests__/retry-tracker.test.ts:2`, which imports `settleFollowUp` from the deleted module — a second consumer outside `bridge.ts`. Drop the synth-coupled cases and keep the `RetryTracker` assertions that stand on their own; verify the file compiles and its remaining cases still cover the native `agent_settled` retry-close path
- [x] 3.2d Retire the now-unreachable client fallback: `packages/client/src/lib/chat/event-reducer.ts:1442` branches on `data.retryPending === true`, whose ONLY producer was `markFloorSettle`. Remove the branch and the two cases at `packages/client/src/lib/__tests__/event-reducer-agent-settled.test.ts:94,110`; verify `grep -rn "retryPending" packages/` returns no hits. (Required by this change's own `pi-api-feature-detection` delta — an unreachable fallback is removed in the same change.)
- [x] 3.2b Verify no retry-lifecycle regression: the retry chain must still close with `auto_retry_end` on the native `agent_settled`, and an aborted turn must still clear `abortLatch` — run the existing retry/abort bridge tests and confirm they cover both paths without the synth block
- [x] 3.3 Delete `packages/extension/src/__tests__/agent-settled.test.ts`, but first move any case asserting the surviving contract — exactly one terminal `agent_settled` per run, `isAgentStreaming=false` — into the bridge's own test so the contract stays covered (design § Risks)
- [x] 3.4 Run `npm test` and verify no orphaned import or type error remains from the deletion

## 4. Stale comment corrections (behavior-free)

- [x] 4.1 Correct the "inert below floor" comments for `session_compact_failed` (≥0.84.3) and `ui_prompt_start`/`ui_prompt_end` (≥0.84.4) in `packages/extension/src/bridge.ts` `passThroughEventTypes` and `packages/server/src/session/event-status-extraction.ts`
- [x] 4.2 Verify the diff for this group is comment-only: `git diff -U0` on the two files shows no changed executable line, and the subscription list is byte-identical (design §4)

## 5. Upstream adoption audits

- [x] 5.1 Audit `ask-user-tool.ts`, `canvas-tool.ts`, `role-model-tools.ts` for the `ctx.cwd`-ignored defect class; for each tool, either resolve relative paths / spawn cwd against `ctx.cwd` or record it as cwd-independent with the reason
- [x] 5.2 Add a test for any tool changed in 5.1 asserting a relative path resolves against a `ctx.cwd` that differs from the host process cwd
- [x] 5.3 Confirm and record that no dashboard code path relied on the pre-0.85.1 ignore-`ctx.cwd` semantics of pi's built-in `bash`/`edit`/`find`/`grep`/`ls`/`read`/`write`
- [x] 5.4 Trace the abort path against the `compacting` latch in `event-status-extraction.ts`: determine whether an aborted manual compaction leaves the latch set now that abort genuinely cancels ([pi#8920](https://github.com/earendil-works/pi/issues/8920)); write a failing test for the stranding case first if it reproduces
- [x] 5.5 If 5.4 reproduces, clear the latch on the abort path (NOT via a defensive timeout — design §8); verify the session accepts a subsequent prompt after an aborted compaction
- [x] 5.6 Audit `bridge.ts:2053` (`thinking_level_select` subscription) and `:2242` (its handler) and `provider-register.ts:447` (`deriveSupportedThinkingLevels`) for double-handling now that supported Anthropic transports persist per-turn effort; verify the bridge never writes a thinking level back onto the session and that `deriveSupportedThinkingLevels` stays a read-only capability projection
- [x] 5.7 Audit `packages/extension/src/commit-draft-agent.ts:69-80` (`sdk.SessionManager.inMemory`) against the 0.85.1 restore-externally-stored-entries API ([pi#8980](https://github.com/earendil-works/pi/pull/8980)); either adopt it or record why the existing construction is retained

## 6. Runtime verification of unexported pi internals (design §7 — not satisfied by a green test run)

- [x] 6.1 Spawn a real dashboard session against the bumped pin and verify the draft-agent path completes with no runtime symbol/shape error; record the observed output as evidence
- [x] 6.2 Run `packages/server/src/__tests__/resource-activation-toggle.test.ts` AND toggle a resource in a live session; verify directory layout (`~/.pi/agent`, the `skills|extensions|prompts|themes` roots, `<agentDir>/npm/node_modules/<name>`) is unchanged at 0.85.1
- [x] 6.3 Verify pattern-matching semantics still hold — `matchesAnyPattern` vs `matchesAnyExactPattern`, and the excludes → force-includes → force-excludes precedence
- [x] 6.4 Verify `getPackageIdentity` normalisation and `findAutoloadDeltaBase` delta resolution (engages only for `scope === "project" && autoload === false`)
- [x] 6.5 Verify `ProjectTrustStore` and `hasTrustRequiringProjectResources` are still reachable through the 0.85.1 exports map; a withdrawal breaks the trust gate at runtime, not at build time

## 7. Catalog and credential-routing verification

- [x] 7.1 Assert against the 0.85.1 catalog that `gpt-6-astra` is published with `provider: "openai-codex"` (alongside `openai`, `github-copilot`, `azure-openai-responses`), confirming no provider-key remap is needed — design §5
- [x] 7.2 Add a test that an `openai-codex`-only credential lists the `openai-codex` Astra entry and does NOT list the `openai` entry of the same model id
- [x] 7.3 Verify `OAUTH_INCOMPATIBLE.anthropic` in `packages/server/src/model-proxy/oauth-compat.ts` is UNCHANGED — `claude-fable-5-1` must NOT be added (the 400 was a client-version header gate, not a catalog gate)
- [x] 7.3b Correct the stale NOTE at `packages/server/src/model-proxy/oauth-compat.ts:35-38` ("a raw `openai` slot will not see the `openai-codex` cred without a provider-key remap"), which instructs a future change to commit exactly the remap design §5 prohibits; replace it with the verified no-remap conclusion and a `See change:` pointer
- [x] 7.4 Verify no dashboard code sets or overrides a client-version / `user-agent` header on an **upstream model request**. The grep `grep -rni "claude-cli\|user-agent" --include=*.ts packages/server/src packages/extension/src` legitimately hits three non-model paths — `pi/pi-dev-version-check.ts:129` (pi.dev self-update check), `auth/provider-auth-handlers.ts:199` (Copilot OAuth device flow), and incoming-UA logging. The pass criterion is that every hit is one of those classes and none constructs an Anthropic/model completion request

## 8. Fable 5.1 acceptance (the bug that motivated the bump)

- [ ] 8.1 With a Claude Pro/Max OAuth credential and the bumped pin, send a prompt to `claude-fable-5-1` through a live session and verify it returns 200 rather than 400 `claude_code_version_too_old` (test-plan: manual-only — superseded by #12.26)
- [ ] 8.2 Verify `claude-fable-5`, `claude-opus-4-5`, `claude-sonnet-4-5` still succeed (no regression on the models that already worked) (test-plan: manual-only — superseded by #12.26)

## 9. Test surface de-versioning

- [x] 9.1 Rename `tests/e2e/pi-084-runtime.spec.ts` to a version-neutral filename (e.g. `pi-runtime.spec.ts`), replace the hardcoded `PINNED_PI = "0.84.4"` (`:20`) **and** the hardcoded `expect(compat.minimum).toBe("0.78.0")` (`:90`) with reads of `packages/server/package.json`, and de-version the describe title (`:41`) and the comments at `:4`/`:14`/`:88-89`/`:99`/`:112`/`:120` — note `:88-89` asserts in prose that "the floor is an INDEPENDENT broad-support value and must NOT have moved", which the lockstep policy directly reverses and which must be rewritten, not merely re-versioned; verify the spec passes with no version literal remaining in it
- [x] 9.2 Add a `"0.85.1": { major: 22, minor: 19 }` row to `PI_MIN_TO_NODE_FLOOR` in `packages/shared/src/__tests__/bundled-node-meets-pi-floor.test.ts` (note the path: `packages/shared`, not `packages/server`). The table is an exact-key lookup on `piCompatibility.minimum` and asserts the row is defined, so the floor raise fails it even though `engines.node` is unchanged at `>=22.19.0`. Verify the test fails before the row is added and passes after, and that `BUNDLED_NODE_VERSION` in `packages/electron/scripts/_node-version.sh` already meets 22.19
- [x] 9.3 Verify `packages/extension/package.json` devDep `typebox` stays `^1.3.7` — pi 0.85.1 bundles TypeBox `1.3.7`, unchanged; confirm from the installed `package.json`, not from the changelog
- [x] 9.4 Confirm zero in-repo references to the dropped `@earendil-works/pi-client` / `@earendil-works/pi-protocol` and no new dependency on `@earendil-works/chord`; confirm all runtime imports still use the root specifier only (no `/client`, `/rpc-entry`, `/experimental/plugin`). Note `pnpm-workspace.yaml` `minimumReleaseAgeExclude` still lists `@earendil-works/pi-client@0.84.1` / `pi-protocol@0.84.1`: leave those entries (harmless historical allowances) and add a `@earendil-works/pi-coding-agent@0.85.1`-style exclusion ONLY if `pnpm install` refuses 0.85.1 on release-age grounds
- [x] 9.5 Record (no edit) that the eight publishable packages keep their broad `@earendil-works/pi-coding-agent >=0.80.10` peer range — design §9; verify `grep -rn '">=0.80.10"' packages/*/package.json` still matches those eight and that none was raised incidentally

## 10. Derived docs and spec bookkeeping

- [x] 10.1 Regenerate the doctor `pi-resolution` module with `doctor --regenerate pi-resolution` (never hand-edit the version tables) and verify its per-module knowledge hash updated
- [x] 10.2 Update the pi version in `docker/Dockerfile.AGENTS.md` (the literal lives in the per-file sidecar; `docker/AGENTS.md` contains no pi version) and add a `## [Unreleased]` entry in `CHANGELOG.md` that calls out the **hard break for pi < 0.85.1** and names the upgrade action (design §2 — communication is the mitigation)
- [x] 10.3 Add `See change: update-pi-core-0-85-adopt-apis` notes wherever gate logic was removed, and update the nearest directory `AGENTS.md` rows for every file added/removed (notably the `agent-settled.ts` deletion and the renamed e2e spec). Also correct the stale "inert below floor" prose duplicated in `packages/extension/src/bridge.ts.AGENTS.md` and `packages/server/src/session/event-status-extraction.ts.AGENTS.md` — task 4.1 fixes only the source comments
- [x] 10.4 Delegate all `docs/` prose writes to a DocScribe subagent in caveman style; apply the tree rows it returns yourself
- [x] 10.5 Record in the archive commit message that `#### Scenario:` headings were pre-renamed in `openspec/specs/{pi-core-version-check,bridge-extension,pi-api-feature-detection}/spec.md` during PLANNING and land in the SAME commit as these artifacts, so `openspec archive` accepts the MODIFIED blocks. Heading lines only; archive replaces the bodies wholesale. Before archiving, confirm they are present: `git diff HEAD --stat openspec/specs/` is empty AND `grep -c "Floor and recommended move together" openspec/specs/pi-core-version-check/spec.md` returns 1 — if the grep returns 0 the renames were lost (stash/checkout) and must be re-applied from this change's delta scenario names. Do NOT interpret a clean `openspec validate` as evidence they were unnecessary — validation is clean *because* they are applied

## 11. Final gates

- [x] 11.1 `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` then `grep -nE 'FAIL|Error|✗|✘|Tests +[0-9]+ (failed|passed)' /tmp/pi-test.log` — verify zero failures
- [x] 11.2 `node scripts/check-conventions.mjs` and `npx openspec validate update-pi-core-0-85-adopt-apis --strict` both pass
- [x] 11.3 `npm run quality:changed` passes (Biome ratchet)
- [x] 11.4 Invoke `review-code` on the full diff, then `doubt-driven-review` specifically on the floor raise before it lands (proposal § Discipline Skills)
- [ ] 11.5 Sweep the synced main specs for stale literals after archive: `grep -rn "0\.84\.4\|0\.78\.0" openspec/specs/` returns only intentional historical references (the `## Purpose` preamble is the classic miss — the sync only rewrites `### Requirement:` blocks)


## 12. Folded test scenarios (from `test-plan.md` — the manifest is the source of truth)

Each task below is one manifest row. Triple = `input · trigger · observable`. The `see` pointer is the nearest existing test of that category to copy harness glue from — extend it in place when the subject matches rather than authoring a new file.

### L1 — vitest

- [x] 12.1 Coherent six-pin fixture passes the checker (test-plan #E1) — `serverPkg` fixture with dep `^0.85.1` + `recommended`/`minimum` `0.85.1`, Dockerfile text `@0.85.1`, workspace override `0.85.1`, checker `minVersion` `0.85.1` · `checkPiPinCoherence(…)` runs · returns falsy (no drift). See `packages/server/src/__tests__/pi-version-skew.test.ts`
- [x] 12.2 One-cell-flipped drift table, 6 cases (test-plan #E2) — the #E1 fixture with exactly ONE pin reverted to `0.84.4`, once per pin · checker runs · each case returns a truthy message NAMING the drifted location; 6/6 fail, none silently passes. See `packages/server/src/__tests__/pi-version-skew.test.ts`
- [x] 12.3 Lagging `minimum` is named specifically (test-plan #E3) — `recommended` `0.85.1`, `minimum` `0.78.0`, all other pins `0.85.1` · checker runs · fails naming `piCompatibility.minimum`. See `packages/server/src/__tests__/pi-version-skew.test.ts`
- [x] 12.4 Existing fixtures keep their ORIGINAL failure reason (test-plan #E4) — both `checkPiPinCoherence` fixtures (`~:59-70` drift, `~:79-90` stale-`minVersion`) after gaining a `minimum` field · each runs · each still fails for its original reason, NOT a new "missing pin" error. See `packages/server/src/__tests__/pi-version-skew.test.ts`
- [x] 12.5 Below-floor BVA on `computeCompatibility` (test-plan #E5) — range `{minimum:"0.85.1", recommended:"0.85.1", maximum:null}`, versions `0.78.0`/`0.84.4`/`0.85.0`/`0.85.1`/`0.86.0` · `computeCompatibility(range, v)` · the first three yield an `error` naming BOTH the running version and `0.85.1`; the last two yield no `error` and falsy `upgradeRecommended`. See `packages/server/src/__tests__/pi-version-skew.test.ts`
- [x] 12.6 Hint band empty under lockstep, branch retained (test-plan #E6) — shipped range `minimum == recommended == "0.85.1"` · enumerate `0.78.0`→`0.86.0` · no version yields `upgradeRecommended === true` with `error === undefined`, while the synthetic range `{0.78.0, 0.84.1}` at `0.84.0` still does. See `packages/server/src/__tests__/pi-version-skew-recommended-0-84.test.ts` (leave that file untouched; assert the empty band in the skew test)
- [x] 12.7 `minimum` as Node-floor table key (test-plan #E7) — `piCompatibility.minimum` = `0.85.1` · the floor test runs · `PI_MIN_TO_NODE_FLOOR["0.85.1"]` is defined AND `BUNDLED_NODE_VERSION` ≥ it; omitting the row fails with "Add a row". See `packages/shared/src/__tests__/bundled-node-meets-pi-floor.test.ts`
- [x] 12.8 Below-floor candidate enumerated-but-unselectable; unknown exempt (test-plan #E8) — candidates: a `0.84.4` install, a `0.85.1` install, one whose version probe fails · `GET /api/pi/installs` · `0.84.4` present + floor-failing + `selectable:false`; `0.85.1` selectable; unknown-version NOT floor-failed and still selectable. See `packages/server/src/__tests__/pi-runtime-routes.test.ts`
- [x] 12.9 Codex credential lists only the `openai-codex` Astra entry (test-plan #E9) — catalog with `gpt-6-astra` under four providers, credential store holding ONLY `openai-codex` · the model list is filtered · the `openai-codex` entry is listed; the `openai`/`github-copilot`/`azure-openai-responses` entries of the same id are NOT. See `packages/server/src/model-proxy/__tests__/internal-registry.test.ts`
- [x] 12.10 `OAUTH_INCOMPATIBLE.anthropic` unchanged (test-plan #E10) — the shipped map · assert its contents · only pre-4.x snapshot ids; `claude-fable-5-1` absent. See `packages/server/src/model-proxy/__tests__/oauth-compat.test.ts`
- [x] 12.11 Publishable peer ranges not raised (test-plan #E11) — the eight `packages/*/package.json` declaring pi peers · assert each range · each still `>=0.80.10`, and the coherence checker does not read them. See `packages/server/src/__tests__/pi-version-skew.test.ts`
- [x] 12.12 Exactly one terminal settle, never synthesized (test-plan #F4) — a native `agent_start`→`agent_end`→`agent_settled` sequence · the run completes · exactly ONE `agent_settled` reaches the wire, it is the forwarded native event, carries no `retryPending`, and `isAgentStreaming` is `false` after. See `packages/extension/src/__tests__/prompt-bus-inflight-settle.test.ts`
- [x] 12.13 Retry chain closes on the native settle with the synth block gone (test-plan #F5) — `agent_end`(error) → `auto_retry_waiting` → `agent_start` → `agent_end`(ok) → native `agent_settled` · replay through the bridge after the deletion · `auto_retry_end` emitted exactly once on the native settle, `abortLatch` cleared, no `setTimeout`-driven synthetic settle. See `packages/extension/src/__tests__/retry-tracker.test.ts`
- [x] 12.14 Removed client fallback has no producer (test-plan #F6) — the reducer after the `retryPending` branch is removed · replay an `agent_settled` with no `retryPending` · converges to the terminal state; `grep -rn "retryPending" packages/` returns zero hits. See `packages/client/src/lib/__tests__/event-reducer-agent-settled.test.ts`
- [x] 12.15 Abort must not strand the `compacting` latch (test-plan #X1) — a session in `compacting` · an abort that cancels compaction emitting NEITHER `session_compact` NOR `session_compact_failed` · the latch clears and a subsequent prompt is accepted. Write this RED first (task 5.4). See `packages/server/src/__tests__/event-status-extraction.test.ts`
- [x] 12.16 Resource-activation properties survive the bump (test-plan #X4) — the four enumerated properties (agent-dir layout; `matchesAnyPattern` vs `matchesAnyExactPattern` + excludes→force-includes→force-excludes precedence; `getPackageIdentity`; `findAutoloadDeltaBase` engaging only for `scope==="project" && autoload===false`) · the suite runs against 0.85.1 · all four hold and a violation names which property moved. See `packages/server/src/__tests__/resource-activation-toggle.test.ts`
- [x] 12.17 Unknown-version pi does not synthesize — accepted residual (test-plan #X5) — a runtime whose version probe returns `undefined`/unparseable · a run completes on it · the bridge does NOT synthesize, and the documented consequence is asserted as intended behavior so a future change cannot restore synthesis by accident. See `packages/extension/src/__tests__/retry-tracker.test.ts`
- [x] 12.18 No prefix/substring provider matching (test-plan #X6) — an `openai-codex` credential and an `openai`-provider model whose id matches an Astra entry · the credential filter runs · the `openai` model is NOT granted the Codex credential. See `packages/server/src/model-proxy/__tests__/internal-registry.test.ts`
- [x] 12.19 Dashboard tools honour `ctx.cwd` (test-plan #X7) — a `ctx.cwd` differing from `process.cwd()` plus a relative path argument · invoke each tool changed by task 5.1 · the relative path resolves against `ctx.cwd`; tools recorded cwd-independent assert that independence explicitly. See `packages/extension/src/__tests__/ask-user-tool.test.ts`

### L3 — Playwright vs the docker harness

- [x] 12.20 Advisory names the required version (test-plan #F1) — a harness dashboard whose reported pi is below the floor · load the dashboard · `PiVersionAdvisory` renders the red "below minimum" state, the copy contains the literal `0.85.1` AND the running version, and `/api/health`'s HTTP status is unchanged (no 503). See `tests/e2e/pi-runtime-picker.spec.ts`
- [x] 12.21 Health reports the governed pin and floor, version-neutrally (test-plan #F2) — harness at the port read from `.pi-test-harness.json` (`dashboardPort`, never a hardcoded `:18000`) · `GET /api/health` · `compatibility.current` == `.recommended` == `.minimum` == the version read from `packages/server/package.json`; no version literal remains in the spec file. See the renamed `tests/e2e/pi-runtime.spec.ts` (task 9.1)
- [x] 12.22 One pi copy in the tree — ghost-version guard (test-plan #F3) — a tree installed with the bumped `pnpm-workspace.yaml` override · probe every resolved `@earendil-works/pi-coding-agent/package.json` in root + workspace `node_modules` · every copy reports `0.85.1` and the version `/api/health` reports equals the version the server actually loads. See the renamed `tests/e2e/pi-runtime.spec.ts` (extend its existing X12 probe-agrees-with-pin case)
- [x] 12.23 Aborted compaction does not wedge the session (test-plan #X2) — a live harness session mid manual-compaction · press abort, then send a new prompt · the new prompt starts a turn (transcript grows) and the session never re-enters a stuck `compacting` badge. See `tests/e2e/blackhole-session-pipeline.spec.ts`
- [x] 12.24 Unexported pi internals hold at runtime (test-plan #X3) — a real spawned session on the bumped pin · exercise the draft-agent path (`SessionManager.inMemory`) and toggle a project resource · both complete with no runtime symbol/shape error and `ProjectTrustStore` + `hasTrustRequiringProjectResources` resolve through the exports map. See `tests/e2e/resource-activation-trust.spec.ts`

### ci

- [x] 12.25 Doctor pi tables are regenerated, not hand-edited (test-plan #X8) — the `pi-resolution` module after `doctor --regenerate pi-resolution` · re-run the regenerator · output is byte-identical (idempotent) and its per-module knowledge hash matches, so a hand-edit is detected as drift. See `packages/extension/src/__tests__/doctor/provenance-hash.test.ts`

### manual-only (deferred post-merge by `ship-change`)

- [ ] 12.26 Fable 5.1 on a real Max subscription (test-plan: manual-only) — requires a live Claude Pro/Max OAuth credential that cannot exist in CI. Prompt `claude-fable-5-1` through a live session on the bumped pin and verify 200 rather than 400 `claude_code_version_too_old`; confirm `claude-fable-5`, `claude-opus-4-5`, `claude-sonnet-4-5` still return 200. Supersedes the ad-hoc checks in tasks 8.1/8.2
