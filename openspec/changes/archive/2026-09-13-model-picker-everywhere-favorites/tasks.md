## 1. ModelSelector favorites fallback (core)

- [x] 1.1 Add failing L1 tests to `packages/client/src/components/__tests__/ModelSelector.test.tsx` (harness exemplar: same file, existing favorites cases at ~L64/75) covering: no props + ctx → stars from ctx, click calls ctx spy (test-plan E1); both props explicit → prop spy only, ctx spy never (E2); favorites prop only → zero stars, no Favs toggle, ctx spy never (E3); handler prop only → stars unpressed, prop spy called (E4); no props + no provider → zero stars, no Favs toggle (E5); favs-only key `"1"` + no source → both models listed (E6); favs-only key `"1"` + ctx favorites `[]` → toggle pressed, empty-match message (E7). Verify: all seven fail before 1.2.
- [x] 1.2 In `packages/client/src/components/settings/ModelSelector.tsx` implement design D1: `callerOwns = favorites !== undefined || onToggleFavorite !== undefined`; else read both from `useModelConfigOptional()`; derive `favoritesEnabled = resolvedToggle !== undefined`; gate star buttons, `favs-only-toggle`, and the `favOnly` filter on it. Verify: 1.1 tests pass; `npx vitest run packages/client/src/components/__tests__/ModelSelector.test.tsx packages/client/src/__tests__/ModelSelector.test.tsx` green.
- [x] 1.3 Add L1 test to `packages/client/src/components/__tests__/SettingsPanel.test.tsx` (harness exemplar: same file): Sessions page inside `ModelConfigProvider` with favorites `["anthropic/a"]` + spy → Default Model picker shows `a` pressed, click calls spy `("anthropic/a", false)` (test-plan F1). Verify: passes with no `SettingsPanel.tsx` change.
- [x] 1.4 Add L1 test to `packages/client/src/__tests__/ModelProxySection.test.tsx` (harness exemplar: same file, plus provider wrapping from `packages/client/src/components/__tests__/SettingsPanel.test.tsx`): inside provider with favorites `["anthropic/a"]`, `availableModels=[a,b]` → add-model and alias-target pickers each show `a` pressed + Favs toggle (test-plan F2). Verify: passes with no `ModelProxySection.tsx` change.

## 2. Runtime hook `usePluginConfigOf`

- [x] 2.1 Add failing L1 tests in `packages/dashboard-plugin-runtime/src/__tests__/plugin-context.test.tsx` (harness exemplar: existing tests in that directory using `applyPluginConfigUpdate`): never-set id `"nope"` → identical object across 3 re-renders, no "getSnapshot should be cached" console error, result `{}` (test-plan E13); mounted for `"roles"` then `applyPluginConfigUpdate({id:"roles",…})` inside `act` → hook returns new config (E14). Verify: both fail.
- [x] 2.2 Implement `usePluginConfigOf<T>(pluginId)` in `packages/dashboard-plugin-runtime/src/plugin-context.tsx` via `useSyncExternalStore(subscribeConfig, getSnapshot, getSnapshot)` with a frozen module-level `EMPTY_CONFIG` for the unset case (design D3); export from `index.ts`. Verify: 2.1 passes.

## 3. Automation settings picker

- [x] 3.1 Add failing L1 tests in new `packages/automation-plugin/src/__tests__/AutomationSettings.test.tsx` (harness exemplar: `packages/automation-plugin/src/__tests__/CreateAutomationDialog.wiring.test.tsx` ~L99 — `PluginContextProvider` + `CurrentPluginLayer pluginId="automation"` + `withUiPrimitiveProvider` with a stub `ui:model-selector` that exposes `onSelect`/`current`/`models`): stub `onSelect("anthropic/x")` + host Save → write payload `defaultModel:"anthropic/x"` (test-plan E8); no `input[type=text]` inside `[data-testid=automation-default-model]` (E9); config `"anthropic/x"` + click `automation-default-model-clear` + Save → written `""` and stub `current===undefined` (E10); config `"gone/model"` → stub `current="gone/model"` (E11); config `"anthropic/x:high"`, no interaction → `current="anthropic/x:high"`, not dirty (E12); roles store empty at mount then `applyPluginConfigUpdate` → stub `models.length===1` without remount (F4); no `<label>` ancestor of the picker trigger (F5); roles cfg without `models` → stub `models=[]`, no throw, clear button operable (X1); roles never arrives → render-count spy ≤ 2 (X2). Verify: all fail before 3.2.
- [x] 3.2 In `packages/automation-plugin/src/client/AutomationSettings.tsx`: replace the text input with `useUiPrimitive(UI_PRIMITIVE_KEYS.modelSelector)` over `usePluginConfigOf("roles").models ?? []`; wrapper `div[data-testid=automation-default-model]`; `current={defaultModel || undefined}`; caption and control as siblings (not inside `<label>`); add clear button `data-testid=automation-default-model-clear` that sets `""`. Verify: 3.1 passes.
- [x] 3.3 Reword `defaultModelPlaceholder` in `packages/automation-plugin/src/i18n.ts` (en / zh-CN / hu) from "provider/model-id" copy to "select a model" copy; add a `defaultModelClear` label key in all three. Verify: `rg 'defaultModelPlaceholder|defaultModelClear' packages/automation-plugin/src/i18n.ts` shows 6 entries.

## 4. Cross-surface E2E

- [x] 4.1 Author `tests/e2e/model-favorites-cross-surface.spec.ts` (harness exemplar: `tests/e2e/empty-model-selector.spec.ts` for selector/harness glue; port from `.pi-test-harness.json`): with one live session, open Settings → Sessions Default Model picker, star model M, navigate to the session composer, open its selector → row M `model-fav-toggle[aria-pressed=true]`; reload → still pressed (test-plan F3). Verify: `npm run test:e2e -- model-favorites-cross-surface` green against the docker harness.

## 5. Verification, docs, review

- [x] 5.1 `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` then grep `FAIL|Tests +[0-9]+ (failed|passed)`. Verify: 0 failed.
- [x] 5.2 Manual check (test-plan: manual-only, F6): `npm run build && curl -X POST http://localhost:8000/api/restart`; open Settings → Automation; confirm picker + clear button align with neighbouring controls and placeholder copy reads naturally in en/zh-CN/hu. Verify: human sign-off.
- [x] 5.3 Delegate to `DocScribe`: `docs/plugin-ui-primitives.md` shell-bound block for `ui:model-selector` — favorites now inherited by `ModelSelector` itself when unowned; replace the stale "context absent without a session" line (`App.tsx` always provides `modelConfig`; only `models`/`setModel`/`refreshModels` are session-gated); document `usePluginConfigOf`. Main agent updates `AGENTS.md` rows for `ModelSelector.tsx` (sidecar), `plugin-context.tsx`, `AutomationSettings.tsx`, `i18n.ts`, new test files, with `See change: model-picker-everywhere-favorites`. Verify: `kb dox lint` clean.
- [x] 5.4 Run `review-code` on the diff before commit. Verify: no open critical/major findings.
