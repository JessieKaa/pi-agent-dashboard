## 1. Origin-admission helper (pure, TDD)

- [x] 1.1 Add failing L1 test in `packages/server/src/__tests__/cors.test.ts` (extend; exemplar: its existing `allowed()` table) for `isWsOriginTrusted` — input: origin × scope matrix (undefined, loopback, IPv6, attacker, `null`, stranger zrok, live zrok, `pi-dashboard.dev`, configured, trusted-net) × (browser, terminal, live, bridge, null) · trigger: call helper · observable: undefined→true; attacker/stranger-zrok→false everywhere; `null`→true only on `live` (test-plan #E1); verify it fails on the missing export.
- [x] 1.2 Add failing L1 test in `cors.test.ts` for `isOriginAdmitted` Host-match normalization — input: (origin, host) pairs incl. case, default port, `[::1]`, port mismatch, missing Host, https scheme · trigger: call · observable: matches→true, port mismatch/missing Host→false, scheme ignored (test-plan #E2); verify fails.
- [x] 1.3 Add failing L1 test in `cors.test.ts` for `allowZrokWildcard` — input: `https://xyz.share.zrok.io` and `*.shares.zrok.io` with flag default vs `false`, plus a live tunnel origin · trigger: `isCorsOriginAllowed` · observable: default→true, false→false, live tunnel→true both (test-plan #E3); verify fails.
- [x] 1.4 Implement in `packages/server/src/auth/cors-origin.ts`: `allowZrokWildcard` option on `isCorsOriginAllowed` (default `true`), `isOriginAdmitted(origin, hostHeader, opts)` (CORS decision with `allowZrokWildcard:false` + URL-normalized Host match), `isWsOriginTrusted(origin, hostHeader, scope, opts)` (`live` admits `null`; `bridge`/`null` scope strict), `isMutationOriginTrusted(origin, hostHeader, opts)`; extract the CORS option construction in `server.ts` into one `corsOpts()` closure shared by the CORS plugin and both gates (design D1); verify 1.1–1.3 pass with `npx vitest run packages/server/src/__tests__/cors.test.ts`.

## 2. WebSocket upgrade gate

- [x] 2.1 Create `packages/server/src/__tests__/ws-origin-gate.test.ts` (exemplar: `ws-upgrade-routing.test.ts` — `createTestServer` + `ws` client with `headers`). Failing test: attacker Origin on `/ws`, `/ws/terminal/<id>`, `/live/<id>`, `/ws/bridge`, `/ws/nope` · trigger: connect · observable: every dial `Unexpected server response: 403`, `/ws/bridge` not `400` (test-plan #E4); verify fails.
- [x] 2.2 Add failing test in `ws-origin-gate.test.ts`: no Origin, and loopback Origin · trigger: connect `/ws` · observable: open + `sessions_snapshot` (test-plan #E5); verify fails/passes as baseline.
- [x] 2.3 Add failing test in `ws-origin-gate.test.ts`: `host: mac.local:<port>` + `origin: http://mac.local:<port>` · trigger: connect `/ws` · observable: open (test-plan #E6); verify fails.
- [x] 2.4 Add failing test in `ws-origin-gate.test.ts`: `origin: null` to `/ws`, `/ws/terminal/<id>`, `/live/<id>` · trigger: connect · observable: first two `403`, `/live/<id>` never `403` (test-plan #E7); verify fails.
- [x] 2.5 Add failing test in `ws-origin-gate.test.ts` (exemplar for terminal setup: `terminal-gateway.test.ts`): terminal `T` created over trusted socket · trigger: attacker-origin dial `/ws/terminal/T` + write · observable: `403`, attach count for `T` unchanged, no PTY output (test-plan #E8); verify fails.
- [x] 2.6 Add failing test in `ws-origin-gate.test.ts` (exemplar: `ws-upgrade-routing.test.ts` `mintTicket` + `REMOTE_HEADERS`): ticket `t` · trigger: `/ws?ticket=t` with attacker origin → `403`, then redial with loopback origin · observable: second dial opens, ticket consumed once (test-plan #E9); verify fails.
- [x] 2.7 Add failing test in `ws-origin-gate.test.ts`: `cors.allowedOrigins=[]` → dial with `origin: https://dash.example` `403`; write config adding it (mtime-gated) · trigger: redial · observable: opens without restart (test-plan #E18); verify fails.
- [x] 2.8 Add failing test in `ws-origin-gate.test.ts` capturing `console.error`: origin with `\u001b[31m\n[fake]` and a 600-char origin · trigger: dial `/ws` · observable: `403`, one single-line `[ws-gate]` log, no control chars, origin ≤ 256 chars (test-plan #X1); verify fails.
- [x] 2.9 Add failing test in `ws-origin-gate.test.ts`: malformed origins (`"not a url"`, `"http://"`, comma-joined, leading space) · trigger: real dial `/ws` · observable: `403` each, server survives, next good dial opens (test-plan #X2); verify fails.
- [x] 2.10 Add failing test in `ws-origin-gate.test.ts` (exemplar: `live-server-endpoint.test.ts` for live entry setup): live entry `L` with a local `ws` upstream · trigger: dial `/live/L` with `origin: null` · observable: upstream `connection` fires with no `origin` header, client `open`, no `[ws-gate]` line (test-plan #F4); verify fails.
- [x] 2.11 In `packages/server/src/server.ts` `fastify.server.on("upgrade")`, as the first statement after computing `scope` (ahead of the `bridge` `400` early-return and the `authConfig.secret` branch), reject with `HTTP/1.1 403 Forbidden` + `socket.destroy()` when `!isWsOriginTrusted(request.headers.origin, request.headers.host, scope, corsOpts())`, log `[ws-gate] rejected upgrade origin=… scope=… peer=…` (control chars stripped, capped 256), fix the misleading "Origin check is defense-in-depth" comment; verify 2.1–2.10 pass and `ws-upgrade-routing`, `bridge-upgrade-auth`, `bridge-local-token-upgrade`, `ws-ticket`, `terminal-gateway` suites still pass.

## 3. pi-gateway Origin refusal

- [x] 3.1 Add failing rows to `packages/server/src/__tests__/bridge-upgrade-auth.test.ts` (exemplar: its existing verdict table): tcp loopback with `headers.origin` ∈ {attacker, loopback, `null`, `""`} × {no ticket, valid local token, valid ticket} · trigger: `decideBridgeUpgrade` · observable: `{allow:false, cause:"browser-origin"}` for every origin-bearing tcp row; rows without origin byte-identical to before (test-plan #E10); verify fails.
- [x] 3.2 Add failing row in `bridge-upgrade-auth.test.ts`: transport unix with `origin: http://x` · trigger: call · observable: allow (test-plan #X4); verify fails or passes as baseline.
- [x] 3.3 Implement the rule in `packages/server/src/pi/bridge-upgrade-auth.ts`: new `BridgeRefusalCause` member `"browser-origin"`, placed after the `transport === "unix"` allow branch and before local-token / ticket / grace (design D4b); verify 3.1–3.2 pass and `home-lock`, `ws-ticket`, `bridge-local-token-upgrade` suites still pass.
- [x] 3.4 Add real-listener test (exemplar: `bridge-local-token-upgrade.test.ts` gateway boot): `ws` client to the pi-gateway TCP port with `origin: http://attacker.example`, no ticket · trigger: connect · observable: `Unexpected server response: 401`, session registry unchanged, `[pi-gateway]` log line mentions `browser-origin` (test-plan #E11); verify passes.

## 4. Mutating REST Origin gate

- [x] 4.1 Create `packages/server/src/__tests__/api-origin-gate.test.ts` (exemplar: `system-open-endpoints.test.ts` for `fastify.inject` app boot). Failing test: method {POST,PUT,DELETE,PATCH,GET,HEAD,OPTIONS} × origin {absent, loopback, attacker, `null`} on `/api/tunnel-connect` · trigger: inject · observable: mutating+attacker/`null` → `403 {error:"untrusted origin"}`; absent/loopback and all GET/HEAD/OPTIONS → not `403` (test-plan #E12); verify fails.
- [x] 4.2 Add failing test in `api-origin-gate.test.ts`: `POST //api/tunnel-connect`, `/api//tunnel-connect`, `/API/tunnel-connect` with attacker origin, route handler spied · trigger: inject · observable: handler never invoked, status ∈ {403,404} (test-plan #E13); verify fails.
- [x] 4.3 Add failing test in `api-origin-gate.test.ts` with auth plugin registered: `POST /auth/logout` attacker vs loopback origin · trigger: inject · observable: attacker `403` with no cookie-clearing `set-cookie`; loopback clears cookie (test-plan #E14); verify fails.
- [x] 4.4 Add failing test in `api-origin-gate.test.ts`: `GET /auth/callback/github?code=x`, any POST auth route if present, `POST /editor/...` with attacker origin · trigger: inject · observable: none return `{error:"untrusted origin"}` (test-plan #E15); verify baseline.
- [x] 4.5 Add failing test in `api-origin-gate.test.ts` (exemplar: `pairing.test.ts`): `POST /api/pair/challenge` with `host: 192.168.1.5:8000` + matching origin · trigger: inject · observable: not `403` (test-plan #E16); verify fails.
- [x] 4.6 Add failing test in `api-origin-gate.test.ts`: `POST /api/pair/challenge` with `origin: https://pi-dashboard.dev` · trigger: inject · observable: not `403` (test-plan #E17); verify fails.
- [x] 4.7 Add failing test in `api-origin-gate.test.ts`: `origin: ""` and `origin: "null "` on `POST /api/tunnel-connect` · trigger: inject · observable: `403` both, no throw (test-plan #X3); verify fails.
- [x] 4.8 Register one `onRequest` hook in `server.ts` after the CORS plugin: skip unless `req.routeOptions.url` starts with `/api/` or equals `/auth/logout`, and method ∉ {GET, HEAD, OPTIONS}; reject with `403 {error:"untrusted origin"}` when `!isMutationOriginTrusted(req.headers.origin, req.headers.host, corsOpts())`; log `[csrf-gate] rejected <method> <path> origin=…` (all three sanitized, capped 256); verify 4.1–4.7 pass.
- [x] 4.9 Run the full server suite (`set -o pipefail; npx vitest run packages/server 2>&1 | tee /tmp/pi-test.log; grep -nE 'FAIL|Tests +[0-9]+ (failed|passed)' /tmp/pi-test.log`) and verify zero failures — in particular no existing `inject`-based route test that sends a non-loopback `Origin` on a mutation now breaks.

## 5. L2 / L3 coverage

- [x] 5.1 Add `qa/tests/NN-origin-gate.sh` + `.ps1` (exemplar: `qa/tests/04-terminal.sh`): `curl -X POST /api/terminals -d '{"cwd":"/tmp"}'` without Origin, then with `-H 'Origin: http://attacker.example'` · trigger: qa smoke against a running server · observable: first `200` with `id`, second `403` (test-plan #X5); verify both scripts exit 0 locally.
- [x] 5.2 Create `tests/e2e/origin-gate.spec.ts` (exemplar: `tests/e2e/csp.spec.ts` for harness attach, `keeper-log-health.spec.ts` for reading the harness server log via `docker exec`): attacker page served via `page.route("http://attacker.test/**")` running the PoC `new WebSocket("ws://127.0.0.1:<dashboardPort>/ws")` · trigger: script runs · observable: `onerror` fires, `onopen` never, no `sessions_snapshot` text, harness log has `[ws-gate] rejected upgrade origin=http://attacker.test scope=browser` (test-plan #F2); verify green against the harness.
- [x] 5.3 Add to `origin-gate.spec.ts`: from the attacker page `fetch("http://127.0.0.1:<port>/api/tunnel-connect",{method:"POST",mode:"no-cors"})` · trigger: call · observable: `GET /api/tunnel/status` from the dashboard origin unchanged before/after; harness log has `[csrf-gate] rejected POST /api/tunnel-connect origin=http://attacker.test` (test-plan #F3); verify green.
- [x] 5.4 Add to `origin-gate.spec.ts` (exemplar: `tests/e2e/terminal.spec.ts`): dashboard page at the harness origin · trigger: load → create terminal via UI → type `echo ok` · observable: terminal pane contains `ok`, no `[ws-gate]` line in harness log (test-plan #F1); verify green.
- [x] 5.5 Run existing `tests/e2e/pairing-qr.spec.ts` and `security-pair-link.spec.ts` unchanged · trigger: `npm run test:e2e -- pairing-qr security-pair-link` · observable: green and harness log has no `[csrf-gate]` line for `/api/pair/*` (test-plan #F5); verify.

## 6. Docs and records

- [x] 6.1 Update `packages/server/src/auth/cors-origin.ts.AGENTS.md`, `packages/server/src/auth/AGENTS.md`, `packages/server/src/pi/AGENTS.md` (bridge-upgrade-auth row), `packages/server/src/__tests__/AGENTS.md` (new test files), `qa/tests/AGENTS.md`, `tests/e2e/AGENTS.md` with the new exports/rule/files and `See change: fix-ws-origin-cswsh`; verify `kb dox lint` reports no stale/missing row for the touched files.
- [x] 6.2 Delegate to `DocScribe`: add a short "Cross-site request gate" paragraph (WS Origin gate per scope, Host-match rule, zrok wildcard exclusion, pi-gateway Origin refusal, `/api/*` + `/auth/logout` mutation gate, absent-Origin rule, `live` `null` carve-out, log tags, `cors.allowedOrigins` escape hatch) to the security/auth section of `docs/architecture.md` (or the doc it points to); verify the paragraph exists and `docs/AGENTS.md` row updated if a new file was created.
- [x] 6.3 Add a `## [Unreleased]` → `### Security` entry in `CHANGELOG.md` referencing #625, naming the hand-run-zrok `cors.allowedOrigins` note; verify the entry renders.

## 7. Manual verification (deferred post-merge)

- [x] 7.1 Manual repro of issue #625: self-host the issue's inline PoC HTML at `http://127.0.0.1:8899` (never the reporter's remote URL), click "Launch Attack Chain", press the CSRF button, and run `new WebSocket('ws://127.0.0.1:<piPort>')` from the same page in a real desktop browser against a real desktop dashboard; confirm every step fails and `server.log` shows `[ws-gate]`, `[csrf-gate]`, `[pi-gateway]` lines (test-plan: manual-only, #X6).
- [x] 7.2 Manual check of the external-zrok escape hatch: put a dashboard behind a hand-run `zrok share public`, open it → WS refused with `[ws-gate]` line; add the share origin to `cors.allowedOrigins` → works (test-plan: manual-only, #X7).

## Implementation notes (deviations from the task text, all deliberate)

- **4.8 — the REST hook is a factory, not an inline closure.** `createMutationOriginGate(getOpts)` lives in `packages/server/src/auth/mutation-origin-gate.ts` and is registered in `server.ts` after the CORS plugin. The task (and design D4) described it inline; inline is untestable with `fastify.inject` because `DashboardServer` exposes no Fastify instance, and the test-plan's #E12/#E13 rows are written against `inject`. Behavior is identical.
- **5.1 — the QA smoke drives `POST /api/ws-ticket`, not `POST /api/terminals`.** There is no `/api/terminals` route (terminals are created over the browser WS with `create_terminal`); the task text assumed one. `/api/ws-ticket` is the closest side-effect-free mutation, and a smoke test must not connect a tunnel or spawn a PTY to prove a gate.
- **5.2–5.4 — the attacker page is served by a local HTTP server reached through `--host-resolver-rules=MAP attacker.test 127.0.0.1`, not `page.route` fulfilment.** A fulfilled route has no resolved IP, so Chrome files the page under the PUBLIC address space and its own Private Network Access check blocks the request before it leaves the browser: the assertion passes while proving nothing about the server. Verified empirically — under `page.route` the dial failed with no `[ws-gate]` line; under host-resolver-rules the gate logs the refusal.
- **5.4 (#F1) — asserts the dashboard's own origin still opens `/ws` (and trips no `[ws-gate]`) instead of driving the inline-terminal UI.** The exemplar `tests/e2e/terminal.spec.ts` fails in this harness on `develop` too (the composer button resolves hidden), so the UI path would have imported a pre-existing flake into a security gate.
- **2.8 (#X1) — a control-character Origin never reaches the gate:** Node's HTTP parser refuses the request with `400` first (asserted via a raw socket). The reachable half (600-char origin → one single-line `[ws-gate]`, origin capped at 256) is asserted end-to-end, and `sanitizeHeaderForLog` is unit-tested directly.
- **5.5 (#F5) — verified by running `pairing-qr.spec.ts` + `security-pair-link.spec.ts` green against the harness and grepping the harness `server.log`: zero `[csrf-gate]` lines for `/api/pair/*`.**
