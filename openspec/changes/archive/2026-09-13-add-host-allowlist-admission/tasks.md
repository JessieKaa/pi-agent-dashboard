## 1. Config surface (`packages/shared`, `config-api.ts`, `config-snapshot.ts`)

- [x] 1.1 Test `allowedHosts` loader (test-plan #E10) — extend `packages/shared/src/__tests__/config-openspec.test.ts` pattern: config without key · `loadConfig()` · `[]`; `["dash.home.arpa", 42, null]` · `loadConfig()` · `["dash.home.arpa"]`; `ensureConfig()` output has no `allowedHosts` key. Verify red first.
- [x] 1.2 Test `hostGate` loader + partial write (test-plan #E11) — same file + `packages/server/src/__tests__/config-api.test.ts` pattern: absent / `{mode:"yes"}` · `loadConfig()` · `report` both; file with `auth` + `allowedHosts` · `writeConfigPartial({hostGate:{mode:"enforce"}})` · file holds `hostGate.mode: "enforce"`, `auth` and `allowedHosts` byte-identical. Verify red first.
- [x] 1.3 Test partial write preserves / replaces `allowedHosts` (test-plan #E12) — `config-api.test.ts` pattern: file `allowedHosts:["a"]` · `writeConfigPartial({auth:{…}})` then `writeConfigPartial({allowedHosts:["b"]})` · `["a"]` intact after first, `["b"]` after second. Verify red first.
- [x] 1.4 Implement: `DashboardConfig.allowedHosts` (optional, string-filtered, default `[]`, not seeded) and `hostGate: { mode }` (absent/unrecognised → `report`, not seeded) in `packages/shared/src/config.ts`; `allowedHosts` + `hostGate` branches in `packages/server/src/config-api.ts` `writeConfigPartial` (each written whole); export the shared hostname regex used by the gate from `packages/shared`. Verify 1.1–1.3 green.
- [x] 1.5 Test legacy pairing key via live snapshot (test-plan #E7) — `ws-origin-gate.test.ts` live-config pattern: config with only `pairing.publicBaseUrls:["https://old.example.com"]` · `livePublicBaseUrls()` then `isHostAdmitted("old.example.com", opts)` · `true`. Verify red first.
- [x] 1.6 Implement `liveAllowedHosts()`, `liveHostGateMode()`, `livePublicBaseUrls()` (via `resolvePublicBaseUrls`) in `packages/server/src/config-snapshot.ts`. Verify 1.5 green.

## 2. Pure decision helpers (`packages/server/src/auth/`)

- [x] 2.1 Test hostname-only normalisation (test-plan #E1) — `packages/server/src/__tests__/cors.test.ts` pattern, new `host-admission.test.ts`: `allowedHosts:["dash.home.arpa"]` · `isHostAdmitted("dash.home.arpa:9443")`, `("DASH.home.arpa.")`, `("[::1]:8000")` · all `true`. Verify red first.
- [x] 2.2 Test fail-closed partitions (test-plan #E2) — same file: opts admitting everything derivable · `isHostAdmitted(undefined)`, `("")`, `("evil.example/@localhost")`, `("a b")`, `(".local")`, `("127.1")`, `("2130706433")` · every call `false`. Verify red first.
- [x] 2.3 Test IP-literal rule on wildcard bind (test-plan #E3) — same file: `bindHost:"0.0.0.0"`, `trustedNetworks:[]` · `192.168.1.50:8000`, `[fe80::1]:8000`, `::ffff:127.0.0.1`, `10.4.0.9` · all `true`; `rebind.example` · `false`. Verify red first.
- [x] 2.4 Test bind-address name (test-plan #E4) — same file: `bindHost:"dash-host"` · `dash-host:8000` / `dash-host2:8000` · `true` / `false`. Verify red first.
- [x] 2.5 Test `.local` label boundary (test-plan #E5) — same file: no config · `mac.local`, `a.b.local`, `.local`, `local`, `evil.localx` · `true, true, false, false, false`. Verify red first.
- [x] 2.6 Test derived sources first-match + unparseable entries (test-plan #E6) — same file: `publicBaseUrls:["https://pi.example.com:8443"]`, `cors.allowedOrigins:["https://dash.example"]`, tunnel `["https://abc.share.zrok.io"]`, `allowedHosts:["dash.home.arpa"]`, plus `"not a url"` in each list · `isHostAdmitted` for the four hosts + `xyz.share.zrok.io` · four `true`, stranger `false`, no throw. Verify red first.
- [x] 2.7 Implement `isHostAdmitted(hostHeader, opts)` in `auth/host-admission.ts` (D1 order: loopback / `net.isIP` / `bindHost` → `<label>.local` → publicBaseUrls → cors → tunnel → allowedHosts; shared regex; skip unparseable). Verify 2.1–2.6 green.
- [x] 2.8 Test same-origin-by-Host tightened only in enforce (test-plan #E8) — `cors.test.ts`: `rebind.example:8000` pair and `mac.local:8000` pair · `isSameOriginByHost(o, h, {…, hostGateMode})` for `report` / `enforce` · rebind `true` in report, `false` in enforce; mac.local `true` in both. Verify red first.
- [x] 2.9 Implement: `isSameOriginByHost(origin, hostHeader, opts)` requires `isHostAdmitted` when `opts.hostGateMode === "enforce"`; thread `opts` through `isOriginAdmitted` / `isWsOriginTrusted` / `isMutationOriginTrusted`. Verify 2.8 + existing `cors.test.ts` green.
- [x] 2.10 Test mode resolver decision table (test-plan #E9) — `packages/server/src/__tests__/csp.test.ts` pattern: env ∈ {unset, `enforce`, `report`, `yes`} × config ∈ {absent, `enforce`} · `resolveHostGateMode(env, cfg)` · unset+absent→`report`,false · `enforce`+absent→`enforce`,true · `report`+`enforce`→`report`,true · `yes`+absent→`report`,false · `yes`+`enforce`→`enforce`,false; `yes` logs exactly one boot line naming `PI_DASHBOARD_HOST_GATE`. Verify red first.
- [x] 2.11 Implement `resolveHostGateMode(env, configMode) → { mode, envOverridden }` with the one-time boot warning. Verify 2.10 green.

## 3. Server wiring (`packages/server/src/server.ts`, routes)

- [x] 3.1 Test REST enforce refusals (test-plan #E13) — `packages/server/src/__tests__/pairing.test.ts` `fastify.inject` pattern: `PI_DASHBOARD_HOST_GATE=enforce` · `GET /api/sessions` `Host: rebind.example:8000` no Origin; `POST /api/tunnel-connect` same Host + matching Origin; `GET /api/health` with no `Host` · each `403`, `error:"host_not_allowed"`, `hint` contains `allowedHosts` + `publicBaseUrls`, no `access-control-allow-origin`, handler spy not called. Verify red first.
- [x] 3.2 Test REST admits in both modes (test-plan #E14) — same pattern: mode ∈ {report, enforce} · `GET /api/health` with `Host:` `localhost:8000`, `127.0.0.1`, `192.168.1.50:8000`, `mac.local:8000`, `pi.example.com` (publicBaseUrls), `dash.home.arpa` (allowedHosts) · `200` for every pair. Verify red first.
- [x] 3.3 Test report mode proceeds + logs (test-plan #E15) — same pattern: env unset, `hostGate` absent · `GET /api/health` `Host: rebind.example` · `200`; exactly one line matching `[host-gate] would-refuse host=rebind.example`. Verify red first.
- [x] 3.4 Test live mode + live allowedHosts (test-plan #E16) — `ws-origin-gate.test.ts` live-config pattern: write `hostGate.mode:"enforce"` to the temp config → `Host: rebind.example`; append `rebind.example` to `allowedHosts` → same; remove `https://pi.example.com` from `publicBaseUrls` → `Host: pi.example.com` · `403`, `200`, `403` with no restart / invalidate. Verify red first.
- [x] 3.5 Implement the `onRequest` Host hook registered before `@fastify/cors` (JSON 403 body from D4, no CORS headers); extend `corsOpts()` with `allowedHosts`, `publicBaseUrls`, `hostGateMode` (live) and `bindHost` (from `ServerConfig.host`, boot-time). Verify 3.1–3.4 green.
- [x] 3.6 Test WS enforce refusal (test-plan #E17) — `packages/server/src/__tests__/ws-upgrade-routing.test.ts` real-handler pattern: enforce, valid single-use ticket on the URL · upgrade `/ws` `Host: rebind.example:8000` + `Origin: http://rebind.example:8000` · socket gets `HTTP/1.1 403` and is destroyed; ticket still unconsumed; log has `[host-gate] refused` + `scope=browser`. Verify red first.
- [x] 3.7 Test WS report mode unchanged (test-plan #E18) — same pattern: report · `/ws` `Host: proxy-int.corp:8000` + matching Origin; `/ws` `Host: mac.local:8000` + matching Origin · both pass the Origin gate; one `would-refuse` line for `proxy-int.corp`, none for `mac.local`. Verify red first.
- [x] 3.8 Implement the Host check at the top of the `upgrade` handler before `isWsOriginTrusted`. Verify 3.6–3.7 green.
- [x] 3.9 Test log sanitisation (test-plan #X1) — `pairing.test.ts` pattern with log spy: `Host: "rebind.example\r\n[host-gate] refused host=forged"` · report request · exactly one `[host-gate]` line, no `\n`, no `forged`; ring key `(malformed)`. Verify red first.
- [x] 3.10 Test per-host rate limit (test-plan #X2) — same pattern: 100× `Host: rebind.example` in <1 s · report · ≤1 line for `rebind.example`; ring `count:100`. Verify red first.
- [x] 3.11 Test distinct-host scan cap (test-plan #X3) — same pattern with fake timers: 10 000 distinct `a<n>.rebind.example` in <1 s · report · ≤60 refusal lines + exactly one `[host-gate] suppressed 9940 refusal lines`; limiter map ≤256; ring length 50. Verify red first.
- [x] 3.12 Implement `[host-gate]` logging (`refused` / `would-refuse`, `host=`, `origin=`, method+url or `scope=`, `sanitizeHeaderForLog`) with per-host 1/min limit, global 60/min cap + `suppressed <n>` summary, 256-entry map (oldest evicted). Verify 3.9–3.11 green.
- [x] 3.13 Test Accept negotiation (test-plan #E19) — `pairing.test.ts` pattern: enforce, `GET /` `Host: rebind.example:8000` · `Accept` ∈ {`text/html,*/*;q=0.8`, `application/json`, `application/json, text/html;q=0.9`, `*/*`, absent} · first → `403 text/html`, other four → `403 application/json`. Verify red first.
- [x] 3.14 Test HTML page content (test-plan #E20) — same pattern: enforce, live tunnel `https://abc.share.zrok.io`, `allowedHosts:["dash.home.arpa"]`, port 8000 · `GET /` `Host: rebind.example:8000` `Accept: text/html` · body has `rebind.example:8000`, `localhost:8000`, `allowedHosts`, `publicBaseUrls`; none of `<script`, `/assets/`, `abc.share.zrok.io`, `dash.home.arpa`. Verify red first.
- [x] 3.15 Test HTML escaping (test-plan #E21) — same pattern: enforce · `GET /` `Host: a<img>.example` `Accept: text/html` · `403`, body has `a&lt;img&gt;.example`, not `<img>`. Verify red first.
- [x] 3.16 Implement the static HTML refusal responder (first `Accept` media type `text/html` → HTML; else JSON; Host escaped; `localhost:<port>` + two config keys; no enumeration; inline CSS with `prefers-color-scheme` palette; copy per `mockups/refused-403.html`). Verify 3.13–3.15 green.
- [x] 3.17 Test `GET /api/host-gate` shape (test-plan #E22) — `pairing.test.ts` pattern: `publicBaseUrls:["https://pi.example.com"]`, `allowedHosts:["dash.home.arpa"]`, tunnel `abc.share.zrok.io`, env `report` + config `enforce` · `GET /api/host-gate` from loopback · `mode:"report"`, `envOverridden:true`, `admitted` has `{pi.example.com, public-base-url}`, `{dash.home.arpa, allowed-host}`, `{abc.share.zrok.io, live-tunnel}`, exactly one `local` + one `ip-address` row; `recent:[]`. Verify red first.
- [x] 3.18 Test ring counts + eviction at 50 (test-plan #E23) — same pattern: report · 3× `Host: rebind.example:8000` + 1× `proxy-int.corp` in 1 s; then 50 distinct hosts; then `rebind.example` once · `rebind.example` `count:3` (no port) + `proxy-int.corp` `count:1`, both `would-refuse`; after 50 distinct: `recent.length === 50`, `proxy-int.corp` evicted, `rebind.example` first with `count:4`. Verify red first.
- [x] 3.19 Test ring malformed key + admitted stops counting (test-plan #E24) — same pattern: report · 2 requests with no `Host`; append `rebind.example` to `allowedHosts`, request it · `{host:"(malformed)", count:2}`; `rebind.example` count unchanged. Verify red first.
- [x] 3.20 Test endpoint auth parity (test-plan #E25) — `pairing.test.ts` OAuth pattern: OAuth configured, non-local peer, no cookie · `GET /api/host-gate` · same status as `GET /api/config` for the same request. Verify red first.
- [x] 3.21 Implement the refusal ring (parsed hostname key, `(malformed)`, every refusal counted, 50-entry LRU-by-lastSeen) and `GET /api/host-gate` (`{ success, mode, envOverridden, admitted, recent }`, `admitted` derived server-side in D1 order with pattern rows, same auth as `GET /api/config`). Verify 3.17–3.20 green.
- [x] 3.22 Test live-tunnel source throws (test-plan #X4) — `pairing.test.ts` pattern with `getLiveTunnelOrigins` mocked to throw: enforce · `GET /api/health` `Host: localhost:8000` and `Host: abc.share.zrok.io` · `200` then `403`, never `500`. Verify red, then make the tunnel source fail-empty; verify green.
- [x] 3.23 Test config unreadable mid-run (test-plan #X5) — `ws-origin-gate.test.ts` live-config pattern: replace temp `config.json` with invalid JSON while running · enforce, `GET /api/health` `Host: dash.home.arpa` (previously in `allowedHosts`) · no `500`; last good snapshot used → `200`; a parse-error log line exists. Verify red/green against the existing snapshot contract.
- [x] 3.24 Test regression #E6 + #E16 (test-plan #X7) — `ws-upgrade-routing.test.ts` + `pairing.test.ts`: enforce · `/ws` `Host: mac.local:8000` + `Origin: http://mac.local:8000`; `POST /api/pair/challenge` from LAN peer `Host: 192.168.1.20:8000` no Origin · upgrade completes; pairing `200`. Verify green (must not go red at any point in section 3).

## 4. Client (`packages/client`, per `mockups/ui-plan.md`; tokens only)

- [x] 4.1 Test Save diff carries new fields (test-plan #E27) — `packages/client/src/components/settings/__tests__/settings-field-contract.test.tsx` pattern: original `{hostGate:{mode:"report"}, allowedHosts:[]}`, draft `{hostGate:{mode:"enforce"}, allowedHosts:["a"]}` · `computeConfigPartial(draft, original)` · partial equals `{ hostGate:{mode:"enforce"}, allowedHosts:["a"] }`; `CONFIG_FIELD_PAGE.allowedHosts === "security"` and `.hostGate === "security"`. Verify red first.
- [x] 4.2 Test section composition (test-plan #F1) — `settings-page-composition.test.tsx` pattern with `GET /api/host-gate` mocked `{mode:"report", envOverridden:false, admitted:[…], recent:[]}` · render Security tab · heading `Allowed hostnames` after `Trusted Networks` and before `Pair a device` in DOM order; radios `Report only` (checked) + `Enforce`; "No refusals since start.". Verify red first.
- [x] 4.3 Test mode consequence inline, no confirm (test-plan #F2) — same file, `window.confirm` spied · click `Enforce` · consequence text mentions `403`; `confirm` not called; draft `hostGate.mode === "enforce"`; no fetch. Verify red first.
- [x] 4.4 Test env override disables (test-plan #F3) — same file: mock `envOverridden:true, mode:"report"` · render · both radios `disabled`; a line contains `PI_DASHBOARD_HOST_GATE`. Verify red first.
- [x] 4.5 Test admitted rows from endpoint (test-plan #F4) — same file: mock `admitted:[{pi.example.com, public-base-url}, {*.local, local}, {any IP address, ip-address}]` · render · `pi.example.com` row has pill `public base URL` + link to `/gateway`, no remove; pattern rows have no link; the client module imports no `isHostAdmitted`. Verify red first.
- [x] 4.6 Test textarea validation (test-plan #E26) — `settings-field-contract.test.tsx` pattern: empty draft · blur after `https://dash.home.arpa:9443/`; `my_service.docker`; `dash.home.arpa` · `aria-invalid=true` + message contains `dash.home.arpa`; `aria-invalid=true` + "cannot be used"; valid, draft `allowedHosts=["dash.home.arpa"]`. Verify red first.
- [x] 4.7 Test Allow is draft-only (test-plan #F5) — same file, `fetch` spied: mock `recent:[{proxy-int.corp, 1, would-refuse}]` · click `Allow proxy-int.corp` · textarea contains `proxy-int.corp`; row gone; panel dirty; zero `PUT`; after simulated Save the `PUT /api/config` body has `allowedHosts:["proxy-int.corp"]`. Verify red first.
- [x] 4.8 Test hidden rows survive refetch (test-plan #F6) — same file: draft `allowedHosts:["proxy-int.corp"]`, refetch returns the same row · re-render · row still hidden. Verify red first.
- [x] 4.9 Test outcome pill per entry (test-plan #F7) — same file: `recent:[{a.example, would-refuse}, {b.example, refused}]`, draft mode `enforce` · render · pills read `would-refuse` and `refused` respectively. Verify red first.
- [x] 4.10 Test endpoint failure degrades (test-plan #X6) — same file: `GET /api/host-gate` mocked `500` · render · mode control + textarea editable; admitted/recent areas show an error line naming the endpoint; no uncaught rejection. Verify red first.
- [x] 4.11 Implement `packages/client/src/components/settings/AllowedHostsSection.tsx` (mode = segmented shape of `packages/client/src/components/Gateway/GatewayProviderSection.tsx`; admitted + recent from `GET /api/host-gate`; textarea = Allowed-Users field contract on the draft with the shared regex; Allow = draft append; hide-in-draft filter at render), compose into the Security tab of `SettingsPanel.tsx`; add `allowedHosts` + `hostGate` to `computeConfigPartial` and `CONFIG_FIELD_PAGE`; i18n keys. Verify 4.1–4.10 green and `packages/client` typecheck clean.
- [x] 4.12 Test gateway pill decision table (test-plan #F8) — `packages/client/src/components/Gateway/__tests__/Gateway.test.tsx` pattern: rows `ok` + in `publicBaseUrls`; `incomplete` with public URL missing; `incomplete` with only `trustedNetworks` delta missing · render `GatewayUrlManager` · pill present / absent / present; no `role=button`; `title` names the hostname. Verify red first.
- [x] 4.13 Implement the `Host admitted` pill in `GatewayUrlManager.tsx` (hostname ∈ `resolvePublicBaseUrls(config)` ∪ `cors.allowedOrigins` hosts, independent of `data-status`). Verify 4.12 green.
- [x] 4.14 E2E Allow → Save → admitted (test-plan #F9) — new `tests/e2e/host-gate-allow.spec.ts`, see `tests/e2e/security-pair-link.spec.ts` for the Security-tab harness glue and port read from `.pi-test-harness.json`: seed one refusal with `curl -H 'Host: proxy-int.corp' http://localhost:<port>/api/health` · open Settings ▸ Security, click `Allow proxy-int.corp`, Save, await refetch · Recent refusals no longer lists it; Currently admitted shows `proxy-int.corp` with pill `allowed host`; harness `config.json` `allowedHosts` contains it.
- [x] 4.15 E2E keyboard-only pass (test-plan #F10) — same spec file, see `tests/e2e/security-pair-link.spec.ts`: Security tab open · Tab from the `Trusted Networks` textarea · focus order mode radio → textarea → each `Allow`; every stop has an accessible name; `axe` no serious/critical violation on the section.
- [x] 4.16 Manual: compare the live section against `mockups/security-allowed-hosts.html` in dark + light (test-plan: manual-only, #F11).
- [x] 4.17 Manual: view the HTML refusal page under `PI_DASHBOARD_HOST_GATE=enforce` at an unlisted name in light and dark OS scheme (test-plan: manual-only, #F12).

## 5. Full suite and quality

- [x] 5.1 `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` then grep `FAIL|Tests +[0-9]+ (failed|passed)`; verify zero failures.
- [x] 5.2 `npm run quality:changed`; verify no new Biome findings.

## 6. Docs and closeout

- [x] 6.1 Update `packages/server/src/auth/AGENTS.md` (`cors-origin.ts`, `host-admission.ts`), `packages/server/src/AGENTS.md` (`server.ts`, `config-snapshot.ts`, `config-api.ts`), `packages/server/src/routes/AGENTS.md` (host-gate route), `packages/shared/src/AGENTS.md` (`config.ts`), `packages/client/src/components/settings/AGENTS.md` (`AllowedHostsSection.tsx`, `SettingsPanel.tsx`), `packages/client/src/components/Gateway/AGENTS.md` (`GatewayUrlManager.tsx`), `tests/e2e/AGENTS.md` (new spec), each with `See change: add-host-allowlist-admission`; verify `kb dox lint` reports no stale/missing rows for the touched files.
- [x] 6.2 Delegate to `DocScribe`: `docs/architecture.md` security section — one paragraph on the Host gate beside the Origin gates; config reference gains `allowedHosts`, `hostGate.mode`, `PI_DASHBOARD_HOST_GATE` (env overrides config; report default, enforce flip; unrecognised value logged and ignored); FAQ entry "I got 'This address is not allowed'"; verify the paragraphs exist and the docs tree row is applied.
- [x] 6.3 Add a `## [Unreleased]` CHANGELOG entry (security): report-only Host gate, `allowedHosts`, Settings ▸ Security ▸ Allowed hostnames with Recent refusals; ask operators to review it (or grep `server.log` for `[host-gate] would-refuse`) before switching to Enforce; verify entry present.
- [x] 6.4 Manual QA: start with `PI_DASHBOARD_HOST_GATE=enforce` and `PI_DASHBOARD_HOST=0.0.0.0`; confirm Electron / `localhost:8000` / `<lan-ip>:8000` / `<name>.local:8000` / a live zrok tunnel all load; `curl -H 'Host: rebind.example' http://127.0.0.1:8000/api/health` → 403 JSON with hint; same with `-H 'Accept: text/html'` → HTML page; the refusal shows in Settings ▸ Security ▸ Recent refusals and Allow + Save admits it; record in the change.
- [x] 6.5 Reply on GitHub issue #637 with the change summary and the report-only → enforce rollout; verify comment posted.
