# Test Plan — add-host-allowlist-admission

Stage: design   Generated: 2026-09-12

Levels: L1 = vitest (`packages/*/src/**/__tests__`), L3 = Playwright vs docker harness (`tests/e2e/`, port from `.pi-test-harness.json`). No L2 rows: the gate is a pure server decision + rendered UI; no OS/process behaviour changes. No perf rows (out of scope per planning answer).

Exemplars (harness glue to copy): `packages/server/src/__tests__/cors.test.ts` (pure helper), `ws-upgrade-routing.test.ts` + `ws-origin-gate.test.ts` (real upgrade handler, live config), `config-api.test.ts` (`writeConfigPartial`), `csp.test.ts` (env-mode resolver), `pairing.test.ts` (`fastify.inject` REST), `packages/shared/src/__tests__/config-openspec.test.ts` (loader), `packages/client/src/components/settings/__tests__/settings-field-contract.test.tsx` + `settings-page-composition.test.tsx` (Settings sections), `packages/client/src/components/Gateway/__tests__/Gateway.test.tsx` (row pills), `tests/e2e/security-pair-link.spec.ts` (Security tab e2e), `tests/e2e/gateway-url-action.spec.ts` (Gateway row e2e).

---

## Scenarios

### Edge-case

| id | requirement | technique | level | disposition | input | trigger | expected observable |
|----|-------------|-----------|-------|-------------|-------|---------|---------------------|
| E1 | host-admission / hostname-only matching | EP | L1 | automated | `opts` with `allowedHosts: ["dash.home.arpa"]` | `isHostAdmitted("dash.home.arpa:9443", opts)`, `("DASH.home.arpa.", opts)`, `("[::1]:8000", opts)` | all three `true` (port stripped, case-folded, trailing dot stripped, brackets stripped) |
| E2 | host-admission / fail closed | BVA (invalid partitions) | L1 | automated | `opts` admitting everything derivable | `isHostAdmitted(undefined)`, `("")`, `("evil.example/@localhost")`, `("a b")`, `(".local")`, `("127.1")`, `("2130706433")` | every call `false` |
| E3 | host-admission / IP literal rule | EP | L1 | automated | `opts.bindHost = "0.0.0.0"`, `trustedNetworks: []` | `isHostAdmitted` for `192.168.1.50:8000`, `[fe80::1]:8000`, `::ffff:127.0.0.1`, `10.4.0.9` | all `true`; and `isHostAdmitted("rebind.example", opts)` `false` |
| E4 | host-admission / bind-address name | EP | L1 | automated | `opts.bindHost = "dash-host"` | `isHostAdmitted("dash-host:8000")` and `("dash-host2:8000")` | `true`, then `false` |
| E5 | host-admission / `.local` label | BVA | L1 | automated | no config | `mac.local`, `a.b.local`, `.local`, `local`, `evil.localx` | `true`, `true`, `false`, `false`, `false` |
| E6 | host-admission / derived sources, first-match | decision-table | L1 | automated | `publicBaseUrls: ["https://pi.example.com:8443"]`, `cors.allowedOrigins: ["https://dash.example"]`, tunnel `["https://abc.share.zrok.io"]`, `allowedHosts: ["dash.home.arpa"]`, plus one unparseable entry `"not a url"` in each list | `isHostAdmitted` for each of the four hosts + `xyz.share.zrok.io` | four `true`, stranger `false`; no throw on the unparseable entries |
| E7 | host-admission / legacy pairing key | EP | L1 | automated | config with only `pairing.publicBaseUrls: ["https://old.example.com"]` | `livePublicBaseUrls()` → `isHostAdmitted("old.example.com")` | `true` |
| E8 | cross-site-request-gate / same-origin-by-Host tightened only in enforce | decision-table (mode × admissible) | L1 | automated | `origin = "http://rebind.example:8000"`, `host = "rebind.example:8000"`; `mac.local` pair | `isSameOriginByHost(o, h, {hostGateMode})` for `report` / `enforce` × rebind / mac.local | rebind: `true` in report, `false` in enforce; mac.local: `true` in both |
| E9 | host-admission / mode resolution | decision-table | L1 | automated | env ∈ {unset, `enforce`, `report`, `yes`} × config ∈ {absent, `enforce`} | `resolveHostGateMode(env, cfg)` | unset+absent→`report`,false · `enforce`+absent→`enforce`,true · `report`+`enforce`→`report`,true · `yes`+absent→`report`,false · `yes`+`enforce`→`enforce`,false; `yes` produces exactly one boot log line naming `PI_DASHBOARD_HOST_GATE` |
| E10 | shared-config / `allowedHosts` loader | EP | L1 | automated | `config.json` without key; with `["dash.home.arpa", 42, null]` | `loadConfig()` | `[]`; `["dash.home.arpa"]`; `ensureConfig()` output has no `allowedHosts` key |
| E11 | shared-config / `hostGate` loader + partial write | EP | L1 | automated | absent; `{mode:"yes"}`; `PUT {hostGate:{mode:"enforce"}}` on a file with `auth` + `allowedHosts` set | `loadConfig()`, `writeConfigPartial` | `report`; `report`; file holds `hostGate.mode: "enforce"` and `auth`, `allowedHosts` byte-identical |
| E12 | shared-config / partial write preserves `allowedHosts` | EP | L1 | automated | file with `allowedHosts: ["a"]` | `writeConfigPartial({ auth: {...} })` then `writeConfigPartial({ allowedHosts: ["b"] })` | after first: `["a"]` intact; after second: `["b"]` (replaced whole) |
| E13 | host-admission / REST enforce refusals | EP | L1 | automated | `PI_DASHBOARD_HOST_GATE=enforce`, `fastify.inject` | `GET /api/sessions` `Host: rebind.example:8000` no Origin; `POST /api/tunnel-connect` same Host + matching Origin; `GET /api/health` with no `Host` | each `403`, body `error:"host_not_allowed"` with `hint` containing `allowedHosts` and `publicBaseUrls`, no `access-control-allow-origin` header; route handler spy not called |
| E14 | host-admission / REST admits in both modes | EP | L1 | automated | mode ∈ {report, enforce} | `GET /api/health` with `Host:` `localhost:8000`, `127.0.0.1`, `192.168.1.50:8000`, `mac.local:8000`, `pi.example.com` (publicBaseUrls), `dash.home.arpa` (allowedHosts) | `200` for every pair |
| E15 | host-admission / report mode proceeds + logs | EP | L1 | automated | env unset, `hostGate` absent | `GET /api/health` `Host: rebind.example` | `200`; log contains exactly one line matching `[host-gate] would-refuse host=rebind.example` |
| E16 | host-admission / live mode + live allowedHosts | state-transition | L1 | automated | server running, env unset | write `hostGate.mode: "enforce"` to temp config → request `Host: rebind.example`; append `rebind.example` to `allowedHosts` → same request; remove `https://pi.example.com` from `publicBaseUrls` → `Host: pi.example.com` | `403`; then `200`; then `403` — no restart, no explicit invalidate |
| E17 | host-admission / WS enforce refusal | EP | L1 | automated | enforce; real `upgrade` handler; valid single-use ticket on the URL | upgrade `/ws` `Host: rebind.example:8000` + `Origin: http://rebind.example:8000` | socket receives `HTTP/1.1 403`, destroyed; ticket store still holds the ticket; log line has `[host-gate] refused` and `scope=browser` |
| E18 | cross-site-request-gate / WS report mode unchanged | EP | L1 | automated | report; real handler | upgrade `/ws` `Host: proxy-int.corp:8000` + matching Origin; upgrade `/ws` `Host: mac.local:8000` + matching Origin | both proceed past the Origin gate (existing admission runs); one `would-refuse` line for `proxy-int.corp`, none for `mac.local` |
| E19 | host-admission / Accept negotiation | decision-table | L1 | automated | enforce, `Host: rebind.example:8000`, `GET /` | `Accept` ∈ {`text/html,*/*;q=0.8`, `application/json`, `application/json, text/html;q=0.9`, `*/*`, absent} | first → `403 text/html`; other four → `403 application/json` |
| E20 | host-admission / HTML page content | EP | L1 | automated | enforce; live tunnel `https://abc.share.zrok.io`; `allowedHosts: ["dash.home.arpa"]`; port 8000 | `GET /` `Host: rebind.example:8000` `Accept: text/html` | body contains `rebind.example:8000`, `localhost:8000`, `allowedHosts`, `publicBaseUrls`; contains none of `<script`, `/assets/`, `abc.share.zrok.io`, `dash.home.arpa` |
| E21 | host-admission / HTML escaping | EP (injection) | L1 | automated | enforce | `GET /` `Host: a<img>.example` `Accept: text/html` | `403`; body contains `a&lt;img&gt;.example`, not `<img>` |
| E22 | host-admission / GET /api/host-gate shape | EP | L1 | automated | `publicBaseUrls: ["https://pi.example.com"]`, `allowedHosts: ["dash.home.arpa"]`, tunnel `abc.share.zrok.io`, env `report` + config `enforce` | `GET /api/host-gate` from loopback | `mode:"report"`, `envOverridden:true`, `admitted` contains `{pi.example.com, public-base-url}`, `{dash.home.arpa, allowed-host}`, `{abc.share.zrok.io, live-tunnel}`, exactly one `local` and one `ip-address` row; `recent: []` |
| E23 | host-admission / ring counts every refusal (BVA at 50) | BVA | L1 | automated | report mode | 3× `Host: rebind.example:8000` + 1× `proxy-int.corp` within 1 s; then 50 further distinct hosts; then `rebind.example` once more | after first burst: `rebind.example` `count:3` (no port), `proxy-int.corp` `count:1`, both `would-refuse`; after 50 distinct: `recent.length === 50`, `proxy-int.corp` evicted (least-recently-seen), `rebind.example` still present with `count:4` and first in order |
| E24 | host-admission / ring malformed key + admitted stops counting | EP | L1 | automated | report | 2 requests with no `Host`; then append `rebind.example` to `allowedHosts` and request it | `recent` has `{host:"(malformed)", count:2}`; `rebind.example` count unchanged after admission |
| E25 | host-admission / endpoint auth parity | EP | L1 | automated | OAuth configured, non-local peer, no cookie | `GET /api/host-gate` | same status as `GET /api/config` under the same request (`401`/`403`) |
| E26 | settings-panel / textarea validation | EP | L1 | automated | `AllowedHostsSection` mounted with empty draft | blur after entering `https://dash.home.arpa:9443/`; `my_service.docker`; `dash.home.arpa` | field `aria-invalid=true` + message contains `dash.home.arpa`; `aria-invalid=true` + message says cannot be used; valid, draft `allowedHosts = ["dash.home.arpa"]` |
| E27 | settings-panel / Save diff carries new fields | EP | L1 | automated | original config `{hostGate:{mode:"report"}, allowedHosts:[]}`; draft `{hostGate:{mode:"enforce"}, allowedHosts:["a"]}` | `computeConfigPartial(draft, original)` | partial `=== { hostGate:{mode:"enforce"}, allowedHosts:["a"] }`; `CONFIG_FIELD_PAGE.allowedHosts === "security"` and `.hostGate === "security"` |

### Frontend-quirk

| id | requirement | technique | level | disposition | input | trigger | expected observable (invariant) |
|----|-------------|-----------|-------|-------------|-------|---------|---------------------------------|
| F1 | settings-panel / section composition | state-transition | L1 | automated | Security tab rendered with `GET /api/host-gate` mocked `{mode:"report", envOverridden:false, admitted:[…], recent:[]}` | render | section heading `Allowed hostnames` sits after `Trusted Networks` and before `Pair a device` in DOM order; radios `Report only` (checked) and `Enforce`; "No refusals since start." |
| F2 | settings-panel / mode consequence inline, no confirm | state-transition | L1 | automated | `window.confirm` spied | click `Enforce` | consequence text changes to mention `403`; `confirm` not called; draft `hostGate.mode === "enforce"`; no fetch issued |
| F3 | settings-panel / env override disables | EP | L1 | automated | mock `envOverridden:true, mode:"report"` | render | both radios `disabled`; a line contains `PI_DASHBOARD_HOST_GATE` |
| F4 | settings-panel / admitted rows from endpoint | EP | L1 | automated | mock `admitted: [{host:"pi.example.com", source:"public-base-url"}, {host:"*.local", source:"local"}, {host:"any IP address", source:"ip-address"}]` | render | row `pi.example.com` has pill text `public base URL` + link to `/gateway`, no remove button; pattern rows have no link; client never calls a derive helper (no `isHostAdmitted` import) |
| F5 | settings-panel / Allow is draft-only | state-convergence | L1 | automated | mock `recent: [{host:"proxy-int.corp", count:1, outcome:"would-refuse"}]`, `fetch` spied | click `Allow proxy-int.corp` | textarea value contains `proxy-int.corp`; row gone; panel dirty indicator on; zero `PUT` requests; after simulated Save the `PUT /api/config` body has `allowedHosts: ["proxy-int.corp"]` |
| F6 | settings-panel / hidden rows survive refetch | state-convergence | L1 | automated | draft `allowedHosts: ["proxy-int.corp"]`; endpoint refetched returning the same `recent` row | re-render after refetch | row still hidden (filter applied at render) |
| F7 | settings-panel / outcome pill per entry | EP | L1 | automated | mock `recent: [{a.example, would-refuse}, {b.example, refused}]`, mode control set to `enforce` in draft | render | pills read `would-refuse` and `refused` respectively (not both `refused`) |
| F8 | settings-panel / gateway pill | decision-table (status × in-list) | L1 | automated | rows: `ok` + in `publicBaseUrls`; `incomplete` with public URL missing; `incomplete` with only `trustedNetworks` delta missing and public URL intact | render `GatewayUrlManager` | pill `Host admitted` present, absent, present; pill has no `role=button`; `title` names the hostname |
| F9 | settings-panel / end-to-end Allow → Save → admitted | state-convergence | L3 | automated | harness dashboard; `curl -H 'Host: proxy-int.corp' http://localhost:<port>/api/health` once (report mode) | open Settings ▸ Security; click `Allow proxy-int.corp`; click Save; wait for refetch | Recent refusals no longer lists it; Currently admitted shows `proxy-int.corp` with pill `allowed host`; harness `config.json` `allowedHosts` contains it |
| F10 | settings-panel / keyboard-only pass | a11y traversal | L3 | automated | Security tab open | Tab from `Trusted Networks` textarea | focus reaches mode radio → textarea → each `Allow` button in order; every stop has an accessible name; `axe` reports no serious/critical violation on the section |
| F11 | settings-panel / mockup fidelity in both themes | visual/subjective | — | manual-only | live dashboard, dark + light | operator compares section against `mockups/security-allowed-hosts.html` | [judgment: spacing / pill colour families read as designed — no automatable observable] |
| F12 | host-admission / HTML refusal page readability | visual/subjective | — | manual-only | `PI_DASHBOARD_HOST_GATE=enforce`, browser at `http://<unlisted-name>:8000` | operator views the 403 page in light and dark OS scheme | [judgment: page legible, three ways in scannable — no automatable observable] |

### Error-handling

| id | requirement | technique | level | disposition | fault | trigger | expected observable |
|----|-------------|-----------|-------|-------------|-------|---------|---------------------|
| X1 | host-admission / log sanitisation | fault-injection (hostile input) | L1 | automated | `Host: "rebind.example\r\n[host-gate] refused host=forged"` | report mode request | exactly one `[host-gate]` line; it contains neither `\n` nor `forged`; ring key is `(malformed)` (fails the positive regex) |
| X2 | host-admission / per-host rate limit | fault-injection (flood) | L1 | automated | 100 requests `Host: rebind.example` in <1 s | report mode | ≤1 `[host-gate]` line for `rebind.example`; ring `count:100` |
| X3 | host-admission / distinct-host scan cap (BVA at 60/256) | BVA + flood | L1 | automated | 10 000 requests with distinct `a<n>.rebind.example` in <1 s (fake timers) | report mode | ≤60 refusal lines + exactly one `[host-gate] suppressed 9940 refusal lines`; limiter map size ≤256; ring length 50 |
| X4 | host-admission / live-tunnel source unavailable | fault-injection (abort) | L1 | automated | `getLiveTunnelOrigins` throws | enforce; `GET /api/health` `Host: localhost:8000` and `Host: abc.share.zrok.io` | `200` for localhost (rule 1 short-circuits); `403` for the tunnel host (source treated as empty, no 500) |
| X5 | host-admission / config file unreadable mid-run | fault-injection (abort) | L1 | automated | temp `config.json` replaced by invalid JSON while running | enforce; `GET /api/health` `Host: dash.home.arpa` (was in `allowedHosts`) | no `500`; decision uses the last good snapshot (existing `config-snapshot` contract) → `200`; a parse-error log line exists |
| X6 | settings-panel / endpoint failure | fault-injection (abort) | L1 | automated | `GET /api/host-gate` mocked `500` | render section | mode control + textarea still render and are editable; admitted/recent areas show an error line naming the endpoint; no uncaught rejection |
| X7 | cross-site-request-gate / regression #E6 + #E16 | regression | L1 | automated | enforce; real upgrade handler + `fastify.inject` | `/ws` `Host: mac.local:8000` + `Origin: http://mac.local:8000`; `POST /api/pair/challenge` from LAN peer `Host: 192.168.1.20:8000` no Origin | upgrade completes; pairing responds `200` (not `403`) |

---

## Coverage summary

- Requirements covered: 11/11 (host-admission ×5, shared-config ×3, cross-site-request-gate ×1, settings-panel ×2)
- Scenarios by class: edge 27 · perf 0 · frontend 12 · error 7
- Scenarios by level: L1 40 · L2 0 · L3 2 · — 2
- Scenarios by disposition: automated 44 · manual-only 2

## New infra needed

- none (L1 patterns exist for every server/client row; L3 rows extend `tests/e2e/security-pair-link.spec.ts`'s Security-tab harness glue with an out-of-band `curl` to seed a refusal — same technique as existing specs that mutate harness state)
