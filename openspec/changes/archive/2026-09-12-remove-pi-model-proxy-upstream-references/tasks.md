## 1. Plugin service probe → `model-proxy` (dashboard-plugin-runtime)

- [x] 1.1 Red tests first in `packages/dashboard-plugin-runtime/src/__tests__/requirement-probes.test.ts` (extend the existing `services` describe block as harness exemplar): dep `() => true` + global `fetch` spy → `services[0]` deep-equals `{ name: "model-proxy", satisfied: true }` and spy never called (test-plan E1); dep `() => false` → `{ satisfied: false, error: "model proxy disabled" }` and `missingFromReport` contains `"model-proxy"` (E2); dep key absent → `error: "probe not wired"` (E3); `services: ["pi-model-proxy"]` → `error: "unknown service name"` (E4); `services: ["model-proxy", "pi-model-proxy"]` → `missingRequirements` deep-equals `["pi-model-proxy"]` (E5); `let v = true` closure, flip to `false` between two calls with different plugin ids → first `satisfied: true`, second `false` (E6). Verify they fail.
- [x] 1.2 Add `isModelProxyEnabled?: () => boolean` to `RequirementProbeDeps`; replace `KNOWN_SERVICES["pi-model-proxy"]` with `"model-proxy"` backed by that dep (design D1). Verify 1.1 passes.
- [x] 1.3 Rename `service-probes/pi-model-proxy.ts` → `service-probes/model-proxy.ts`; delete `detectPiModelProxy`, `ProxyDetection`, `PROXY_MODEL_PREFERENCE`, `pickProxyDefaultModel`, `PROXY_MODELS_URL`, and `RequirementProbeDeps.fetchImpl` (design D3; drop the `fetchImpl` fixtures in `requirement-probes.test.ts`); keep only the config-backed probe fn. Update `server/index.ts` barrel export + `manifest-types.ts` comment (`"model-proxy" only`). Verify `rg -n "pi-model-proxy" packages/dashboard-plugin-runtime/src` returns only test assertions for the unknown-name case.
- [x] 1.4 Update `plugin-status-store-probe.test.ts` and `manifest-validator-requires.test.ts` fixtures from `pi-model-proxy` to `model-proxy`. Verify `npx vitest run packages/dashboard-plugin-runtime` green.
- [x] 1.5 In `packages/server/src/server.ts` declare `let modelProxyMounted = false` at `createServer` scope (above the ~1748 `setCompleteListener` callback — NOT inside the nested Model Proxy block, which is block-scoped), assign `modelProxyMounted = fullCfg.modelProxy.enabled` inside that block (~1941), and inject `isModelProxyEnabled: () => modelProxyMounted` at both `RequirementProbeDeps` sites (post-install `refreshRequirementProbesFor(null, {...})` ~1748 and loader `requirementDeps` ~2286) — boot-time value, NOT a live `loadConfig()` read (design D1). Verify by grepping both sites carry the dep and `npx vitest run packages/server/src/__tests__/plugin` (or the nearest loader test) is green.
- [x] 1.5b Third site: add optional `isModelProxyEnabled?: () => boolean` to the `deps` option type of `registerRecommendedRoutes` (optional so the 20 existing `setupRoute` calls in `recommended-routes.test.ts` keep compiling under `tsc --noEmit`) (`packages/server/src/routes/recommended-routes.ts:~318`, currently `{ packageManagerWrapper }` only), forward it into `reqDeps` (~348), and pass `() => modelProxyMounted` at the `server.ts:~1791` call site. Red test first, in a NEW file `packages/server/src/__tests__/recommended-routes-service-probe.test.ts` (the existing `recommended-routes.test.ts` imports the real manifest and no entry declares `services`): `vi.mock("@blackbelt-technology/pi-dashboard-shared/recommended-extensions.js", () => ({ RECOMMENDED_EXTENSIONS: [...] }))` — the exact subpath specifier `recommended-routes.ts:~37` imports (root-package mock silently no-ops; precedent `tunnel-ngrok.test.ts:17`) — with one entry declaring `requires: { services: ["model-proxy"] }`; register with `isModelProxyEnabled: () => true`, assert the enriched entry has `requirements.services[0].satisfied === true` and no `missingRequirements`; call `invalidateRecommendedCache()` (module-level 60 s cache, see `recommended-routes.test.ts:~213` `beforeEach`), then a second case with `() => false` asserts `missingRequirements` deep-equals `["model-proxy"]` (app-build glue: `setupRoute` ~241) (test-plan E7); third case `isModelProxyEnabled: () => { throw new Error("boom") }` → HTTP 200, entry returned with `requirements === undefined` and no `missingRequirements` (test-plan X1). Verify `rg -n "isModelProxyEnabled" packages/server/src --glob '!**/__tests__/**'` returns exactly 4 hits: the option typedef in `recommended-routes.ts`, and the 3 injection sites in `server.ts` (~1748, ~1791, ~2286).
- [x] 1.5c Wiring source-assertion test, new file `packages/server/src/__tests__/model-proxy-probe-wiring.test.ts` (see `packages/server/src/__tests__/plugin-spawn-scope-env.test.ts:~84` for the read-`server.ts`-source pattern): input = `server.ts` text; trigger = locate the 3 `RequirementProbeDeps` sites (post-install `refreshRequirementProbesFor(`, `registerRecommendedRoutes(`, loader `requirementDeps:`); observable = each contains `isModelProxyEnabled: () => modelProxyMounted`, `let modelProxyMounted` is declared at `createServer` scope above the first site, and the assignment `modelProxyMounted = fullCfg.modelProxy.enabled` appears exactly once, inside the Model Proxy block (test-plan E8, X2).
- [x] 1.5d Barrel-API test in `packages/dashboard-plugin-runtime/src/__tests__/requirement-probes.test.ts` (same file as 1.1): `import * as srv from "../server/index.js"` → `srv.detectPiModelProxy`, `srv.pickProxyDefaultModel`, `srv.PROXY_MODEL_PREFERENCE`, `srv.PROXY_MODELS_URL` all `undefined`; a `// @ts-expect-error` line assigning `fetchImpl` into a `RequirementProbeDeps` literal (test-plan E9).
- [x] 1.6 Update `packages/dashboard-plugin-runtime/src/server/service-probes/AGENTS.md`, `server/AGENTS.md` rows; add `## [Unreleased]` CHANGELOG entry naming the `pi-model-proxy` → `model-proxy` rename and the removed barrel exports (incl. `RequirementProbeDeps.fetchImpl`).

## 2. Recommended manifest (shared)

- [x] 2.1 Red test: in `packages/shared/src/__tests__/recommended-extensions.test.ts` (existing closed-registry guard at ~122 is the exemplar) assert no entry has `id`/`source` containing `@blackbelt-technology/pi-model-proxy` and no entry `description` contains the bare string `pi-model-proxy` (test-plan E10); drop it from the two expected-id lists (~20, ~171) and update the `KNOWN_SERVICES` set (~125) to `["model-proxy"]` so every declared `requires.services` ∈ `{"model-proxy"}` (E11). Verify fails.
- [x] 2.2 Delete the manifest entry in `packages/shared/src/recommended-extensions.ts` (~322-335). Verify 2.1 passes; `npx vitest run packages/shared` green.

## 3. Pi-core package list (server)

- [x] 3.1 Red test in `packages/server/src/__tests__/pi-core-checker.test.ts` (existing mocked-`npm list -g --json` cases are the exemplar): `CORE_PACKAGE_NAMES.length === 3`, excludes `@blackbelt-technology/pi-model-proxy`, `DISPLAY_NAMES` has no such key (test-plan E12); mocked `npm list -g --json` containing `@blackbelt-technology/pi-model-proxy@0.2.0` plus the 3 core packages → status `packages[]` has exactly 3 rows, none named `@blackbelt-technology/pi-model-proxy` (E13). Verify fails.
- [x] 3.1b Regression re-pin in the existing `usePackageOperations` / `coreUpdate` client test (`rg -l coreUpdate packages/client/src --glob '*.test.*'` for the exemplar): Update All with the 3 remaining core names → exactly 3 enqueues, sources `pi-core:<name>`, none containing `pi-model-proxy` (test-plan E16).
- [x] 3.2 Remove the entry from `CORE_PACKAGE_NAMES` and `DISPLAY_NAMES` in `packages/server/src/pi/pi-core-checker.ts`; fix the header comment (line ~5) and `packages/server/src/routes/pi-core-routes.ts:9` comment; fix `packages/client/src/components/packages/UnifiedPackagesSection.tsx:99` comment. Verify 3.1 passes.
- [x] 3.3 Update `packages/server/src/pi/AGENTS.md` row for `pi-core-checker.ts`.

## 4. Coexistence advisory (client)

- [x] 4.0 Red tests in `packages/client/src/__tests__/ModelProxySection.test.tsx` (existing `renderSection` helper is the exemplar): render with the full current prop set → no text matching `/upstream/i` and no `<code>:9876</code>` inside a Note; a `// @ts-expect-error` render passing `upstreamExtensionDetected` (test-plan E14); regression: `secondPort: 9876` config, change input to `9877` → `onChange` called with `objectContaining({ secondPort: 9877 })` and placeholder still "e.g. 9876" (E15). Verify E14 fails.
- [x] 4.1 Remove `upstreamExtensionDetected` from `Props` and the banner block (~lines 511-525) in `packages/client/src/components/settings/ModelProxySection.tsx`. Verify `tsc --noEmit -p packages/client` reports the now-unused prop at the `SettingsPanel.tsx:1959` call site (expected red) and 4.0 passes.
- [x] 4.2 In `SettingsPanel.tsx` remove the `useInstalledPackages("global")` call, `upstreamPiModelProxyInstalled`, and the prop pass (design D4); drop the `useInstalledPackages` import if unused. Verify `tsc` clean and `rg -n "upstreamPiModelProxyInstalled|upstreamExtensionDetected" packages/client/src/components/settings/SettingsPanel.tsx` empty. Do NOT touch the second `useInstalledPackages("global")` at ~3260 — it belongs to `GlobalPackagesBrowseAndDialogs`, a different component in the same file. Source-assertion test alongside 4.0 (pattern: `plugin-spawn-scope-env.test.ts:~84`): `SettingsPanel.tsx` text contains exactly one `useInstalledPackages("global")` and zero `upstreamPiModelProxyInstalled` (test-plan F1).
- [x] 4.3 Remove the 7 banner-only i18n keys (`common.note`, `common.theUpstream`, `packages.extensionIsAlsoActiveInOne`, `common.whileTheUpstreamUses`, `common.consider`, `packages.disablingTheUpstreamExtension`, `common.toAvoidDuplicateListeners`) from all four locale sources `packages/client/src/lib/i18n-en-source.json`, `packages/client/src/lib/i18n/i18n-hu.ts`, `packages/client/src/lib/i18n/i18n.tsx` (zh-CN), `packages/client/src/lib/i18n/i18n-legacy-aliases.ts` — re-verify each key has no other consumer (`rg '"<key>"' packages/client/src --glob '!**/i18n*'`) before deleting; keep `common.eG9876` (secondPort placeholder). Test (new `packages/client/src/lib/__tests__/i18n-orphans.test.ts`, or extend the nearest `i18n*.test.ts` — `rg -l i18n packages/client/src --glob '*.test.*'` for the exemplar): each of the 7 keys appears 0 times across the four catalogs and `i18n-legacy-aliases.ts` (as alias source or target); `common.eG9876` present in all catalogs; `node scripts/i18n-parity.mjs` exits 0 (test-plan E17).
- [x] 4.4 Update `ModelProxySection.tsx.AGENTS.md` and the `settings/AGENTS.md` row for `SettingsPanel.tsx`. Verify `npx vitest run packages/client/src/components/settings` green.

## 5. Docs (DocScribe-delegated)

- [x] 5.1 Spawn `DocScribe`: delete `docs/migration/from-pi-model-proxy.md` + `docs/migration/from-pi-model-proxy.md.AGENTS.md`; remove the row from `docs/AGENTS.md`; remove the migration link paragraph at `README.md:414`; reword `README.md:~551` (`pi-anthropic-messages` row) "pi-model-proxy providers" → "dashboard model proxy providers" (design D7); rewrite `docs/architecture.md:~746` (probe now `service-probes/model-proxy.ts`, boot-time `isModelProxyEnabled` dep) and `:~2002` (drop pi-model-proxy from the pi-core list). Verify `rg -n "pi-model-proxy" README.md docs/ --glob '!docs/qa/**'` empty and `ls docs/migration/` has no leftover empty dir (remove it if empty).
- [x] 5.1b Main agent: reword the `pi-anthropic-messages` entry description in `packages/shared/src/recommended-extensions.ts:~151` the same way (D7); edit the `openspec/specs/model-proxy/spec.md` Purpose preamble to drop the "coexists with upstream `@blackbelt-technology/pi-model-proxy`" clause (design D6 — sync cannot reach it). Verify `rg -n "pi-model-proxy" packages/shared/src/recommended-extensions.ts openspec/specs/model-proxy/spec.md` empty.
- [x] 5.2 Run `kb dox lint` (or the repo's DOX lint) and fix any stale/orphan rows the deletion created.

## 6. Verification

- [x] 6.0 Repo-surface test, new `packages/server/src/__tests__/no-upstream-model-proxy-refs.test.ts` (exemplar for `execSync`-in-vitest: `plugin-spawn-scope-env.test.ts`): run the 6.1 `rg` from the repo root with identical globs; assert every match path is under `packages/server/src/model-proxy/convert/`, equals `packages/server/src/model-proxy/UPSTREAM.md`, `openspec/changes/add-package-health-cleanup/proposal.md`, or is under `packages/dashboard-plugin-runtime/src/__tests__/`; assert `docs/migration/from-pi-model-proxy.md` does not exist (test-plan E18).
- [x] 6.1 `rg -n "pi-model-proxy" --glob '!node_modules' --glob '!pnpm-lock.yaml' --glob '!openspec/changes/archive/**' --glob '!openspec/specs/**' --glob '!openspec/groups/**' --glob '!docs/qa/**' --glob '!Prompt stories/**' --glob '!CHANGELOG.md' --glob '!openspec/changes/remove-pi-model-proxy-upstream-references/**'` (unscoped name, so bare mentions are caught too) returns only: `packages/server/src/model-proxy/convert/*` attribution headers + `UPSTREAM.md`, `openspec/changes/add-package-health-cleanup/proposal.md`, and runtime test assertions for the unknown-service-name case. `openspec/specs/**` is excluded because archive sync updates it; the preamble edit in 5.1b is verified there.
- [x] 6.2 `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log; grep -nE 'FAIL|Tests +[0-9]+ (failed|passed)' /tmp/pi-test.log` — all green.
- [x] 6.3 `npm run quality:changed` clean; run `doubt-driven-review` on the spec removal + service rename, then `review-code` on the diff before commit.
- [x] 6.4 Manual (test-plan: manual-only, F3): `/api/restart`, open Settings → Model Proxy with the upstream extension still registered in `~/.pi/agent/settings.json#packages` → section layout unchanged apart from the missing amber Note, nothing looks orphaned; also eyeball `GET /api/pi-core/status` has no `pi-model-proxy` row.
- [x] 6.5 Manual (test-plan: manual-only, F2): read `README.md` proxy section and the `docs/architecture.md` probe paragraph end-to-end — prose reads as a complete story without the migration link.
