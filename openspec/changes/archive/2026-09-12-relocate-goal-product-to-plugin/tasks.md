## 1. Widen ServerPluginContext (core; goal still runs in core — no behaviour change, landable alone)

- [x] 1.1 Add `spawnToken?`, `resume?: { sessionFile }`, `initialPrompt?` to `PluginSpawnOptions` and `mintSpawnToken`, `renameSession`, `assignSessionRef(sessionId, ref, opts?: { persist?: boolean })`, `networkGuard`, `onShutdown` to `ServerPluginContext` in `packages/dashboard-plugin-runtime/src/server/server-context.ts`; extend `pluginSpawnToSessionOptions` to map `resume` → session-level `{ sessionFile, mode: "continue" }` without touching the isolation `mode`. Verify: `npx tsc -p packages/dashboard-plugin-runtime --noEmit` passes.
- [x] 1.2 Add non-destructive `has(token)` to `PendingPluginRefRegistry` in `packages/server/src/pending/pending-plugin-ref-registry.ts`.
- [x] 1.3 Test (test-plan #E16, L1, see `packages/server/src/__tests__/pending-plugin-ref-registry.test.ts`): file `tok-A` · call `has` → `resolve` → `has`, and `has` after `PENDING_PLUGIN_REF_TTL_MS` on fake timers · `true` → ref returned intact → `false`; expired → `false`.
- [x] 1.4 Implement the spawn widenings in `server.ts` plugin `spawnSession`: honour caller `spawnToken` (trusted-only; reject with `{ success:false, message }` when `pendingPluginRefRegistry.has(token)`), enqueue `initialPrompt` via `pendingInitialPromptRegistry` before spawn and consume it on failure/throw, alongside `remove(token)`.
- [x] 1.5 Test (test-plan #E1, L1, see `packages/server/src/__tests__/plugin-spawn-scope-env.test.ts`): trusted plugin with `spawnToken` "1111…" + `pluginRef {goalId}` · `ctx.spawnSession` · `spawnPiSession` gets that token, registry filed under it with owner `goal` before the await, `result.spawnToken` equals it.
- [x] 1.6 Test (test-plan #E2, L1, same exemplar): registry holds `tok-A` for owner `automation`; plugin passes `spawnToken: "tok-A"` · `ctx.spawnSession` · `{ success:false, message }`, `spawnPiSession` not called, `resolve("tok-A").ownerId === "automation"`.
- [x] 1.7 Test (test-plan #E3, L1, same exemplar): decision table trusted∈{100,1000} × token∈{supplied,absent} · `ctx.spawnSession` · untrusted → `{success:false}` and registry never filed; trusted+absent → fresh UUID v4 `result.spawnToken`.
- [x] 1.8 Test (test-plan #E4, L1, same exemplar): `{ resume: { sessionFile: "/tmp/s.jsonl" }, mode: "worktree" }` · `pluginSpawnToSessionOptions` · output has `sessionFile` + `mode: "continue"` and the worktree isolation flag still mapped.
- [x] 1.9 Test (test-plan #E5, L1, same exemplar): `resume.sessionFile` `""` and `42` · `pluginSpawnToSessionOptions` · no throw, no `sessionFile` key, no `mode: "continue"`.
- [x] 1.10 Test (test-plan #E6, L1, same exemplar): `initialPrompt: "/goal reprime"`, spawn succeeds · `ctx.spawnSession` · `enqueue(cwd, prompt)` call-ordered before `spawnPiSession`; queue still holds it after success.
- [x] 1.11 Test (test-plan #E7, L1, see `packages/server/src/__tests__/pending-initial-prompt-registry.test.ts` for the cap glue): 8 prompts queued for `cwd`, 9th spawn with `initialPrompt` · `ctx.spawnSession` · `enqueue` returns `false`, spawn still `success:true`, queue length stays 8.
- [x] 1.12 Test (test-plan #E15, L1, see `plugin-spawn-scope-env.test.ts`): trusted plugin · `ctx.mintSpawnToken()` twice · both UUID v4, values differ.
- [x] 1.13 Test (test-plan #X1, L1, same exemplar): `spawnPiSession` resolves `{success:false}` · `ctx.spawnSession({ spawnToken: "tok-A", initialPrompt })` · `has("tok-A") === false`, `consume(cwd)` called, result `{success:false}`.
- [x] 1.14 Test (test-plan #X2, L1, same exemplar): `spawnPiSession` rejects · `ctx.spawnSession({ initialPrompt })` · ref removed, prompt consumed, `{ success:false, message }`, no unhandled rejection.
- [x] 1.15 Implement `ctx.renameSession` (update name + `broadcastSessionUpdated` + `rename_session` to pi; trusted-gated; `false` for unknown session / empty name), `ctx.assignSessionRef` (`sanitizePluginRef` with reserved keys + keyOwners → `sessionManager.update`; `persist !== false` → `mergeSessionMeta` + `broadcastSessionUpdated`; `undefined` clears each layer; `false` for unknown session / untrusted), `ctx.mintSpawnToken`, `ctx.networkGuard` (the core instance from `server.ts:1384`), `ctx.onShutdown` (subs dispatched at the `goalSupervisor?.dispose()` point, before `piGateway.stop()`, try/catch per sub, returns unsubscribe).
- [x] 1.16 Test (test-plan #E8, L1, new `packages/server/src/__tests__/plugin-ctx-session-mutations.test.ts` copying the fake-ctx glue of `plugin-spawn-scope-env.test.ts`): trusted plugin, live `s1` · `ctx.renameSession("s1", "goal: ship")` · `true`; in-memory name set; one `broadcastSessionUpdated`; one `rename_session` to pi.
- [x] 1.17 Test (test-plan #E9, L1, same file): untrusted / unknown id / empty name · `ctx.renameSession` · `false` each; no update, broadcast or pi message.
- [x] 1.18 Test (test-plan #E10, L1, same file): `s1` with `sessionFile` · `ctx.assignSessionRef("s1", { goalId: "g1" })` · `true`; in-memory `goalId`; `mergeSessionMeta({goalId:"g1"})` once; `broadcastSessionUpdated` once.
- [x] 1.19 Test (test-plan #E11, L1, same file): `s1` has `goalId` in memory + meta · `assignSessionRef("s1", { goalId: undefined })` · memory undefined; meta key removed; broadcast carries `goalId: undefined`.
- [x] 1.20 Test (test-plan #E12, L1, same file): `s1` has `goalId` in memory + meta · `assignSessionRef("s1", { goalId: undefined }, { persist: false })` · `true`; memory undefined; `mergeSessionMeta` not called; `broadcastSessionUpdated` not called.
- [x] 1.21 Test (test-plan #E13, L1, same file): `{ status: "ended", name: "x", goalId: "g1" }` · `assignSessionRef` · `status`/`name` unchanged, `goalId` set, warn-once logged, returns `true`.
- [x] 1.22 Test (test-plan #E14, L1, same file): untrusted plugin / unknown session · `assignSessionRef` · `false`; no update, meta write or broadcast.
- [x] 1.23 Test (test-plan #X3, L1, same file): `mergeSessionMeta` throws `EACCES` · `assignSessionRef("s1", { goalId: "g1" })` · memory applied, broadcast sent, warn logged, returns `true`.
- [x] 1.24 Test (test-plan #X7, L1, same file): `piGateway.sendToSession` returns `false` · `ctx.renameSession("s1", "n")` · name + broadcast still applied, returns `true`.
- [x] 1.25 Test (test-plan #E17, L1, see `packages/server/src/__tests__/shutdown-terminates-any-strategy.test.ts`): two `onShutdown` subs, spy `piGateway.stop` · server `stop()` · both subs once, before `piGateway.stop()` and `fastify.close()`; an unsubscribed sub is not invoked.
- [x] 1.26 Test (test-plan #X4, L1, same exemplar): first sub throws · server `stop()` · `[plugin-onShutdown]` logged, second sub runs, `piGateway.stop()` still called, `stop()` resolves.
- [x] 1.27 Run `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` and confirm zero failures; core goal product still wired and green.

## 2. Relocate the goal product into goal-plugin (atomic with group 3 — one commit range)

- [x] 2.1 `git mv` `packages/server/src/goal/{goal-store,goal-supervisor,goal-status-projector,goal-verdict-accumulator,goal-session-primer,decorate-goals-spend,goal-budget-guard}.ts` → `packages/goal-plugin/src/server/` and `routes/goal-routes.ts` → `goal-plugin/src/server/routes.ts`; fix relative imports; replace the `mintSpawnToken` import in `goal-supervisor.ts` with an injected dep; re-declare `routes.ts`'s deps interface against `PluginSessionManager`, `ServerPluginContext["networkGuard"]` and a `knownFolderCwds: () => string[]` (route bodies unchanged). Verify: `npx tsc -p packages/goal-plugin --noEmit` passes with no `../auth`/`../persistence`/`../session`/`../routes` imports remaining.
- [x] 2.2 `git mv` the 8 core tests (`__tests__/goal-*.test.ts`, `decorate-goals-spend.test.ts`) → `packages/goal-plugin/src/server/__tests__/`, rewrite imports. Verify: `npx vitest run packages/goal-plugin` passes with the moved suites.
- [x] 2.3 Build the product composition root in `goal-plugin/src/server/index.ts`: `createGoalStore()`; `goals_update` via `ctx.broadcastToSubscribers` with `decorateGoalsWithSpend(goals, { get: id => ctx.sessionManager.getSession(id) })`; `goal_status` peers (verdict accumulator, status projector, budget guard) via `ctx.registerPiHandler`; primer on `ctx.sendToSession` / `ctx.renameSession`; keep the existing `goal_status` snapshot broadcast + `plugin_action` handler.
- [x] 2.4 Test (test-plan #E21, L1, see `packages/goal-plugin/src/server/__tests__/plugin-action-handler.test.ts` fake-ctx glue): `g1` linked to sessions `a` (cost 1.5) and `b` (cost 2) · store mutation · `ctx.broadcastToSubscribers` once with `{ type: "goals_update", cwd, goals }` and `goals[0].spend === 3.5`.
- [x] 2.5 Test (test-plan #E22, L1, same exemplar): plugin registered on fake `ctx`; synthetic `goal_status { verdict: "pass" }` from `s1` of `g1` · dispatch via captured `registerPiHandler("goal_status")` · verdict persisted on `g1`, projector updates `status`, budget-exceeded path halts as `goal-budget-guard.test.ts` asserts.
- [x] 2.6 Wire `spawnDriver` on `ctx.spawnSession({ cwd, model?, spawnToken, pluginRef: { goalId }, lifecycle: { recover: false }, resume?, initialPrompt? })`; supervisor deps `isSessionLive`/`resolveSessionFile` via `ctx.sessionManager.getSession` cast, `killByToken` → `ctx.abortSpawnedRun({ spawnToken })` then `killBySession` → `ctx.abortSpawnedRun({ sessionId })`, death fanout via `ctx.onSessionEnded`; 30 s `unref`'d boot-reconcile timer; `ctx.onShutdown(() => { clearTimeout(bootReconcileTimer); supervisor.dispose(); })`.
- [x] 2.7 Test (test-plan #E23, L1, see `goal-supervisor.test.ts`): `inFlightSpawn.spawnToken = "tok-A"`, `driverSessionId = "s1"`, `abortSpawnedRun` returns `false` then `true` · supervisor abort · called with `{ spawnToken }` first then `{ sessionId }`; when the first returns `true` the second is not called.
- [x] 2.8 Test (test-plan #X5, L1, same exemplar): shutdown sub has run (`supervisor.dispose()`) · `ctx.onSessionEnded` handler fires for the driver · `ctx.spawnSession` not called, no timer scheduled.
- [x] 2.9 Test (test-plan #X6, L1, same exemplar, fake timers): persisted `g1.inFlightSpawn = { spawnToken: "tok-old" }`, plugin registers on fresh `ctx`, no session registers · advance `30_000` ms · `reconcileOnBoot` runs, `ctx.spawnSession` once with a new minted token + `pluginRef { goalId: "g1" }`, `inFlightSpawn.spawnToken` updated.
- [x] 2.10 Register routes from `goal-plugin/src/server/routes.ts` on `ctx.fastify` under the unchanged `/api/folders/goals*` paths with `{ preHandler: ctx.networkGuard }`, `applyGoalIdToSession` → `ctx.assignSessionRef`, known-cwd check → `ctx.consume("host.knownFolderCwds")`. Verify: moved `goal-routes.test.ts` passes unchanged in REST paths + response shapes.
- [x] 2.11 Test (test-plan #E18, L1, see `packages/roles-plugin/src/server/__tests__/roles-routes.test.ts` for preHandler introspection + `goal-routes.test.ts` inject glue): routes mounted with `ctx.networkGuard`; inject from non-loopback without auth · `GET /api/folders/goals?cwd=/x` · `403`; `ctx.networkGuard` is the same reference core passes to `registerSessionRoutes`; all six routes report a `preHandler`.
- [x] 2.12 Test (test-plan #E19, L1, see `goal-routes.test.ts`): plugin registered on fresh fastify · route table · exactly today's six method+path pairs; nothing under `/api/plugins/goal/`.
- [x] 2.13 Test (test-plan #E20, L1, see `goal-routes.test.ts`): cwd ∈ {active-unpinned, pinned-inactive, neither} · `GET /api/folders/goals?cwd=…` · `200`, `200`, and today's `rejectInvalidCwd` status+body for neither.
- [x] 2.14 Implement the driver-link handler on `ctx.onSessionResolved` (port of `event-wiring.ts` `linkGoalDriver`: outgoing driver cleared via `assignSessionRef(prev, { goalId: undefined }, { persist: false })`, `replaceDriver`, clear `inFlightSpawn`, `assignSessionRef(new, { goalId })`, prime) with the `driverSessionId !== sessionId` guard.
- [x] 2.15 Test (test-plan #E24, L1, see `plugin-action-handler.test.ts` fake ctx): `g1.driverSessionId = "s1"` (dead), `inFlightSpawn` set · handler `("s2", { goalId: "g1" })` · `assignSessionRef("s1", {goalId: undefined}, {persist:false})`, `replaceDriver(cwd,"g1","s2")`, `inFlightSpawn` cleared, `assignSessionRef("s2", {goalId:"g1"})` persisted, primer to `s2` exactly once.
- [x] 2.16 Test (test-plan #E25, L1, same exemplar): `g1.driverSessionId = "s2"` · handler `("s2", { goalId: "g1" })` again · no `replaceDriver`, no primer, no `assignSessionRef`.
- [x] 2.17 Test (test-plan #E26, L1, see `plugin-spawn-scope-env.test.ts` register path + `packages/server/src/spawn-process/__tests__/headless-pid-registry-plugin-ref.test.ts`): `s1` pid-registry entry carries `pluginRef { goalId: "g1" }`, `g1.driverSessionId = "s2"`, `s1` re-registers with no pending token · `session_register` · `onSessionResolved` not dispatched to `goal`; `driverSessionId` still `s2`; no primer; `s1` in-memory `goalId` merged from the ref; and `g1.status` stays `respawning` (pins the accepted D2 `replaceDriver` status/baseline divergence).

## 3. Retire goal wiring from core (atomic with group 2)

- [x] 3.1 Remove from `server.ts`: the seven `./goal/*` imports, `registerGoalRoutes`, `GOAL_REF_OWNER`, `goalStore`, `goalSupervisor`, `spawnGoalDriver`, `primeGoalSessionImpl`, `applyGoalIdToSession`, the `goal_status` peer block, the death-fanout `onDriverDeath` call, dispose, and boot reconcile (the `onShutdown` dispatch stays at that point).
- [x] 3.2 Remove from `event-wiring.ts`: `goalStore`/`primeGoalSession` deps, `linkGoalDriver`, the post-register `goalId` block; update event-wiring tests.
- [x] 3.3 Delete `packages/server/src/goal/` (incl. `AGENTS.md` + `*.ts.AGENTS.md` sidecars) and `routes/goal-routes.ts`.
- [x] 3.4 Test (test-plan #E27, L1, new `packages/server/src/__tests__/core-goal-free.test.ts` using `fs` walk like `event-wiring-source-stamp.test.ts`): post-move tree · scan `packages/server/src` (excluding `AGENTS.md`) for `goal` · zero hits outside shared type re-exports; `goal/` dir and `routes/goal-routes.ts` absent.
- [x] 3.5 Test (test-plan #X8, ci, see `.github/workflows/ci.yml` test job): the PR diff · `git diff --name-status develop...HEAD` · relocated files added under `goal-plugin/src/server/` and deleted under `packages/server/src/goal/` + `routes/goal-routes.ts` in the same range; `npm test` boots the server without `FST_ERR_DUPLICATED_ROUTE`.
- [x] 3.6 Full suite: `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` → zero failures; `npm run quality:changed` clean; `kb dox lint` reports no orphan/missing rows for the removed paths.

## 4. Docs and verification

- [x] 4.1 Add per-file rows for the relocated modules, `routes.ts`, and moved tests to `packages/goal-plugin/src/server/AGENTS.md` (+ `__tests__/AGENTS.md`); update `packages/server/src/AGENTS.md`, `routes/AGENTS.md`, `pending/AGENTS.md` rows for removals/`has()`; update `dashboard-plugin-runtime/src/server/AGENTS.md` for the new capabilities. Verify: `kb dox lint` clean.
- [x] 4.2 DocScribe: `docs/architecture.md` goal section (product now plugin-hosted; core goal-agnostic) and `dashboard-plugin-skill/.pi/skills/dashboard-plugin-scaffold/references/server-context-api.md` entries for `mintSpawnToken`, `renameSession`, `assignSessionRef` (+ `persist`), `networkGuard`, `onShutdown`, spawn `spawnToken`/`resume`/`initialPrompt`, and the duplicate-token rejection. Verify: both files mention each capability by name.
- [x] 4.3 Test (test-plan #F1, L3, see `tests/e2e/bus-client-goal-plugin-action.spec.ts`): harness up · `POST /api/folders/goals { cwd, objective, spawn: true }` then poll sessions + goals · one session with that `goalId`, goal `driverSessionId` equals it, `.meta.json` has `goalId`, exactly one `/goal` primer prompt in server log.
- [x] 4.4 Test (test-plan #F2, L3, same spec): F1 state · kill the driver via the browser-bus `shutdown` message (real process death) · supervisor classifies (`respawns[]` + `respawning` + new `inFlightSpawn` token) · complete the respawned driver's register over a bridge socket with the persisted token · new session id with same `goalId` as `driverSessionId`, status `pursuing` (or `respawning` again if the harness's resumed pi crashes after the handover — its re-classified death under the same goal is correct); old session's in-memory `goalId` absent in `/api/sessions`; primer count on new session is 1.
- [x] 4.5 Test (test-plan #F3, L3, see `tests/e2e/keeper-restart-survival.spec.ts`): F1 state · restart the driver's keeper · `goalId` retained, `driverSessionId` unchanged, no second `/goal` prompt.
- [x] 4.6 Test (test-plan #F4, L3, see `bus-client-goal-plugin-action.spec.ts`): F1 state · today's unlink route · `/api/sessions` shows no `goalId`, `.meta.json` key removed, goal `driverSessionId` null.
- [x] 4.7 Test (test-plan #F5, L3, same spec): F1 state · driver emits `goal_status { verdict }` · `GET /api/folders/goals?cwd` carries verdict + status; goal file under `~/.pi/dashboard/goals/` has identical keys to a pre-change fixture.
- [x] 4.8 Manual (test-plan: manual-only, #F6) — deferred to post-merge verification: open the folder Goals page after relocation and compare with a pre-change screenshot — layout/labels unchanged.
- [x] 4.9 Run `review-code` on the full diff with the byte-identical contract checklist (REST paths/shapes, `.meta.json` keys, `goals_update`/`goal_status` wire types, spawn opts). Verify: no open Major findings.
