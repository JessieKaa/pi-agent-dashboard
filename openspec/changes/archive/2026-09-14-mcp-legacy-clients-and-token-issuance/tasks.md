## 1. Protocol era resolution (mcp-server-plugin)

- [x] 1.1 Test: decision table over method × header × `_meta` for `resolveProtocolVersion`, extend `packages/mcp-server-plugin/src/server/__tests__/protocol.test.ts` (see its existing E13/E14/E15 cases for the `meta()` helper) — every cell yields the D1 result or failure code, no cell throws; (absent header, absent or progressToken-only `_meta`, non-initialize) → legacy `2025-03-26`; `["a","b"]` header → `AmbiguousHeader`; (absent header, `_meta` 2026-07-28) → `MissingHeader`; (header 2026-07-28, no `_meta`) → `MissingMeta` (test-plan #E1)
- [x] 1.2 Test: modern strictness regression guard in `protocol.test.ts` — rerun E13/E14; rewrite E15 so (`_meta` 2026-07-28, no header) → `MissingHeader` (test-plan #X11)
- [x] 1.3 Implement `protocol.ts`: `LEGACY_PROTOCOL_VERSIONS`, `MODERN_PROTOCOL_VERSION`, `SUPPORTED_PROTOCOL_VERSIONS` = union, `AmbiguousHeader` failure, `resolveProtocolVersion(method, headers, params)` → `{ era, version }` per D1 rules 0–4; rewrite the Decision-10 docblock. Verify 1.1 + 1.2 pass.
- [x] 1.4 Test: legacy `initialize` answered, extend `packages/mcp-server-plugin/src/server/__tests__/routes.test.ts` (see its existing bearer + inject fixture) — `initialize` with `params.protocolVersion` ∈ {2025-03-26, 2025-06-18, 2025-11-25}, no header → 200, `protocolVersion` echoed, `capabilities` deep-equals `{tools:{listChanged:false}}`, `serverInfo.name` non-empty, `mcp-session-id` matches `/^[0-9a-f]{32}$/` (test-plan #E2)
- [x] 1.5 Test: initialize negotiates down in `routes.test.ts` — `params.protocolVersion: "2027-01-01"` → 200, `protocolVersion === "2025-11-25"`, no `error`, `mcp-session-id` present (test-plan #E3)
- [x] 1.6 Test: initialize without version refused in `routes.test.ts` — `params: {}` and `protocolVersion: 42` → `data.type === "UnsupportedProtocolVersion"`, message contains all four revisions, no `mcp-session-id` (test-plan #E4)
- [x] 1.7 Test: modern era refuses handshake in `routes.test.ts` — `initialize` with `protocolVersion: "2026-07-28"`; `ping` and `notifications/initialized` with header+`_meta` 2026-07-28 → 404, `-32601`, no `mcp-session-id` (test-plan #E5)
- [x] 1.8 Test: legacy notifications accepted in `routes.test.ts` — header 2025-06-18; `notifications/initialized`, `notifications/cancelled`, `notifications/zzz`, one carrying `"id": 7` → 202, empty body (test-plan #E6)
- [x] 1.9 Test: legacy `ping` in `routes.test.ts` — header 2025-06-18 → 200, `result` deep-equals `{}` (test-plan #E7)
- [x] 1.10 Test: legacy tools calls equal modern in `routes.test.ts` — same `tools/list` and `tools/call server/discover` with (a) header 2025-06-18 no `_meta`, (b) header+`_meta` 2026-07-28 → `result` bodies deep-equal; (a) has no `mcp-session-id` (test-plan #E8)
- [x] 1.11 Test: discover lists every served revision, extend `packages/mcp-server-plugin/src/server/__tests__/dispatch.test.ts` (see its existing `server/discover` case) — versions array deep-equals `["2025-03-26","2025-06-18","2025-11-25","2026-07-28"]` (test-plan #E9)
- [x] 1.12 Test: unknown session id still works in `routes.test.ts` — header 2025-06-18, `Mcp-Session-Id: deadbeef`, `tools/list` → 200 with `result.tools`, not 404 (test-plan #E10)
- [x] 1.13 Test: modern ignores session id in `routes.test.ts` — header+`_meta` 2026-07-28, `Mcp-Session-Id: deadbeef`, `tools/list` → 200, no `mcp-session-id` response header (test-plan #E11)
- [x] 1.14 Test: listen subject to version resolution in `routes.test.ts` (see its existing streaming-deps fixture with the subscribe spy) — `subscriptions/listen sessionIds:["s1"]` with (a) header 2026-07-28 no `_meta` → `MissingMeta`, (b) header 1999-01-01 → `UnsupportedProtocolVersion`, (c) header 2026-07-28 + `_meta` 2025-06-18 → 400 `HeaderMismatch`, (d) header 2025-06-18 → 404 `-32601`; subscribe spy never called (test-plan #E12)
- [x] 1.15 Test: legacy authenticated identically in `routes.test.ts` — no `Authorization` / invalid bearer on `initialize` 2025-06-18, `ping`, `notifications/initialized` → 401, `www-authenticate: Bearer`, no `mcp-session-id` (test-plan #X1)
- [x] 1.16 Test: session id does not carry auth in `routes.test.ts` — valid `initialize` → captured id; `tools/list` with that `Mcp-Session-Id` and no `Authorization` → 401 (test-plan #X2)
- [x] 1.17 Test: ambiguous header refused in `routes.test.ts` — two `MCP-Protocol-Version` headers on `tools/list` and on a valid `initialize` → 400, `data.type === "AmbiguousHeader"`, no `mcp-session-id` (test-plan #X3)
- [x] 1.18 Test: malformed legacy payloads never 500 in `routes.test.ts` (see its existing malformed-body cases) — header 2025-06-18; `not json`, `[]`, `{"jsonrpc":"2.0"}` → JSON-RPC parse/invalid-request error, status ≠ 500, no `unhandledRejection` (test-plan #X10)
- [x] 1.19 Implement `dispatch.ts`: delete the internal version re-resolution (`dispatchRpc` takes the resolved `{ era, version }`), legacy-era `initialize` (capabilities `{tools:{listChanged:false}}`, negotiate-down to `2025-11-25`, `UnsupportedProtocolVersion` naming all four on missing/non-string version), `notifications/*` → 202 (prefix wins), `ping` → `{}`; modern era keeps 404/-32601 for all three. Verify 1.5–1.9, 1.11 pass.
- [x] 1.20 Implement `routes.ts`: resolve version once before the listen interceptor and pass it down; mint `Mcp-Session-Id` (random 128-bit hex) only on a successful legacy `initialize`; ignore incoming `Mcp-Session-Id`; legacy `subscriptions/listen` → 404/-32601 without opening a stream; rewrite the `send()` "NOT setting Mcp-Session-Id" comment and `provisioning.ts` docblocks ("ignore both", "exactly one revision"). Verify 1.4, 1.10, 1.12–1.18 pass; `mcp.json` provisioning still pins `2026-07-28`.

## 2. Direct device-token issuance (server)

- [x] 2.1 Test: registry row without `source`, extend `packages/server/src/__tests__/paired-devices.test.ts` (see its existing temp-registry fixture) — pre-written row lacking `source` lists as `"pairing"`, `verify(token)` returns it, after `add()` the file's old row contains `"source":"pairing"` (test-plan #E15)
- [x] 2.2 Implement `paired-devices.ts`: `source: "pairing" | "manual"` on `PairedDevice` + `PairedDeviceView`, `add(label, source = "pairing")`, loader default. Verify 2.1 passes.
- [x] 2.3 Test: label BVA, extend `packages/server/src/__tests__/pairing.test.ts` (see its existing fastify-inject + temp-registry route fixture) — `POST /api/paired-devices` from loopback with `label` ∈ {"", " ", "a", "x"×64, "x"×65, "é"×32, "é"×33, 123, absent} → invalid cases 400 with registry row count unchanged; valid cases 200, `data.token` ≥ 32 chars, `data.device.source === "manual"`, label trimmed (test-plan #E13)
- [x] 2.4 Test: mint response envelope in `pairing.test.ts` — body shape `{success:true, data:{device:{id,label,createdAt,lastSeen,source}, token}}`, no `tokenHash`; subsequent `GET /api/paired-devices` row has no `token`/`tokenHash` (test-plan #E16)
- [x] 2.5 Test: paired device cannot clone itself in `pairing.test.ts` — only credential a valid device bearer, `x-forwarded-for: 203.0.113.9` → 401, row count unchanged (test-plan #X4)
- [x] 2.6 Test: trusted network alone cannot mint in `pairing.test.ts` (see `packages/server/src/__tests__/auth-plugin.test.ts` for the `trustedNetworks` boot shape) — auth disabled, `trustedNetworks: ["10.0.0.0/8"]`, remote `10.1.2.3`, no credential → 401; `GET /api/paired-devices` from same address → 200 (test-plan #X5)
- [x] 2.7 Test: unadmitted Host refused in report mode in `pairing.test.ts` — host gate `report`, loopback, `Host: attacker.example:8000`, `Origin: http://attacker.example:8000` → 403, row count unchanged; `GET /api/paired-devices` with same Host → 200 (test-plan #X6)
- [x] 2.8 Test: local browser mints with auth on in `pairing.test.ts` (see `auth-plugin.test.ts` for the OAuth-configured fixture) — auth enabled, loopback, no cookie, `Host: localhost:8000` → 200 with token (test-plan #X7)
- [x] 2.9 Test: local token mints from non-loopback in `pairing.test.ts` — valid `X-Pi-Local-Token`, `x-forwarded-for: 203.0.113.9` → 200 with token (test-plan #X8)
- [x] 2.10 Test: mint route not a public prefix in `pairing.test.ts` — auth enabled, remote address, no credential → 401 (test-plan #X9)
- [x] 2.11 Implement: `request.authVia` marker set in `auth-plugin.ts` (`"session"`) and `bearer-auth.ts` (`"device"`); `operatorGuard` preHandler (session ∨ local token ∨ `isGenuinelyLocal`, AND `isHostAdmitted` enforce semantics → 403); `POST /api/paired-devices` in `pairing-routes.ts` with `typeof label === "string"`, trim, `MAX_DEVICE_LABEL_BYTES = 64` (`Buffer.byteLength`), `registry.add(label, "manual")`, `200 { success, data: { device, token } }`; not in `PUBLIC_PAIRING_PREFIXES`. Verify 2.3–2.10 pass.
- [x] 2.12 Test: minted token reaches `/mcp` as a device caller, extend `packages/mcp-server-plugin/src/server/__tests__/routes.test.ts` (see its device-bearer fixture) — token from `registry.add("cli","manual")` on a temp registry → `tools/list` 200; caller resolved `kind:"device"` (test-plan #E14). Verify it passes with no further code change.

## 3. Settings UI (client)

- [x] 3.1 Test: create-token API helper, new `packages/client/src/lib/pairing/__tests__/paired-devices-api.test.ts` (see sibling `single-encoder.test.ts` for the vitest layout) — mocked `fetch`; `createPairedDevice("cli")` → one `POST` to a URL ending `/api/paired-devices`, JSON body `{label:"cli"}`, resolves to `{device, token}` unwrapped from `data` (test-plan #F5)
- [x] 3.2 Implement `paired-devices-api.ts`: `createPairedDevice(label)` via `getApiBase()`, `PairedDeviceView.source`. Verify 3.1 passes.
- [x] 3.3 Test: pairing rows unchanged, new `packages/client/src/components/connectivity/PairedDevicesSection.test.tsx` (see `packages/client/src/components/settings/__tests__/AllowedHostsSection.test.tsx` for the settings-section render pattern) — rows `[{source:"pairing"},{source:"manual"}]` via mocked API → exactly one "manual" badge, both rows have a revoke control (test-plan #F4)
- [x] 3.4 Implement `PairedDevicesSection.tsx`: "Create token for an MCP client" button → label input → one-shot panel (token + copy; `claude mcp add --transport http pi-dashboard <getApiBase()>/mcp --header "Authorization: Bearer <token>"` + copy); panel state cleared on dismiss; "manual" badge; strings via `i18nT`. Verify 3.3 passes.
- [x] 3.5 Test: snippet copy-ready, new `tests/e2e/mcp-token-settings.spec.ts` (see `tests/e2e/pairing-qr.spec.ts` for the real-registry `request` glue and `tests/e2e/mcp-client-settings-a11y.spec.ts` for Settings navigation; read `dashboardPort` from `.pi-test-harness.json`) — Settings → Paired Devices, click create, type `claude-code`, submit → a code element matching `/^claude mcp add --transport http \S+ https?:\/\/[^/]+\/mcp --header "Authorization: Bearer [A-Za-z0-9_-]{32,}"$/` whose host equals `page.url()` origin; token element text equals the bearer in the snippet (test-plan #F1)
- [x] 3.6 Test: token not shown again in `mcp-token-settings.spec.ts` — dismiss → `getByText(token)` count 0, `body.innerText` lacks the token; list has a `claude-code` row with a "manual" badge; `GET /api/paired-devices` row `source === "manual"` (test-plan #F2)
- [x] 3.7 Test: manual rows revocable in `mcp-token-settings.spec.ts` — revoke the `claude-code` row, confirm → row gone; `POST /mcp tools/list` with the F1 token → 401 (test-plan #F3)
- [x] 3.8 Manual: paste the Settings snippet into a real `claude mcp add`, run `claude mcp list` and one tool call against the dashboard; tool result returned (test-plan: manual-only)

## 4. Docs and closeout

- [x] 4.1 `docs/faq.md`: "How do I connect Claude Code / Cursor to the dashboard MCP?" (mint token in Settings → paste snippet; tunnel/reverse-proxy base URL note; revoke in Paired Devices; bearer lands in shell history → revoke + re-mint; legacy clients get no event streaming; `DELETE /mcp` on shutdown → 405 is expected) — delegated to DocScribe. Verify entry present and cross-refs resolve.
- [x] 4.2 `docs/architecture.md` §MCP endpoint + `packages/mcp-server-plugin/README.md`: dual-era contract, opaque `Mcp-Session-Id`, negotiate-down rule, streaming modern-only, operator-gated mint route — delegated to DocScribe. Verify prose matches spec scenarios.
- [x] 4.3 Update directory `AGENTS.md` rows for every touched file (`protocol.ts`, `dispatch.ts`, `routes.ts`, `provisioning.ts`, `paired-devices.ts`, `pairing-routes.ts`, `auth-plugin.ts`, `bearer-auth.ts`, `paired-devices-api.ts`, `PairedDevicesSection.tsx`, new test files) with `See change: mcp-legacy-clients-and-token-issuance`. Verify `kb dox lint` reports no stale rows for them.
- [x] 4.4 File follow-up issue: `DELETE /api/paired-devices/:id` still admits a device bearer via `networkGuard` (tighten with the new `authVia` marker). Verify issue URL recorded in the PR description.
- [x] 4.5 Run `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` and `npm run quality:changed`; verify green. Run `review-code` on the diff before commit.
- [x] 4.6 Archive prep: after `openspec archive`, grep `openspec/specs/dashboard-mcp-server/spec.md` for `2026-07-28`-only wording in the `## Purpose` preamble and fix by hand (archive-sync trap). Verify grep returns only dual-era text.
