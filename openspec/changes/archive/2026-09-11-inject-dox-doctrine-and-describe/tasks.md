## 1. Canonical doctrine refresh + config `doctrine` group

- [x] 1.1 Refresh the READ section of `packages/extension/.pi/skills/project-init/dox-doctrine.md` to the root `AGENTS.md` READ content (lane rule + numbers, corpus boundaries, trust-verdict row, `ctx_execute_file` row, fall-through); repo-specific lines stay out. Verify by diffing against root `AGENTS.md` Docs-First Gate table.
- [x] 1.2 Test (test-plan #E23) — content contract for the canonical READ section: input `dox-doctrine.md` READ section · trigger read · observable contains `doc_type` lane rule, `tests/ qa/ scripts/ docker/`, `ctx_search`/`memory_search` corpus line, `STALE` `GONE` `MOVED` `FRESH`, `--doc-type agents` row, `kb_get` row, `ctx_execute_file` row, "Fall-through". New file `packages/kb-extension/src/__tests__/doctrine-content.test.ts`; see `packages/kb/src/__tests__/dox-source-coverage.test.ts` for the read-a-repo-file pattern. Verify it fails before 1.1/2.1 and passes after.
- [x] 1.3 Tests (test-plan #E1 #E2 #E3 #E4 #E5) in new `packages/kb/src/__tests__/config-doctrine.test.ts`; see `packages/kb/src/__tests__/search-opts.test.ts` for the temp-dir `loadConfig` harness. Triples: no files → `{kb,false}` + `doctrineSource:"none"`; global `{inject:"off"}` only → `"off"` + `"global"`; project with only `readDiscipline` → `origin:"project"` AND `doctrineSource:"none"`; global `{inject:"kb"}` + project `{write:true}` → `{kb,true}` + `"project"`; project `{doctrine:{}}` → `"project"` + defaults. Verify red.
- [x] 1.4 Tests (test-plan #E6 #E7 #E8) same file: project `{doctrine:null}` → throws naming `doctrine`; `{inject:"manual"}` → throws containing `manual`; `{write:"yes"}` → throws naming `doctrine.write`. Verify red.
- [x] 1.5 Implement in `packages/kb/src/config.ts`: `doctrine` in `DEFAULTS` + `NESTED_KEYS`; `doctrineSource` computed from raw layers (non-null object = present); validation for `inject` ∈ {`kb`,`off`} and boolean `write`. Verify 1.3 + 1.4 green and existing `packages/kb` tests unchanged.

## 2. Doctrine module + `before_agent_start` hook (packages/kb-extension)

- [x] 2.1 `git mv packages/extension/.pi/skills/project-init/dox-doctrine.md packages/kb-extension/dox-doctrine.md`; delete the `dox:read:manual` section; add `dox-doctrine.md` to `files` in `packages/kb-extension/package.json`. Verify `npm pack --dry-run -w packages/kb-extension` lists it.
- [x] 2.2 Test (test-plan #E9) — decision table for `buildDoctrineFragment`: `{kb,false}` → delimiter + READ, no WRITE heading; `{kb,true}` → READ + WRITE; `{off,false}` and `{off,true}` → `""`. New file `packages/kb-extension/src/__tests__/doctrine.test.ts`; see `packages/kb-extension/src/__tests__/guard.test.ts` for the pure-module style. Verify red.
- [x] 2.3 Tests (test-plan #E10 #E11 #E12) same file — `hasLegacySeed`: content with `<!-- dox:write:start -->` and no marker → `true`; marker + pointer text, no delimiter → `false`; content with `<!-- dox:read:manual:start -->` → `true`. Verify red.
- [x] 2.4 Tests (test-plan #E13 #E14 #E15) same file — insertion: prompt `"A\nCurrent working directory: /x\n"` → fragment index < anchor index and anchor line preserved verbatim; prompt `"A"` → `"A\n\n── dox doctrine ──…"`; prompt already containing the delimiter, run twice → exactly 1 delimiter. Verify red.
- [x] 2.5 Tests (test-plan #E16 #E17) same file — composition with the real `spliceContextFragment` imported from `packages/extension/src/dashboard-context-injector.ts`: kb-then-bridge and bridge-then-kb both yield a prompt containing `── dox doctrine ──` AND `You are pi session`, one of each; see `packages/extension/src/__tests__/dashboard-context-injector.test.ts`. Verify red.
- [x] 2.6 Implement `packages/kb-extension/src/doctrine.ts` (pure, no pi imports): `loadDoctrineSections`, `hasLegacySeed`, `buildDoctrineFragment`, `insertFragment`, `buildFirstContactNudge`, `buildMigrationNudge`. Verify 2.2–2.5 green.
- [x] 2.7 Tests (test-plan #E18 #E19 #E20 #E21) in new `packages/kb-extension/src/__tests__/doctrine-hook.test.ts` with a fake `pi` capturing `before_agent_start` (see `packages/kb-extension/src/__tests__/reindex.test.ts` for the fake-pi harness) and a temp cwd: `doctrineSource:"none"` over 2 turns → turn 1 has the nudge naming the 4 options, `.pi/dashboard/knowledge_base.json`, the non-interactive rule; turn 2 does not · legacy contextFiles + none → no fragment, migration text present, first-contact absent · legacy + project `{inject:"off"}` → no fragment, no nudges · no config on turn 1, write `{doctrine:{inject:"kb",write:true}}`, turn 2 → WRITE heading present only on turn 2. Verify red.
- [x] 2.8 Tests (test-plan #X1 #X2) same file — fault injection: project file `{"doctrine": ` over 3 turns → READ present, WRITE absent, no nudge, `console.warn` called once with `[kb]` prefix · doctrine file path unreadable → no throw, prompt unchanged, one `[kb]` warn. Verify red.
- [x] 2.9 Test (test-plan #P1) same file — 100 sequential events against warm project+global config → p95 handler wall time < 20 ms. Verify red (handler absent).
- [x] 2.10 Wire the handler in `packages/kb-extension/src/extension.ts`: per-turn `loadConfig(systemPromptOptions.cwd)` in try/catch → defaults fallback + one `[kb]` warn per session; legacy detection over `contextFiles`; once-per-instance nudge flag; insert-before-anchor; delimiter idempotence. Verify 2.7–2.9 green and full `packages/kb-extension` suite green.
- [x] 2.11 Test (test-plan #X4) in `packages/kb-extension/src/__tests__/reindex.test.ts` (extend): reindex fn stubbed to throw `SQLITE_BUSY` once → logged with `[kb]` prefix, not rethrown; next scheduled reindex succeeds. Verify red, then harden `reindex.ts` to pass.

## 3. Seed shrink + project-init skill

- [x] 3.1 Test (test-plan #E22) — update `packages/extension/src/__tests__/project-init-seed-doctrine.test.ts` (+ `project-init-doctrine-seeds.test.ts`): empty AGENTS.md, `seedDoctrine` twice → block contains `<!-- dox-doctrine -->`, `pi-dashboard-kb-extension`, `.pi/dashboard/knowledge_base.json`, no `dox:*:start`; second call `seeded:false`, file byte-identical. Verify red.
- [x] 3.2 Shrink `packages/extension/src/project-init/seed-doctrine.ts` to the fixed pointer block (drop `kbWired`/section extraction/doctrine path). Verify 3.1 green.
- [x] 3.3 Update `packages/extension/.pi/skills/project-init/SKILL.md` step 5: seed pointer; write `doctrine` into project `knowledge_base.json` from the DOX answer (`{inject:"kb",write:true}` when `DOX_ENABLED`, read-merge-write); after `kb dox init` ask "run dox-describe now?". Verify `kb dox lint` clean on the skill dir.
- [x] 3.4 Add a `doctor` check to `packages/extension/.pi/skills/doctor/`: "doctrine configured in project config but kb extension not loaded". Verify by reading the check in the rendered report (manual in 6.5).

## 4. `dox describe --list` (packages/kb)

- [x] 4.1 Tests (test-plan #E24 #E25 #E26 #E27 #E28 #X3) in new `packages/kb/src/__tests__/dox-describe.test.ts`; see `packages/kb/src/__tests__/dox-triage.test.ts` for the temp-tree builder. Triples: `a/AGENTS.md` (1 empty) + `b/AGENTS.md` (1 empty, 1 filled) → 2 groups, subjects `a/x.ts` `b/y.ts`, total 2 · `--json` → array of `{agentsPath, subjects[]}` + `total:2` · `--dir a` → only `a`, total 1 · all filled → `[]`, total 0, exit 0 · whitespace-only purpose → counted empty · `c/AGENTS.md` prose-only → skipped, no throw. Verify red.
- [x] 4.2 Implement `listEmptyPurposeRows` in `packages/kb/src/dox.ts` (reuse `parseRows` from `dox-triage.ts`, same walk as `dox init`) and `dox describe --list [--json] [--dir]` in `packages/kb/src/cli.ts`. Verify 4.1 green.

## 5. `dox-describe` skill

- [x] 5.1 Create `packages/kb-extension/.pi/skills/dox-describe/SKILL.md`: run `kb dox describe --list --json`; plan = files, rows, rough token estimate, cap 50 rows (rest deferred); `ask_user` confirm; fan out ≤2 concurrent write-capable `Agent` calls (inline label, not `Explore`), one per `AGENTS.md`, with row rules (one line, `| File | Purpose |`, symbols verbatim, key exports/contracts, ≤200 chars, condense never sidecar, edit only own file, must read each file, leave unreadable rows empty); parent runs `kb dox lint` + one reindex; report deferred/unreadable rows; headless `pi -p` escape hatch section. Add `.pi/skills/dox-describe/` to `files` in `packages/kb-extension/package.json`. Verify `kb dox lint` clean.
- [x] 5.2 Test (test-plan #E29) in `packages/kb-extension/src/__tests__/doctrine-content.test.ts` (extend): `package.json` `files` includes `dox-doctrine.md` and `.pi/skills/dox-describe/`, both exist on disk. Verify green after 2.1 + 5.1.
- [x] 5.3 Add DOX rows for `doctrine.ts`, `dox-doctrine.md`, the new tests and the skill dir in the nearest `AGENTS.md` files. Verify `kb dox lint` clean.

## 6. Dogfood + docs + verification

- [x] 6.1 Root `AGENTS.md`: remove Docs-First Gate, Investigation Protocol and the generic Documentation Update Protocol rows; re-home the `docs/faq.md`/`README.md` grep row and the `run-fixtures.ts` reproduce line; keep DocScribe/caveman rule, subagent table, commands. Set `doctrine: {inject:"kb", write:true}` in `.pi/dashboard/knowledge_base.json` (same commit). Verify with 6.2.
- [x] 6.2 Test (test-plan #E30) in `packages/kb-extension/src/__tests__/doctrine-content.test.ts` (extend): root `AGENTS.md` has no `## Docs-First Gate` / `## Investigation Protocol` heading, contains `docs/faq.md` and `run-fixtures.ts`; `.pi/dashboard/knowledge_base.json` `doctrine.write === true`. Verify green after 6.1.
- [x] 6.3 Delegate to DocScribe: `docs/architecture.md` config reference gains `doctrine` (`inject`, `write`, `doctrineSource`, first-contact + migration flow, legacy guard, defaults fallback); `docs/faq.md` entry "doctrine not injected / nudge keeps firing"; apply returned tree rows. Verify `kb dox lint` clean.
- [x] 6.4 `npm run reload`; `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log`; grep summary; `node scripts/check-conventions.mjs`; `npm run quality:changed`. Verify all green.
- [x] 6.5 Manual (test-plan: manual-only #E31): temp project via `project-init` with no `doctrine` key → first turn asks the 4-option question → choose `kb read+write` → project file gains `doctrine` with other keys intact → next session: no nudge, READ+WRITE fragment exactly once.
- [x] 6.6 Manual (test-plan: manual-only #E32): temp project with a legacy kb-variant full seed → first turn offers migration → accept → block replaced by pointer → first-contact question follows → next session injects.
- [x] 6.7 Manual (test-plan: manual-only #E33): temp `kb dox init` tree with 51 empty rows → run `dox-describe` → plan shows 51 rows + cap 50 → confirm → 50 filled, 1 deferred → run 2 fills 1 → run 3 modifies nothing → `kb dox lint` clean → `kb_search` finds a new purpose.
- [x] 6.8 Manual (test-plan: manual-only #E34): `project-init` with DOX in a temp dir → after tree scaffold it asks "run dox-describe now?".
- [x] 6.9 Manual (test-plan: manual-only #E35): temp project with `doctrine` key and kb extension not loaded → `doctor` report names "doctrine configured but kb extension not loaded".
- [x] 6.10 Manual (test-plan: manual-only #X5): `dox-describe` on a tree with a row pointing at a 0-byte file → cell stays empty and is listed in the final report.
