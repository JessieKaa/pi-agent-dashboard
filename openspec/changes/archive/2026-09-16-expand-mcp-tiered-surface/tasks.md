Test tasks reference `test-plan.md` rows as `(test-plan #id)`; each carries the row's input · trigger · observable and an exemplar test to copy harness glue from.

## 1. Tier on the paired-device registry + route→tier map (server, shared)

- [x] 1.1 Test, extend `packages/server/src/__tests__/paired-devices.test.ts` (test-plan #E1 #E2 #E3 #E4 #E5): `add("x","manual")` · `list()` · `tier:"observe"`; `add("x","pairing")` · `list()` · `tier:"operate"`; `add("x","manual","root")` · call · throws and file byte-length unchanged; legacy row without `tier`/`source` · `verify(token)`+`list()` · `{id,tier:"operate"}` and listed `source:"pairing"`; row with `"futureField":1` · add another then revoke it · field still present in file
- [x] 1.2 Implement `paired-devices.ts`: `Tier` + `TIERS` + `rank()`, `tier` on `PairedDevice`/view, `add(label, source, tier?)` with per-source default, loader default `operate`, `verify` → `{ id, tier } | null`; export `Tier` from `packages/shared/src/rest-api.ts`. Verify 1.1 green
- [x] 1.3 Test, extend `packages/server/src/__tests__/pairing.test.ts` (test-plan #E6 #E7): `POST /api/paired-devices` `{label}` / `{label,tier:"control"}` / `{label,tier:"admin"}` · each · 200 `observe` / 200 `control` / 400 with `list()` length unchanged; `POST /api/pair/approve` with and without `tier:"control"` · redeem completes · row `control` / `operate`
- [x] 1.4 Implement tier on `pairing-routes.ts` mint + approve + list; `server.ts` registers new service `host.verifyDeviceTokenTier` (old key untouched). Verify 1.3 green
- [x] 1.5 Test, new `packages/server/src/__tests__/pair-reachable-urls-routes.test.ts`, see `packages/server/src/__tests__/pairing.test.ts` for the route harness (test-plan #E28): `localEndpoints()` stub `[http://localhost:8000, http://192.168.1.4:8000]` + tunnel `https://x.share.zrok.io` + public `[]` · `GET /api/pair/reachable-urls` · exactly those 3, tunnel first, no duplicates
- [x] 1.6 Implement `GET /api/pair/reachable-urls` in `pairing-routes.ts` merging `tunnel/tunnel-endpoints.ts#localEndpoints()` with the existing `getReachableUrls` dep; exclude it by name from `PUBLIC_PAIRING_PREFIXES`. Verify 1.5 green
- [x] 1.7 Write `packages/shared/src/route-tiers.ts`: `ROUTE_TIERS`, `routeTier(method, pattern)` (unlisted → `operate`), seeded with every current `/api/*` route at `operate` (zero behaviour change); `tsc --noEmit` passes

## 2. REST tier gate (server)

- [x] 2.1 Test, new `packages/server/src/__tests__/route-tier-gate.test.ts`, see `packages/server/src/__tests__/bearer-auth.test.ts` + `localhost-guard.test.ts` for the hook harness (test-plan #E23 #E24 #E25 #X8): decision table principal ∈ {observe/control/operate bearer, cookie, loopback+observe bearer, trusted-net+observe bearer} × route ∈ {`GET /api/sessions` observe, `POST /api/session/:id/prompt` control, `POST /api/restart` operate, unlisted `POST /api/fixture`} from `203.0.113.5` unless loopback/trusted · request · 403 + `WWW-Authenticate` only for off-host bearer below route tier, cookie/loopback/trusted never tier-403, unlisted → `scope="operate"`; no credential off-host · `GET /api/sessions` · status equals pre-change snapshot; control/operate bearer · `POST /api/ws-ticket` · 403 `scope="operate"` / 200; control bearer · `POST /api/restart` · one `auth.tier_refused` log `{deviceId, method:"POST", route:"/api/restart", principalTier:"control", requiredTier:"operate"}` and restart spy not called
- [x] 2.2 Implement `auth/bearer-auth.ts` (`request.principalTier`) and new `auth/route-tier-gate.ts` (applies iff `request.authVia === "device"` and not genuinely-local/trusted; `/api/*` only), registered in `server.ts` after both admission hooks; list `/api/ws-ticket` at `operate`. Verify 2.1 green and full `packages/server` suite unchanged
- [x] 2.3 Test, extend `packages/server/src/__tests__/session-action-handler-followup-queue.test.ts` pattern into new `session-lifecycle-route.test.ts` (test-plan #E26 #E27): `POST /api/session/:id/lifecycle` × action ∈ {stop_after_turn, retry, force_kill, kill_process} × principal ∈ {control bearer off-host, operate bearer, loopback} · request · control+force_kill/kill_process → 403 `scope="operate"`, all others reach the extracted handler spy once with `{sessionId, action}`; WS `extension_ui_response` and `POST /api/session/:id/extension-ui-response` with the same `requestId` · each path · both clear `pendingUiRequests[S][requestId]`, spy args identical
- [x] 2.4 Implement: extract `extension_ui_response` and the four lifecycle verbs from the gateway switch / `session-action-handler.ts` into shared handlers; add `POST /api/session/:id/extension-ui-response` and `POST /api/session/:id/lifecycle` (`control` in `ROUTE_TIERS`; in-handler `operate` check for `force_kill`/`kill_process`); WS cases call the same functions. Verify 2.3 green

## 3. Tier resolution and enforcement (mcp-server-plugin)

- [x] 3.1 Test, extend `packages/mcp-server-plugin/src/server/__tests__/tools.test.ts` auth cases (test-plan #E8 #E9): `verifyDeviceTokenTier` stub → `{id,tier:"control"}` / only old key present → `"dev1"` · `authenticate()` · `tier` = `control` / `operate`; session-kind caller · `authenticate()` · `control`
- [x] 3.2 Implement `tokens.ts`/`auth.ts`/`index.ts`: `tier` on `McpCaller`, consume `host.verifyDeviceTokenTier` with fallback to `host.verifyDeviceToken` → `operate`. Verify 3.1 green
- [x] 3.3 Test, extend `__tests__/dispatch.test.ts` (test-plan #E10 #X1 #X2 #X3 #X4): fixture manifest 1 tool/tier · `tools/list` as observe/control/operate · `{o}`/`{o,c}`/`{o,c,p}`; observe caller · `tools/call send_prompt` · HTTP 403, header `WWW-Authenticate: Bearer error="insufficient_scope", scope="control"`, JSON-RPC `-32001` `data.scope:"control"`, handler spy not called, log `mcp.tier_refused {caller, tool, callerTier, requiredTier}`; session caller `A` control · `tools/call force_kill {sessionId:"A"}` · 403 `scope="operate"` and no guard log; observe caller · `send_prompt {}` · 403 not `-32602`; any caller · `tools/call nope` · `-32601` unchanged
- [x] 3.4 Implement tier filter + scope challenge in `dispatch.ts` (order tier → validation → guard). Verify 3.3 green
- [x] 3.5 Test, extend `__tests__/routes.test.ts` (test-plan #E11 #E12 #E13): operate token · `tools/list` at `/mcp/observe`,`/mcp/control`,`/mcp` · 1/2/3 tools; observe token · `/mcp/control` · 1 tool; any token · `POST /mcp/operate`, `GET /mcp/nope`, `PUT /mcp/observe` · 404 JSON / 404 JSON / 405, never `text/html`
- [x] 3.6 Implement path cap in `routes.ts` (`effectiveTier = min(caller.tier, pathCap)`), `/mcp/*` catch-all 404 JSON for all methods, 405 discipline on the two capped paths. Verify 3.5 green

## 4. Manifest, codegen, binders (mcp-server-plugin, server)

- [x] 4.1 Add `ts-json-schema-generator` devDependency to `packages/mcp-server-plugin` (`pnpm add -D`); write `src/server/tools.manifest.ts` (`ToolRow`, `bind.fixed`) and `src/server/tools.denylist.ts` seeded with the 4 existing tools as `context` rows and a temporary denylist for everything else; `tsc --noEmit` passes
- [x] 4.2 Implement `src/codegen/generate-tools.ts` (pattern `packages/bus-client/src/codegen/generate-verbs.ts`): `inputSchema`, `paramSplit` with `:id` → `sessionId` renaming, `sessionTargeting` flag validation, `tier` derived from `ROUTE_TIERS` for `rest` rows, README `<!-- tools:start/end -->` block; add `"codegen"` script; `npm run codegen -w @blackbelt-technology/pi-dashboard-mcp-server` emits the 4-tool file
- [x] 4.3 Test, new `__tests__/generated-freshness.test.ts`, see `packages/bus-client/src/__tests__/verb-completeness.test.ts` (test-plan #E17): checked-in `generated/tools.ts` + README block · regenerate in memory · deep-equal; hand-edit one description · rerun · inequality
- [x] 4.4 Test, new `__tests__/manifest-invariants.test.ts` (test-plan #E14 #E18): full manifest · invariants · every observe tool `readOnlyHint:true`, no destructive tool at observe, `force_kill` operate+destructive, descriptions ≤ 120 chars, names unique, each `rest` row tier ≥ `routeTier`; rows with `:id` path / `:sessionId` path / `sessionId` schema · check · each flagged `sessionTargeting`, fixture row under `/api/session/:id/x` without flag fails
- [x] 4.5 Test, new `packages/server/src/__tests__/mcp-manifest-completeness.test.ts`, see `packages/server/src/__tests__/system-routes-restart.test.ts` for server boot + `packages/bus-client/src/__tests__/verb-completeness.test.ts` for the union walk (test-plan #E15 #E16): booted `/api/*` routes R via `onRoute` + union V · test · `rest.paths ∪ session.messages = (R ∪ V) − DENYLIST`, removing one denylist entry fails naming the route; every R route has a `ROUTE_TIERS` entry, a fixture route without one fails naming it. Add the plugin as a workspace devDependency of `packages/server`
- [x] 4.6 Test, extend `__tests__/dispatch.test.ts` (test-plan #E19 #E20 #E21 #E22 #X5 #X6 #X7 #E30): `GET /api/session/:id/events` args `{sessionId:"S1",since:5}` · call · inject url `/api/session/S1/events?since=5` no body; args `{sessionId:"S1/../../restart"}` and `{sessionId:"a%2Fb"}` · call · `-32602`, inject not called; `force_kill` row args `{sessionId:"S2"}` · operate call · inject `POST /api/session/S2/lifecycle` body `{action:"force_kill"}`; control caller · 4 existing tools · byte-identical to pre-change snapshots; session caller `A` · every session-targeting row with `sessionId:"A"` · guard refuses, inject never called, log has caller/target/tool; inject returns 500 `{success:false,error:"boom"}` · call · `isError:true` text contains `boom`; device caller `203.0.113.5` with `x-forwarded-for` / session caller · call · inject options `remoteAddress:"203.0.113.5"` + `authorization`,`host`,`x-forwarded-for`, no `origin` / `remoteAddress:"127.0.0.1"`, no `authorization`, no `x-forwarded-*`; manifest row whose route is unregistered · activation · absent from `tools/list`, one `mcp.manifest_route_missing` log
- [x] 4.7 Implement binders in `dispatch.ts` (`rest` via `fastify.inject` with identity/headers per design D4, `encodeURIComponent` + reject `/ ? # %` in path args, `session` via `ctx.sendToSession`, `context` unchanged, envelope mapping) and `hasRoute` skew check at activation; move `fastify` to `INTERNAL_ONLY` in `tools.ts`. Verify 4.3–4.6 green
- [x] 4.8 Test, new `__tests__/inject-overhead.test.ts` (test-plan #P1): 100 sequential `tools/call list_sessions` via a `rest`-bound fixture row vs the `context` row, 10 warm-up · run · p95(rest) − p95(context) < 20 ms
- [x] 4.9 Test, new `packages/server/src/__tests__/host-gate-inject.test.ts`, see `host-gate.test.ts` (test-plan #X11): host gate enforce mode admitted `dash.local` · device caller `rest` row with original `host: dash.local` · injected request 200; `host` not forwarded · same call · 403 (negative control)
- [x] 4.10 Test, extend `__tests__/dispatch.test.ts` (test-plan #X10): observe token verified for `tools/list`, row revoked · next `tools/call list_sessions` · 401 not 403, inject not called

## 5. Grow the manifest domain by domain (mcp-server-plugin, shared)

Each task: add rows to `tools.manifest.ts`, lower the domain's `ROUTE_TIERS` entries to the design D7 tier, add missing `*Request` types to `packages/shared/src/rest-api.ts`, shrink the temporary denylist, run codegen, verify 4.3–4.5 green.

- [x] 5.1 Session lifecycle: `resume_session`, `shutdown_session`, `archive_session`, `rename_session`, `set_session_tags` (control); `stop_after_turn`, `retry_session` (control, lifecycle route); `force_kill`, `kill_process` (operate, destructive, lifecycle route). Denylist the WS verbs as `duplicate-of:`
- [x] 5.2 Session I/O: `prompt_response`, `flow_control`, `edit_followup_queue` (`session` binders); `extension_ui_response`, `set_model`, `set_thinking_level` (`rest`); denylist `set_model`/`set_thinking_level` verbs `duplicate-of:`, `prompt_resync_request`/`subagent_resync_request` as UI plumbing
- [x] 5.3 Session reads (observe): events-from-seq, tool-result, attachment, session-diff, session-file, transcript, history-backfill
- [x] 5.4 Git + worktrees: status/branches/changed-files/PRs (observe); checkout (control, destructive), commit/commit-draft/worktree create/init/merge/pr (control)
- [x] 5.5 Files + OpenSpec + goals + automation: read/tree/grep (observe), write/mkdir (control); openspec tasks/config (observe), toggle/attach (control); goals + automation read (observe) / write, toggle, run (control)
- [x] 5.6 Models/providers/roles/presets/plugins/packages/resources/config/system/tunnel/pairing/pi-core: lists, health, doctor, tunnel status (observe); roles/presets set (control); restart, config write, provider-auth status/login (no key material), packages install/remove/update, plugins enable/disable, resources, tunnel start/stop, paired-device list/revoke, reachable-urls, pi-core update (operate); final reasoned denylist: api-key read+write, `/auth/*`, `/v1/*`, model-proxy, ws-ticket, `/api/pair/{challenge,payload,poll,redeem,approve}`, electron, force-shed, UI-only + transport verbs, `duplicate-of:` twins of the 4 context tools
- [x] 5.7 Verification: `npm test` green; log `tools/list` size per tier (expect roughly 45/85/130)

## 6. Issuance UI and CLI

- [x] 6.1 Test, new `packages/client/src/components/connectivity/__tests__/PairedDevicesSection.test.tsx`, see `packages/client/src/components/settings/__tests__/AllowedHostsSection.test.tsx` for the RTL harness (test-plan #F1 #F2 #F3 #F4 #F5): dialog open · select `operate` · default was `observe`, warning contains "restart", no snippet yet; `/api/pair/reachable-urls` mocked `[origin, https://x.share.zrok.io]` · mount · select preselects `window.location.origin`, 2 options; tier control + base zrok + mint `tok_123` · submit · snippet contains `https://x.share.zrok.io/mcp` and `Bearer tok_123`, token in DOM exactly once; dismiss + reopen · no `tok_123`; list `[{tier:"observe"},{tier:"operate"}]` · render · each row shows its tier
- [x] 6.2 Implement `PairedDevicesSection.tsx` + `paired-devices-api.ts` (tier radio with descriptions, base-URL select, tier badge, `reachableUrls()`); pairing approve dialog gets the tier picker (default `operate`). Verify 6.1 green, `npm run build` ok
- [x] 6.3 Test, new `packages/server/src/__tests__/cli-token.test.ts`, see `cli-restart.test.ts` for the mocked-fetch CLI harness (test-plan #E29 #X9): `--label ci --tier control` with 2 reachable urls · run · token in stdout exactly once + 2 `claude mcp add` lines; `--label ci --url https://x/` · 1 line targeting `https://x/mcp`; `--tier control` without label · usage error exit 2, no fetch; fetch rejects `ECONNREFUSED` · `token create --label x` · exit ≠ 0, stderr says dashboard not running, no token
- [x] 6.4 Implement `case "token"` in `packages/server/src/cli.ts`. Verify 6.3 green

## 7. E2E, docs, tree rows, closeout

- [x] 7.1 Test, new `tests/e2e/mcp-tiered-token.spec.ts`, see `tests/e2e/security-pair-link.spec.ts` for the Settings pairing surface and `tests/e2e/mcp-client-harness-integration.spec.ts` for hitting `/mcp` on the harness port (test-plan #F6 #F7): Settings → create token tier `observe` · copy token, `POST <harness>/mcp tools/list` from the runner's off-host address · no `send_prompt`, `tools/call send_prompt` → 403 with `WWW-Authenticate` containing `insufficient_scope` and `scope="control"`; token minted `operate` via `POST /api/paired-devices` · `tools/list` then `tools/call force_kill` on a seeded session · list has `force_kill`, `isError:false`, session card leaves running within 10 s
- [x] 7.2 Manual: read the three tier descriptions in the create-token dialog as a first-time operator and judge clarity (test-plan: manual-only)
- [x] 7.3 Delegate to DocScribe: `docs/architecture.md` §MCP endpoint (tier model, REST gate, path caps, manifest pipeline); `docs/faq.md` entries "which tier for Claude Code / Cursor", "connect an agent from another machine", "observe still exposes repo contents", "existing tokens are operate — re-mint narrower"; `kb dox lint` clean
- [x] 7.4 Update `AGENTS.md` rows: `packages/mcp-server-plugin/src/server/AGENTS.md`, `packages/server/src/auth/AGENTS.md`, `packages/server/src/pairing/AGENTS.md`, `packages/server/src/session/AGENTS.md`, `packages/client/src/components/connectivity/AGENTS.md`, `packages/shared/src/AGENTS.md`; `kb dox lint` reports no missing rows
- [x] 7.5 Verification: `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` green; `npm run quality:changed` clean
