## 1. Slice A — Honest coverage report (design D1)

- [ ] 1.1 Add `@vitest/coverage-v8` as a root devDependency pinned to the exact installed `vitest` version via `pnpm add -Dw`; commit the lockfile delta.
- [ ] 1.2 Add `test.coverage` to root `vitest.config.ts`: `provider: "v8"`, explicit `include: ["packages/*/src/**/*.{ts,tsx}"]`, `exclude` for `**/__tests__/**`, `**/test-support/**`, `**/*.d.ts`, `packages/electron/**`, `reportOnFailure: true`, `reporter: ["text-summary", "json-summary"]`, with a comment naming the out-of-scope dirs (electron, `scripts/`, `tests/`, `.pi/`).
- [ ] 1.3 Add root `package.json` script `test:coverage` = the `test` script's `HOME=$(mktemp -d -t pi-test-XXXXXX) NODE_OPTIONS="--localstorage-file=…"` wrapper + `vitest run --coverage`.
- [ ] 1.4 Test: root coverage config shape — see `packages/shared/src/__tests__/sync-versions-spec.test.ts` for the read-root-config harness. Input: import root `vitest.config.ts`, read `test.coverage` · trigger: micromatch `include`/`exclude` against `packages/extension/src/bridge.ts`, `packages/extension/src/__tests__/x.test.ts`, `packages/shared/src/test-support/x.ts`, `packages/electron/src/x.ts`, `x.d.ts` · observable: only `bridge.ts` matches; `reportOnFailure === true`; `provider === "v8"` (test-plan E1)
- [ ] 1.5 Test: `test:coverage` script carries the isolation wrapper — see `packages/apple-tools/src/__tests__/package-manifest.test.ts` for the read-package.json harness. Input: root `package.json` `scripts["test:coverage"]` · trigger: string parse · observable: contains `HOME=$(mktemp -d -t pi-test-`, `--localstorage-file=`, `--coverage` (test-plan E3)
- [ ] 1.6 Test (ci-scenario): unloaded entry point reported at 0% — see `scripts/__tests__/knip-scan.test.mjs` for the `RUN_CI_SCENARIOS`-gated subprocess harness. Input: `npm run test:coverage -- --project @blackbelt-technology/pi-dashboard-extension` in a subprocess · trigger: read `coverage/coverage-summary.json` · observable: entry for `packages/extension/src/bridge.ts` exists; `lines.pct === 0` until slice C lands (flip the expected to `> 0` in task 3.12); `total.lines.total` includes its line count (test-plan E2)
- [ ] 1.7 Test (ci-scenario): report survives a failing test — same harness as 1.6. Input: `test:coverage` run on a tiny fixture project containing one `it` that throws (fixture under `scripts/__tests__/fixtures/`) · trigger: subprocess exit · observable: exit code ≠ 0 AND `coverage-summary.json` exists and is non-empty (test-plan X1)
- [ ] 1.8 Run `npm run test:coverage` once; paste the extension, server and shared `total` lines into design.md `## Measurements` as the baseline.

## 2. Slice B — Source-grep test triage (design D4)

- [ ] 2.1 List every `*.test.ts(x)` under `packages/**/__tests__` that `readFileSync`s a `.ts`/`.tsx` source path (`rg -l 'readFileSync\([^)]*\.tsx?' --glob '**/__tests__/*.test.ts*' packages`); record the 14 files with a keep / replace / delete disposition and one-line reason in the commit message of 2.4.
- [ ] 2.2 Keep: add a JSDoc `@structural-pin <reason>` tag on the `describe` block of every kept file (`ChatView.notify-gate`, `i18n-orphans`, `engine-purity`, `no-bash-on-windows`, `no-tui-multiselect-arm-regression`, and any other justified pin).
- [ ] 2.3 Replace: for each file whose assertion is a runtime behaviour, write the behavioural test beside it in the same `__tests__` dir (copy the nearest sibling's harness) and delete the text assertion.
- [ ] 2.4 Delete: remove files that are neither, one commit, reason per file in the message.
- [ ] 2.5 Test: every remaining source-reading test carries `@structural-pin` — see `packages/shared/src/__tests__/no-bash-on-windows.test.ts` for the tree-scan lint harness. Input: all `__tests__/*.test.ts(x)` containing `readFileSync(` on a `.ts`/`.tsx` path · trigger: read the JSDoc above the first `describe` · observable: contains `@structural-pin ` + ≥10 non-space chars; failure message lists offenders (test-plan E10)

## 3. Slice C — Bridge handler extraction (design D2, D3)

- [ ] 3.1 Closure inventory: script (`scripts/bridge-closure-inventory.mjs`, throwaway, deleted in 3.13) that lists every identifier declared inside `initBridge` and referenced from the ranges to be moved (`session_start` L2642–~3460, `turn_end`, `session_shutdown`, `session_info_changed`, `project_trust`, the two forwarder loops L2081/L2510, the 21-branch dispatch ~L1097–1200); diff the list against the existing `BridgeState` fields; write the resulting `BridgeInstance` field list into design.md D2.
- [ ] 3.2 Run `doubt-driven-review` on the `BridgeInstance` shape (artifact: the field list + `createInstance` signature; contract: bridge-decomposition delta spec) before any move.
- [ ] 3.3 Create `packages/extension/src/bridge-handlers/instance.ts`: `BridgeInstance` type + `createInstance(pi, generation, shared)` with `isActive()` reading `shared.generation === generation`; wire `initBridge` to create it after the generation bump. No behaviour moves yet; extension suite green.
- [ ] 3.4 Move the `ctx.ui` patching sub-block (~L2771–2920, from `originalNotify`/`const originals` through the `multiselect` patch) to `bridge-handlers/ui-prompt-patch.ts`; `session_start` calls it. Retarget `no-tui-multiselect-arm-regression.test.ts` to scan `tui-prompt-adapter.ts` (same two substrings). Commit; suite green; `npm run reload` smoke.
- [ ] 3.5 Move the inbound WebSocket dispatch to `bridge-handlers/dispatch.ts` as `handleInboundMessage(inst, msg)`; the connection `onMessage` callback delegates. Commit; suite green; reload smoke.
- [ ] 3.6 Move the five named lifecycle handlers plus `handleSessionChange` and `startGitPollTimer` to `bridge-handlers/lifecycle.ts`; move the `sendStateSync`/`replaySessionEntries` wrappers to `bridge-handlers/session-helpers.ts`. Commit; suite green; reload smoke.
- [ ] 3.7 Move the two forwarder loop bodies to `bridge-handlers/forwarders.ts` (`registerEnrichedForwarders`, `registerPassThroughForwarders`); `EVENT_BUS_MAP` and the emit intercept stay in `bridge.ts`. Commit; suite green; reload smoke.
- [ ] 3.8 Retarget the remaining source-reading tests whose needle moved (`bridge-agent-settled-contract`, `bridge-liveness-heal`, `bridge-queue-update-forward`, `command-path-mapping`, `multiselect-dashboard-routing`, `no-session-replacement-calls`, `pi-version-tracker` E8/E17, `session-register-carries-pid`, `ui-stale-guard`) to the new module paths, in the same commit as the move that displaced each needle.
- [ ] 3.9 Test: each extracted module runs against a stub instance without loading the entry point — see `packages/extension/src/__tests__/prompt-bus-wiring.test.ts` for the stub-`pi` harness. Input: import `lifecycle.ts` / `dispatch.ts` / `forwarders.ts` / `ui-prompt-patch.ts` directly; stub `pi` recording `on`/`sendMessage`; `createInstance(pi, 1, {})` · trigger: one representative call per module (`session_start` with `reason: "start"`; `handleInboundMessage(inst, {type: "stop_after_turn"})`; register forwarders then emit one event; patch a stub `ctx.ui`) · observable: only `inst`/`inst.shared` fields change; `../bridge.ts` is absent from the loaded module set (test-plan E4)
- [ ] 3.10 Test: inbound dispatch decision table — same harness as 3.9. Input: table of the 21 `msg.type` values with minimal payloads, expected effect captured from the pre-extraction handler (e.g. `stop_after_turn` → `inst.shared.shouldStopAfterTurn === true`; `set_thinking_level` → `pi.setThinkingLevel(payload)`; `credentials_updated` → provider re-register called; `{type: "__nope__"}` → no call, no throw) · trigger: `handleInboundMessage(inst, msg)` per row · observable: per-row expectation (test-plan E5, X5)
- [ ] 3.11 Test: extracted modules never import the entry point — see `packages/shared/src/__tests__/no-direct-child-process.test.ts` for the import-scan harness. Input: `packages/extension/src/bridge-handlers/*.ts` · trigger: parse import specifiers · observable: none resolves to `../bridge.ts` or `./bridge.js` (test-plan E6)
- [ ] 3.12 Test: retargeted multiselect guard fails on a re-added arm — extend `no-tui-multiselect-arm-regression.test.ts`. Input: `tui-prompt-adapter.ts` source and a copy with `else if (prompt.type === "multiselect" && originals.custom) {}` appended · trigger: run the guard predicate on both · observable: original passes, mutated fails (test-plan E7). Also flip task 1.6's expected `bridge.ts` `lines.pct` to `> 0`.
- [ ] 3.13 Test: every source-reading test's needle is present in the file it now reads — new `packages/extension/src/__tests__/source-guards-not-vacuous.test.ts`; see `no-tui-multiselect-arm-regression.test.ts` for the read-source harness. Input: the 10 reader test files, each's target path + at least one needle extracted by regex · trigger: read target · observable: needle found (test-plan E8). Delete `scripts/bridge-closure-inventory.mjs`.
- [ ] 3.14 Test: `session_start` delegates session change to `session-sync` — see `packages/extension/src/__tests__/session-register-carries-pid.test.ts` for the session-sync mock harness. Input: `lifecycle.ts` with `session-sync` mocked · trigger: `session_start` with `reason: "resume"` · observable: `handleSessionChange` called once with `inst.sessionId` (test-plan E9)
- [ ] 3.15 Test: reload takeover keeps the old instance inert — same harness as 3.9. Input: `shared = {generation: 1}`; `instA = createInstance(pi, 1, shared)`, `instB = createInstance(pi, 2, shared)`; `shared.generation = 2`; `turn_end` handlers wired to both · trigger: fire `turn_end` on both · observable: `instA.isActive() === false` and A sends nothing; B sends its `turn_end`-driven message exactly once (test-plan X2)
- [ ] 3.16 Test: cross-reload values adopted, not defaulted — same harness. Input: `shared = {generation: 1, sessionId: "S1", attachedChange: "c1", shouldStopAfterTurn: true}` · trigger: `createInstance(pi, 2, shared)` · observable: `inst.shared.sessionId === "S1"`, `attachedChange === "c1"`, `shouldStopAfterTurn === true` (test-plan X3)
- [ ] 3.17 Test: subagent re-entry guard unchanged — extend `packages/extension/src/__tests__/bridge-followup-queue-drain.test.ts` if it already covers `prev.pi`, else add beside it. Input: `process[BRIDGE_KEY] = {generation: 1, pi: piA}` · trigger: call the default export with `piB` · observable: `generation` still 1; no `on` registered on `piB` (test-plan X4)
- [ ] 3.18 Test: `session_start` with `ctx.hasUI === false` — same harness as 3.9. Input: stub ctx `{hasUI: false}` · trigger: `session_start` · observable: `ui-prompt-patch` not invoked, TUI adapter not registered, dashboard adapter registered (test-plan X6)
- [ ] 3.19 Update the `bridge-extension`, `command-routing`, `mid-turn-prompt-queue`, `multiselect-dialog`, `agent-session-context-injection` delta specs only if a module name chosen in 3.3–3.7 differs from design D2; otherwise no edit (deltas are file-neutral).

## 4. Slice D — Hygiene, node-env split, isolate gate (design D5, D6, D7, D8)

- [ ] 4.1 Measure baseline: `HOME=$(mktemp -d) npx vitest run --project @blackbelt-technology/pi-dashboard-web --reporter=json` and the same for `shared`; record wall / `environment` / `import` in design.md `## Measurements` as the baseline for 4.18.
- [ ] 4.2 Classify pure client tests: script over `packages/client/src/**/__tests__/*.test.ts` (never `.tsx`) whose text and direct in-repo imports contain none of `react`, `@testing-library`, `document.`, `window.`, `localStorage`, `navigator`; write the resulting glob list.
- [ ] 4.3 Add `packages/client/vitest.node.config.ts` importing the base config and `PARALLEL_MAX_WORKERS` from `../../vitest.workers`, spreading `base.resolve`, setting `name: "@blackbelt-technology/pi-dashboard-web-node"`, `environment: "node"`, `pool: "forks"`, `maxWorkers`, the 4.2 `include`, `globalSetup` and `testTimeout` from base, no `setupFiles`; add the file path to root `vitest.config.ts` `test.projects`; add the same globs to `exclude` in `packages/client/vitest.config.ts`. Run the node project; move any file failing for a DOM reason back to jsdom.
- [ ] 4.4 Test: no file included by both client projects, none dropped — see `packages/shared/src/__tests__/sync-versions-spec.test.ts` for the read-config harness. Input: resolved `include`/`exclude` of both client configs globbed against `packages/client/src` · trigger: set ops · observable: intersection empty; union equals the pre-change jsdom include set (record that set's size as a constant) (test-plan E11)
- [ ] 4.5 Test: node config imports the worker module, no literal — extend the existing `vitest.workers` single-source guard (`rg -l vitest.workers --glob '**/__tests__/*.test.ts' packages` → nearest) . Input: `packages/client/vitest.node.config.ts` text · trigger: grep · observable: contains `from "../../vitest.workers"`, no `maxWorkers:` followed by a literal (test-plan E12)
- [ ] 4.6 Test: a DOM test resolves to jsdom only — same harness as 4.4. Input: a `render()`-calling `.tsx` test path · trigger: match against both includes · observable: jsdom only (test-plan E13)
- [ ] 4.7 Test: misclassified pure test fails loudly — see `scripts/__tests__/knip-scan.test.mjs` for the subprocess harness. Input: a temp test file in the node include set that references `document` · trigger: run the node project on it · observable: failure output contains `document is not defined` (test-plan X7)
- [ ] 4.8 Hygiene sweep: run the jsdom client project with `--no-isolate`, group failures by message, add to `packages/client/src/test-support/` a `restoreGlobals` afterEach (localStorage descriptor, `getBoundingClientRect`, `matchMedia`, timers) and a `renderWithProviders` helper that wraps in the default `ThemeProvider`; fix residual files; repeat until `--no-isolate` reports 0 failures.
- [ ] 4.9 Test: shared setup restores a replaced `localStorage` — see `packages/client/src/__tests__/display-prefs-migration.test.ts` for a localStorage-touching client test. Input: a test assigning `globalThis.localStorage = stub` · trigger: shared `afterEach` · observable: `typeof localStorage.clear === "function"` and the property descriptor equals the original (test-plan F4)
- [ ] 4.10 Test: `renderWithProviders` supplies a default `ThemeProvider` — see `packages/client/src/__tests__/multi-ask-panel.test.tsx` for a ThemeProvider-wrapped render. Input: a component calling `useThemeContext()` rendered without an explicit provider · trigger: render via the helper · observable: no throw; default theme applied (test-plan F5)
- [ ] 4.11 Test: per-file HOME holds under `isolate:false` — see `packages/shared/src/test-support/AGENTS.md` for the per-file HOME hook. Input: two client test files each writing `$HOME/.pi/marker-<file>` · trigger: run with `--no-isolate` · observable: each sees only its own marker; `process.env.HOME` differs between them (test-plan F2)
- [ ] 4.12 Test: localStorage is clean on entry under `isolate:false`, shuffled — same pair of files. Input: each writes `localStorage.setItem("k", <file>)` · trigger: `--no-isolate --sequence.shuffle` · observable: `localStorage.getItem("k") === null` on entry in both (test-plan F3)
- [ ] 4.13 Test (ci-scenario): order independence — see `scripts/__tests__/knip-scan.test.mjs` harness. Input: jsdom client project · trigger: `vitest run --project … --sequence.shuffle --sequence.seed=1` then `--sequence.seed=2` · observable: both exit 0 with the same passed/failed counts as the default order (test-plan F1)
- [ ] 4.14 Test (ci-scenario): three consecutive green runs on the gate commit — same harness. Input: jsdom client project · trigger: three sequential `vitest run --project …` subprocesses · observable: 3/3 exit 0 (test-plan P2)
- [ ] 4.15 Decide the isolate gate: if 4.11–4.14 all pass, set `isolate: false` in `packages/client/vitest.config.ts` with a comment citing this change; if any fails, leave `isolate: true` and record which condition failed under design.md `## Risks / Trade-offs`.
- [ ] 4.16 Split `packages/shared/src/tool-registry/__tests__/ensure-cli.test.ts`, `packages/shared/src/platform/__tests__/git-checkout-roots.test.ts`, `packages/shared/src/__tests__/openspec-poller-parity.test.ts` by scenario group so no file exceeds ~10s; assertion count unchanged.
- [ ] 4.17 Test: split shared files stay under 10s each and lose no assertions — see `packages/electron/src/__tests__/async-dedupe-guards.test.ts` for the `--reporter=json` parse harness. Input: `vitest run --project shared --reporter=json` · trigger: read per-file `duration` and assertion totals for the split groups · observable: every split file < 10 000 ms; summed assertion count equals the pre-split constant (test-plan E14)
- [ ] 4.18 Record after-measurements (3 runs) in design.md `## Measurements` next to the 4.1 baseline (test-plan P1, manual-only).

## 5. Closeout

- [ ] 5.1 Update the directory `AGENTS.md` rows: `packages/extension/src/AGENTS.md` (new `bridge-handlers/` dir → scaffold `bridge-handlers/AGENTS.md`; `bridge.ts` row purpose), `packages/client/src/test-support/AGENTS.md` (new helpers), `packages/client/AGENTS.md` (`vitest.node.config.ts`), root-level rows for `vitest.config.ts` and `package.json` if a root `AGENTS.md` tree row exists for them.
- [ ] 5.2 `npm run quality:changed`; `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` green; `npm run test:coverage` writes a report.
