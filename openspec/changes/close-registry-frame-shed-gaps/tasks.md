## 1. `sessions_reordered` → state (`packages/server/src/pairing/browser-gateway.ts`)

- [ ] 1.1 Test class + key — see `packages/server/src/__tests__/browser-gateway-dropped-frames.test.ts`. Triple: `sessions_reordered {cwd:"/a"}` · call `frameClassOf(msg)` · returns `{cls:"state", key:"sessions_reordered:/a"}` (test-plan #E1). Verify red first.
- [ ] 1.2 Test per-entity key sharing — see `packages/server/src/__tests__/browser-gateway-dropped-frames.test.ts`. Triple: two reorders for `/a` and one for `/b` · call `frameClassOf` on each · the two `/a` frames yield an identical key and the `/b` key differs (test-plan #E2). Verify red first.
- [ ] 1.3 Test deferred reorder is latest-wins — see `packages/server/src/__tests__/browser-gateway-host-pressure-reconcile.test.ts` for the force-shed/drain pattern. Triple: socket above threshold with reorders `/repoA`=[a,b],[b,a],[a,b] then `/repoB`=[c] · socket drains · exactly 2 frames received, one `/repoA` carrying the last ordering `[a,b]` and one `/repoB`, with `droppedFrames.total` unchanged by them (test-plan #E3). Verify red first.
- [ ] 1.4 Test retention is coalesced not accumulated — see `packages/server/src/__tests__/browser-gateway-load.test.ts`. Triple: one socket held above threshold with 200 reorders spread across 20 group keys · measure retained pending-state reorder frames · ≤1 per group key (≤20 total), independent of the 200 event count (test-plan #P1). Verify red first.
- [ ] 1.5 Implement D1 (one `case` in `frameClassOf`). Verify 1.1–1.4 green and the existing `browser-gateway-*` / `ws-frame-delivery` tests green.

## 2. Protocol + session-manager invariant

- [ ] 2.1 Add the additive optional `reconciled?: boolean` to `SessionAddedMessage` in `packages/shared/src/browser-protocol.ts`. No behaviour yet; `npm run build` clean.
- [ ] 2.2 Test that registration is the only path to a non-ended record — see `packages/server/src/__tests__/session-death-attribution.test.ts`. Triple: a `memory-session-manager` instance · enumerate every write path (`register`, `unregister`, `update`, `restore`, `remove`) · only `register` yields a non-ended `status`, and a `restore`d record stays `ended` (test-plan #E12). This pins the invariant the reconcile rule depends on; it should pass green on current code and fail if a future path revives a record without registering.

## 3. Registry debt register (`browser-gateway.ts`)

- [ ] 3.1 Test kind precedence — see `packages/server/src/__tests__/browser-gateway-host-pressure-reconcile.test.ts`. Triple: a debt entry pre-seeded with each of `updated`/`added`/`removed` · record each of the three kinds over it (9 combinations) · resulting kind is the newly recorded one except `updated` over `added` → `added` and `updated` over `removed` → `removed` (test-plan #E4). Verify red first.
- [ ] 3.2 Test `sawAdd` survives a kind supersede — same file. Triple: shed `session_added` then shed `session_removed` for `s7` · inspect the entry before flush · kind is `removed` AND `sawAdd` is still true (test-plan #E11). Verify red first.
- [ ] 3.3 Test flush branch — owed `removed`, no record — same file. Triple: debt `{kind:"removed"}` for `s1` with `sessionManager.get("s1")` undefined · flush below threshold · socket receives `session_removed {sessionId:"s1"}` (test-plan #E5). Verify red first.
- [ ] 3.4 Test flush branch — owed `removed`, record no longer ended — same file. Triple: debt `{kind:"removed"}` for `s2` with a record whose `status` is `"active"` · flush · socket receives `session_added` for `s2` with `reconciled:true` and NO `session_removed` (test-plan #E6). Verify red first.
- [ ] 3.5 Test flush branch — owed `removed`, record ended, `sawAdd` true — same file. Triple: debt `{kind:"removed", sawAdd:true}` for `s3` with an ended record · flush · socket receives `session_added` for `s3` carrying the ended record and `reconciled:true`, and NO bare `session_removed` (test-plan #E7). Verify red first.
- [ ] 3.6 Test flush branch — owed `removed`, record ended, `sawAdd` false — same file. Triple: debt `{kind:"removed", sawAdd:false}` for `s4` with an ended record · flush · socket receives `session_removed {sessionId:"s4"}` and NO `session_updated` (test-plan #E8). Verify red first.
- [ ] 3.7 Test flush branch — owed `added` — same file. Triple: shed `session_added` for `s5` carrying `spawnRequestId:"r1"` · flush while `s5` exists · socket receives `session_added` with the current full record, `spawnRequestId:"r1"` and `reconciled:true` (test-plan #E9). Verify red first.
- [ ] 3.8 Test flush branch — owed `updated` carries the clearing values — same file. Triple: shed `session_updated` for `s6` whose record has `currentTool` and `hostPressure` unset · flush · frame carries current `status`, `currentTool:null` and `hostPressure:null` — not `undefined`, not the stale tool name (test-plan #E10). Verify red first. `hostPressure` is load-bearing here (`See change: fix-false-unresponsive-badge`).
- [ ] 3.9 Test an archived session reconciles as removed — same file. Triple: debt owed for `s8`, `s8` archived so no record remains · flush · socket receives `session_removed {sessionId:"s8"}` (test-plan #E15). Verify red first.
- [ ] 3.10 Test counters count every kind — see `packages/server/src/__tests__/browser-gateway-dropped-frames.test.ts`. Triple: one shed frame of each of the three kinds · flush · `statusReconcileQueued` +3 and `statusReconcileSent` +3, and no new `/api/health` field appears (test-plan #E14). Verify red first.
- [ ] 3.11 Test the debt record is byte-free — same file. Triple: 500 shed registry frames of mixed kinds on one socket · read the pending-state byte accounting · bytes attributable to the debt register are 0 and `stalledSocketsTerminated` is not incremented by it (test-plan #E13). Verify red first.
- [ ] 3.12 Test the register stays O(ids) under flood — see `packages/server/src/__tests__/browser-gateway-load.test.ts`. Triple: 1000 distinct sessions each shedding all three kinds on one socket · measure until drain · entry count ≤1000, per-entry payload bytes 0, no queued frame retained (test-plan #P2). Verify red first.
- [ ] 3.13 Test a shed reconcile re-records for every kind — see `packages/server/src/__tests__/browser-gateway-host-pressure-reconcile.test.ts`. Triple: socket re-crosses the threshold during flush · a reconcile `session_added` and a reconcile `session_removed` are each themselves shed · both ids remain owed with their kind intact and are retried on a later drain (test-plan #X1). Verify red first. This requires amending the `sendTo` shed site, which today re-records only `session_updated` and reads `msg.sessionId` — absent on `session_added`.
- [ ] 3.14 Test teardown releases the register and its timer — same file. Triple: debt outstanding · socket close, socket error, and stalled-terminate in turn · entries dropped and the reconcile interval cleared in all three paths, no leaked timer (test-plan #X2). Verify red first.
- [ ] 3.15 Test a partial drain leaves the remainder owed — same file. Triple: 5 owed ids with the threshold re-crossed after the 2nd · flush · the first 2 delivered and counted sent, the remaining 3 still owed, `statusReconcileSent` counting only delivered frames (test-plan #X3). Verify red first.
- [ ] 3.15b Test a delivered lifecycle frame clears older debt — same file. Triple: `session_added` for `s10` shed (debt `{kind:"added", sawAdd:true}`), then `session_removed` for `s10` delivered successfully, then flush after drain with `s10`'s record ended · flush · NO reconciled `session_added` for `s10` is emitted and the ended row is not resurrected (test-plan #E21). Verify red first.
- [ ] 3.16 Implement D2: `StatusDebt.entries` map carrying `kind` + `spawnRequestId?` + `sawAdd`, LWW kind precedence, debt cleared on a successful lifecycle delivery (without discarding debt recorded during the send), `broadcast()` dirty derivation for the three types (`session_added` id lives at `msg.session.id`), the `sendTo` shed-site re-record for all three kinds, and the ordered `flushStatusDebt` dispatch reusing the `session_added` record-shaping helper. Update the `DroppedFrameStats` doc comment to say registry frames rather than `session_updated`. Verify 3.1–3.15b green and the existing reconcile scenarios green.

## 4. Client — reconciled add handling (`packages/client/src/hooks/useMessageHandler.ts`)

- [ ] 4.1 Test a reconciled add never navigates on any tier — see `packages/client/src/hooks/__tests__/useMessageHandler.tier25-fallback.test.tsx`. Triple: browser displaying session `X` with pending spawn `r2` outstanding · `session_added {spawnRequestId:"r2", reconciled:true}`, then separately a reconciled add matching only by cwd and one matching only by worktree · displayed session stays `X` in all three cases, and the `r2` pending-spawn record is consumed with its placeholder cleared (test-plan #F3). Verify red first.
- [ ] 4.2 Test a reconciled add with no request id touches no spawn state — same file. Triple: an unrelated user spawn pending for `/repoA` with its own placeholder · `session_added` for a DIFFERENT `/repoA` session with `reconciled:true` and no `spawnRequestId` · displayed session unchanged, the `/repoA` placeholder still present, and the unrelated spawn still auto-navigates when its own `session_added` arrives (test-plan #F4). Verify red first.
- [ ] 4.3 Test a reconciled add is an upsert that spares siblings — see `packages/client/src/hooks/__tests__/useMessageHandler.snapshot-replace.test.tsx`. Triple: client holds `s5` plus a sibling in the same cwd with `resuming:true` · reconciled `session_added` for `s5` · the `s5` row is updated not duplicated and the sibling's `resuming` flag is still true (test-plan #F5). Verify red first.
- [ ] 4.4 Test an added ended session counts toward the ended total — same file. Triple: client holds no `s9` and `endedTotals["/repoA"]` is 4 · `session_added` for `s9` with `status:"ended"` and group key `/repoA` · `endedTotals["/repoA"]` becomes 5 and the expander label agrees with the rendered ended rows (test-plan #E16). Verify red first.
- [ ] 4.5 Test a re-delivered ended session is not double-counted — same file. Triple: client already holds ended `s9` with `endedTotals["/repoA"]` at 5 · a second `session_added` for `s9` carrying the same ended record · the total stays 5 (test-plan #E17). Verify red first.
- [ ] 4.6 Test deferred reorder does not lose a concurrently added session — see `packages/client/src/hooks/__tests__/useMessageHandler.snapshot-replace.test.tsx`. Triple: client holds order `[a,b]` for `/repoA` · `session_added` for `c` then a deferred `sessions_reordered` for `/repoA` omitting `c` · order converges to `[...reordered, c]` with `c` retained at the tail, never evicted (test-plan #F6). Verify red first.
- [ ] 4.7 Implement the client reconciled-add path: merge upsert (`{...existing, ...msg.session}`), navigation gated off for every spawn-correlation tier when `reconciled` is set, pending-spawn/placeholder cleanup only on an exact `spawnRequestId` match, and the `endedTotals` bump for a not-previously-held already-ended session. Verify 4.1–4.6 green.

## 5. Client — paging release + exhausted (`useMessageHandler.ts`, `SessionList.tsx`, `App.tsx`)

- [ ] 5.1 Test page reply sets generation and exhausted — see `packages/client/src/hooks/__tests__/useMessageHandler.snapshot-replace.test.tsx`. Triple: `pageReplyGen["/a"]`=1 with `/a` not exhausted · `sessions_page_result {cwd:"/a", sessions:[], order:[], hasMore:false}` then a second with `hasMore:true` · after the first gen=2 and `/a` exhausted, after the second gen=3 and `/a` not exhausted (test-plan #E18). Verify red first.
- [ ] 5.2 Test every `endedTotals` mutation re-arms paging — same file. Triple: `/repoA` marked exhausted · each of the six mutations in turn (`session_updated`→ended, `session_removed`, `session_archived`, snapshot, the `App.tsx` server-switch reset, `session_added` of an unheld ended session) · the exhausted mark clears in all six cases (test-plan #E19). Verify red first. Prefer wiring the clear as a diff on the map rather than an enumerated call-site list.
- [ ] 5.3 Test the paging marks live in the group-key space — same file. Triple: worktree session cwd `/repoA/.worktrees/wt1` with group key `/repoA` and a `sessions_page_result {cwd:"/repoA", hasMore:false}` · that session transitions to ended, changing `endedTotals["/repoA"]` · the exhausted mark clears, proving no raw-cwd/group-key split (test-plan #E20). Verify red first.
- [ ] 5.4 Test the in-flight mark releases on any reply — see `packages/client/src/components/__tests__/SessionList.test.tsx`. Triple: page request in flight for `/repoA` under fake timers with `pagedCount` unchanged · `sessions_page_result {cwd:"/repoA", sessions:[], hasMore:false}` · the mark releases immediately with the 15 s timer never advanced, proving release does not depend on the timeout (test-plan #F1). Verify red first.
- [ ] 5.5 Test the exhausted cwd suppresses the affordance and re-arms — same file. Triple: `endedTotals["/repoA"]` exceeding held ended with the last reply `hasMore:false` · render, click where "more" was, then change `endedTotals["/repoA"]` · no "more" control rendered, the click sends no `sessions_page`, and the control returns after the total changes (test-plan #F2). Verify red first.
- [ ] 5.6 Implement D3: `pageReplyGen` + `pageExhausted` derived in `useMessageHandler`, owned and threaded from `App.tsx`; `SessionList` release effect keyed on `pageReplyGen`; `requestEndedPage` and the affordance predicate additionally requiring not-exhausted; snapshot clearing the exhausted mark unconditionally. Verify 5.1–5.5 green and `npm run build` clean.

## 6. End-to-end (`tests/e2e/`)

- [ ] 6.1 E2E — a shed registry burst still yields a complete sidebar. See `tests/e2e/sessions-page-stub-group.spec.ts` for harness attach + seeding. Triple: harness dashboard with a browser socket driven above the shed threshold while sessions are spawned and ended · socket drains · within 1 s the sidebar shows a row for every session the server holds with statuses matching `/api/health`, and no session is missing until reconnect (test-plan #F7). Read the port from `.pi-test-harness.json` (`dashboardPort`), never a hardcoded `:18000`.
- [ ] 6.2 E2E — paging never dead-ends on the same offset. Same exemplar. Triple: harness with a stub ended group whose pageable set is shrunk out-of-band so the reply is empty · expand the ended list, then trigger an `endedTotals` change · the "more" control disappears after the empty reply rather than re-requesting the same offset, and reappears after the total changes (test-plan #F8).
- [ ] 6.3 E2E — a reconnect snapshot re-arms a stale exhausted mark. Same exemplar. Triple: `/repoA` marked exhausted, then the socket drops and reconnects with identical `endedTotals` · the reconnect snapshot is applied · the exhausted mark clears and the "more" control is available again despite byte-identical totals (test-plan #X4).

## 7. Manual verification (deferred post-merge)

- [ ] 7.1 Watch the sidebar settle during a large drain-and-reconcile burst and confirm no distracting flicker, reshuffling, or scroll jump (test-plan: manual-only).

## 8. Docs + closeout

- [ ] 8.1 Delegate to `DocScribe`: `docs/architecture.md` "Frame delivery policy" — `sessions_reordered` in the state list; the debt register covering add/remove with the `sawAdd` and status-based removal rules; the `reconciled` flag; the paging release/exhausted rule. Verify `sessions_reordered` appears under the state class in the doc.
- [ ] 8.2 `AGENTS.md` rows: `packages/server/src/pairing/browser-gateway.ts.AGENTS.md` (class, debt shape, flush order), `packages/shared/src/` row for the `reconciled` field, `packages/client/src/hooks/AGENTS.md` (`useMessageHandler.ts`), `packages/client/src/components/session/AGENTS.md` (`SessionList.tsx`), and the `App.tsx` row. Verify `kb dox lint` clean.
- [ ] 8.3 `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` zero failures; `npm run quality:changed` clean; comment on #648 and #649 (noting items 2–3 were already fixed) with the change name.
